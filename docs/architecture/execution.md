# Framework implementation evidence

Canonical plan: `docs/plans/2026-09-22-1141-feat-loom-full-framework-plan.md`.
Execution uses the current Codex session with an active goal, sequentially per project instructions. No external model or worker route is selected.

## Starting state

The supplied folder was a Bun starter without Git. Git initialization was authorized, and the supplied starter and conversation/plan were preserved in commit `fa443b7` on `feat/loom-framework`. No remote is configured. Package namespace and publication remain undecided.

## Workspace foundation (U1)

Nine workspaces are declared, including two independent example packages. Core and tooling export compiled ESM and declarations; Node, browser, and Bun TypeScript presets separate ambient APIs. Astro receives its supported preset when its integration is installed.

Oxlint and @oxlint/plugins are both pinned at 1.85.0. All 15 generic anti-slop rules are errors. The plugin is vendored in `tools/oxlint/anti-slop`; agent assets and vendored code are excluded. Lint is a Turbo root task per the official Oxc guide. Effect rules will be enabled when the validator compatibility fixture adds Effect as a direct dependency.

Configuration-only work uses install/build/typecheck/lint and task-graph smoke verification instead of synthetic unit tests. The central test command intentionally fails until real tests exist. Empty module entry points reserve package boundaries; they do not claim implemented framework APIs.

Observed: frozen install, library builds, seven package typechecks and root lint passed. Turbo discovered all nine workspaces. After removing two generated JS artifacts, a second build restored both from cache (2/2 hits). Browser-only typechecking passed without Node or Bun ambient types. Root Turbo lint reported zero warnings and errors.

## Remaining units

U2 local compatibility: Standard Schema tests passed for four vendors; Drizzle additive/rename generation and ambiguity rejection passed; PostgreSQL 18 generated migration replay, references, RQB v2, UUIDv7, milliseconds, and populated rename passed. Node 24 imported compiled exports without Bun globals, and a fresh directory installed a packed core artifact and imported server/client/React exports. Astro/Fumadocs build and check passed in a TypeScript 6 probe. The user subsequently required TypeScript 7; all workspace manifests now pin 7.0.2, and docs use Astro sync plus tsc and build, with missing Astro-specific semantic diagnostics recorded in compatibility.md. The user authorized the narrow Drizzle patch after reproduction. See compatibility.md for exact versions and provider acceptance still required.

## Schema compiler (U3)

Implemented immutable field/table declarations and structural metadata, deterministic fingerprints, native Drizzle tables, branded entity IDs, forward/circular references, fixed SQL naming, numeric precision, JSON, enum check constraints, composite indexes and uniqueness. PostgreSQL owns UUIDv7 and bigint millisecond defaults. Migration bootstrap statements install triggers rejecting system-field updates; safe-integer decoding rejects overflowing timestamps.

Evidence: new schema tests first failed on the missing public export, then passed after implementation. Consumer type fixtures reject wrong-entity IDs, missing required fields, invalid enums/defaults and unknown index fields. An actual PostgreSQL migration replay caught parameterized DDL literals, fixed with Drizzle literal encoding. Direct SQL checks verify FK restrictions, enum constraints and system-field immutability. Mutable author inputs are snapshotted. Full workspace check passed (13 tasks, 9 unit tests); PostgreSQL/Node integration passed (8 tests). No casts are needed in consumers.

The compact object grammar cannot detect a JavaScript duplicate key that was already overwritten before the callback returns; TypeScript diagnoses duplicate literal keys. Distinct entity/field names that map to the same SQL identifier are rejected. Enum storage is native text plus a database CHECK constraint, avoiding separately mutable PostgreSQL enum types. Trigger installation is an explicit migration operation; runtime startup does not install it.

## Validation boundaries (U4)

Fields accept Standard Schema v1 validators without vendor introspection. Derived storage, insert, patch, command and public interfaces preserve validator input versus storage output types. Valibot 1.5.0 is the internal storage/wire parser; author validators remain vendor-independent. Normalized field and cross-field outputs are checked again against storage and allowed keys. Server/system writes and unknown keys are rejected. Public masks default empty and can explicitly include system IDs. Patch omission remains unchanged; explicit undefined is rejected; insert-only validator defaults are supported. Bigint and numeric precision use strings on the wire; dates become ISO strings and unsafe wire integers/nonfinite numbers fail.

Evidence: validation exports failed before implementation; strengthened insert-default tests failed before fixing omission handling. Runtime and type fixtures cover four vendors, async issues, transformed output rejection, all masks, opt-in system projection, cross-field checks and attempted mask widening. Full check passed (13 tasks, 18 unit tests); database/Node integration remained green (8 tests). Structural fingerprints intentionally omit validator implementation; build/content identity must account for validation-code changes in U6.

## Relations and database access (U5)

Runtime connection creation accepts native Relations v2 definitions over the exact compiled table identities. It rejects mismatched schemas, unsupported protocols and invalid pool sizes before opening a socket, then verifies PostgreSQL 18 before returning a native Drizzle database and its pool. Connections use small bounded pools and explicit cleanup. Idle pool failures publish a payload-free diagnostics-channel event. No migration credential or DDL is used in this path.

Evidence: unit tests first failed on the absent connection export. The PostgreSQL fixture exercises self-relations, distinct author/reviewer references, a many-to-many junction, relation filters, nested projection, ordering and a repeatable-read read-only transaction. Type fixtures retain nullable relations and reject unknown filters. Full check passed (13 tasks, 20 unit tests); 9 PostgreSQL/Node integration tests passed.

At the U3-U5 boundary, ce-simplify-code's three prompt assets were reviewed sequentially per workspace instructions. Reuse: no behavior-equivalent duplication worth extracting (0 applied). Quality: replaced nested boundary-selection expressions with explicit branches (2 applied). Efficiency: replaced repeated relation-target array construction/scans with one Set (1 applied). Per-validation normalized-boundary caching was skipped as low value (1 skipped). Existing validation and safety checks were preserved; full checks above ran after the changes. This is simplification evidence, not the final ce-code-review receipt.

## Configuration and generated contracts (U6, partial)

Implemented strict versioned configuration, bounded defaults, path containment, registered public/internal function definitions, deterministic discovery and content-versioned generated contracts. The CLI currently supports init, generate, schema inspect, schema diff, migrations generate and doctor, with structured/human output and stable usage/project-error exit codes. Initialization preserves existing files and rejects escaping backend symlinks. Executable project errors are not echoed, since arbitrary messages can contain credentials.

Generated public references contain only identity metadata; type-only imports preserve argument/return validator contracts. Internal references and server registry are separate artifacts. Complete generations are staged before an atomic current-link replacement. Stable JS/declaration wrappers and equal-depth generation directories preserve TypeScript 7 and runtime resolution. Build identity includes bundled configuration and source, package manifest and Bun lockfile. Repeated generation verifies existing artifacts before activation.

Evidence: configuration/discovery tests first failed on missing exports. Integration caught and fixed initialization through an escaping symlink, Bun output-directory resolution, and TypeScript's logical symlink-relative imports. Four CLI integration tests pass (33 assertions), including a fresh TypeScript 7 consumer, browser bundling without internal references, source-change invalidation, failed-generation preservation, real CLI success, sanitized errors and usage exit codes. Full workspace check passes (13 tasks, 23 unit tests). Astro build/typecheck initially raced over Vite's dependency cache; the docs typecheck now depends on its build in Turbo.

U6 is not complete: dev and deployment commands depend on U8/U14. Concurrent generation coordination remains part of U8. Migration application/status are now implemented as described below.

## Migration planning (U7, partial)

The adapter owns the pinned Drizzle snapshot/diff entry points; no subprocess or independent SQL diff engine is used. Snapshot identities are structural hashes rather than random Drizzle IDs. The planner accepts an explicit baseline, generates SQL through Drizzle and classifies structural changes conservatively: nullable/defaulted additions can be automatic, while required backfills, constraint validation, type changes, deletions and unsupported changes require review. Concurrent indexes are marked nontransactional. Explicit renames preserve data but still require review.

Evidence: three planner tests failed on missing exports before implementation, then passed (13 assertions). An actual PostgreSQL 18 fixture creates the application namespace, applies an initial baseline, inserts a row, expands the live schema and verifies that planning against the original baseline retains the release addition. Explicit rename preserves the populated row; subsequent planning produces no SQL. A subprocess test verifies that resolver progress does not pollute stdout. The narrow patch now uses Drizzle's existing async context for noninteractive, quiet resolution. A fresh standalone consumer installed the patch, verified ambiguous rejection and explicit rename through both ESM and CommonJS under Node 24, and passed frozen install. Full workspace check passes (13 tasks, 26 unit tests, zero lint warnings).

Artifact persistence now writes complete SQL/plan directories through staging and an atomic rename under an exclusive filesystem generation lock. Reads validate the pinned supported snapshot vocabulary, recompute full artifact and structural hashes, compare the SQL file, and reconstruct one connected history from an empty baseline. Source-control review establishes trust; hashes detect edits, not malicious replacement of both content and hashes. Unsupported snapshot entities fail closed. The CLI accepts explicit rename hints from a project-relative JSON file and labels diff output as committed-baseline planning.

Additional evidence: complex snapshot round-trips cover foreign keys, enums, uniqueness, composite indexes, numeric and JSON fields. Tests reject duplicate baselines, altered SQL and changed snapshots. CLI subprocess tests generate readable release artifacts and subsequently report an empty diff. Four planner tests pass (17 assertions), and four CLI tests pass (41 assertions).

Database bootstrap now uses a dedicated PostgreSQL 18 migration session, bounded lock/statement waits and a transaction-scoped bootstrap lock. Framework metadata version/hash history is separate from application migration history. Bootstrap creates a NOLOGIN runtime role, rejects administrative authority and other role memberships, and denies runtime/public metadata access. Credential provisioning for that role remains provider work; this fixture exercises it with SET ROLE. Per-schema default-privilege revokes cannot override PostgreSQL's global defaults, so concrete function grants are revoked when artifacts are applied rather than relying on that ineffective setting ([PostgreSQL default privileges](https://www.postgresql.org/docs/18/sql-alterdefaultprivileges.html)).

Application migration execution takes a session advisory lock on a dedicated connection, reads and validates artifacts under the lock, checks applied history against both Loom and ORM ledgers, and checks catalog drift. Drizzle's exported programmatic ORM migrator runs within an outer transaction; its nested transaction is a savepoint. SQL, system-field triggers, explicit runtime DML grants and both history entries commit together. Changes requiring review require the exact artifact hash. Nontransactional changes currently fail closed pending their recovery runner. Catalog evidence includes relations, columns, constraints, indexes, triggers, routines, policies, grants, views, sequence definitions and types, excluding row contents and sequence counters.

Evidence: bootstrap tests failed on the missing export before implementation. A follow-up inherited-role test reproduced excessive authority before the guard was fixed. Bootstrap/drift integration tests pass (12 assertions). The runner integration test (written after the first runner implementation) passes 13 assertions: concurrent callers apply once, runtime cannot alter schema or mutate system fields, unreviewed changes stop, a populated required-field failure rolls back both schema and history, backfill/retry succeeds, fresh replay converges on the upgraded catalog, and external DDL stops execution. The workspace check passes with TypeScript 7 and zero lint warnings.

Migration CLI application/status now use the configured migration URL environment variable, distinct from the runtime credential variable. Application requires an explicit runtime role, checks that the source schema has generated release artifacts, verifies pending review hashes, and rechecks source content identity under the migration lock. Receipts identify the database, role and namespace without including the URL. Status takes a shared advisory lock and a repeatable-read read-only transaction; it never bootstraps metadata. It reports pending artifacts, catalog drift, diverged application/ORM history and invalid framework version/hash history. Known command errors use fixed messages and database failures are sanitized; inconsistent status returns exit class 4.

Evidence: status on an untouched database reports pending work without creating a metadata namespace. Extended runner tests verify applied status, live drift and altered framework history. A real CLI subprocess fixture generates, applies and inspects artifacts, verifies target identity, rejects missing runtime-role input and ungenerated schema, reports external DDL drift, and checks that the disposable connection password is absent from output. This fixture passes 28 assertions. These status/CLI tests followed implementation; they are integration evidence rather than a test-first claim.

U7 custom artifact validation is implemented below; nontransactional execution and recovery remain U16 work. U8 must recheck source fingerprints under its development-sync lock. U8-U20 remain unimplemented; no full runtime, development lifecycle, browser interaction, cloud, capacity, or release acceptance is claimed by these checks.

## Custom SQL artifacts and toolchain update

Migration format 2 records the parent artifact hash separately from schema fingerprints. This permits successive data-only backfills while rejecting branches, stale writers and disconnected history. Custom SQL is parsed with pinned MIT-licensed libpg-query 18.1.5, using PostgreSQL statement byte offsets so Unicode, comments, strings and function bodies retain their boundaries. Supported statement classes are explicit; top-level transaction/session control is rejected. Custom SQL always requires exact-hash review, and concurrent indexes require an explicit nontransactional marker. These checks define an execution contract, not a sandbox: reviewed SQL uses migration credentials. The transactional runner still refuses nontransactional artifacts until U16 provides recovery.

The CLI accepts `migrations generate --name <name> --sql <project-relative file> --mode transactional|nontransactional`. A real CLI fixture exposed property-order differences between parsed committed snapshots and fresh Drizzle snapshots; classification now canonicalizes both with the pinned snapshot validator. It verifies data-only generation, required review, application, and preserved history. Unit tests cover statement boundaries, forbidden control, invalid SQL, concurrent-index marking and stale data-only writers. Tests followed implementation in this unit.

The user requested Vite+ and explicitly extensionless source imports. Vite+ 1.0.0-rc.0 now supplies Oxlint 1.85.0, Oxfmt 0.70.0, Vitest 5.0.1 and library packing. Root lint/format settings live in vite.config.ts; all anti-slop rules remain errors, and plugin authoring imports use Vite+'s bundled entry. Turborepo remains the task orchestrator. Static checks depend on compiled package outputs, and the root Vite config participates in cache identity. Unit tests run through Vite+; Bun integration tests retain their actual Bun runtime. Bun's rejection matcher typings triggered type-aware warnings, so integration rejection assertions use node:assert/strict with the same rejection expectations. Tooling imports Bun lazily only when bundling a project.

TypeScript 7.0.2 uses Preserve/Bundler resolution, with extensionless source imports and scaffold examples. Core/tooling use `vp pack` in unbundle mode, preserving package entry paths and external dependencies while emitting Node-compatible JavaScript and declarations. TypeScript 7 declaration generation currently carries the packager's experimental API notice. Astro keeps its framework build. Owned files were formatted; historical plans, patch files and vendored agent/plugin assets were excluded. No Vitest compatibility flags or weakened rules were needed.

Verification: all 13 workspace check tasks pass, including 30 Vitest tests and Vite+ format/lint/type checks with zero findings. All 20 PostgreSQL/CLI/Node integration tests pass (132 Bun expectations plus Node rejection assertions). Consumer TypeScript fixtures and native Node imports pass against the new package outputs. This is an implementation checkpoint, not the final framework review or cloud acceptance.

## Candidate generation (U8, partial)

Generation now separates immutable candidate preparation from activation. `prepareProject` writes and validates a candidate without changing the active link. `activateProject` checks its source version, verifies artifact contents, rechecks source identity and switches the link. Ordinary `generateProject` uses the same publication path. An exclusive project-local directory lock serializes generators across processes, with a bounded acquisition wait and cleanup after success or failure. An abandoned lock fails closed; automatic crash recovery is not yet implemented and must be addressed by the development lifecycle rather than deleting another owner's lock.

Evidence: the extended CLI integration fixture first failed on the missing preparation export. It now checks three concurrent generators, candidate preparation retaining the previous link, stale-candidate rejection after a later edit, latest-candidate activation, and compilation failure preserving the active artifacts. The four CLI integration tests pass (40 Bun expectations plus Node rejection assertions). The workspace check passes all 13 tasks with 30 unit tests and zero Vite+ findings. Database live introspection/sync, the watcher, runtime replacement and stop/restart recovery remain U8 work.

## Live schema introspection (U8, partial)

The pinned PostgreSQL patch now exposes Drizzle's existing read-only introspector and snapshot serializer. Loom validates the selected namespace and supported snapshot vocabulary. Introspection runs queries sequentially on the supplied dedicated session; its caller must provide locking and transaction isolation. This supplies the live baseline independently of committed release artifacts.

PostgreSQL round-trip tests exposed false differences in the timestamp default, enum CHECK expressions and constraint naming metadata. The compiler now emits the canonical timestamp expression and enum `ANY` expression with explicit text casts. Before Drizzle generates SQL, Loom aligns only CHECK expressions whose PostgreSQL parser trees match after removing source offsets. Original snapshot hashes and literals remain intact. Safety classification uses that same comparison and disregards only the author-specified-name flag, which introspection cannot reconstruct. Actual constraint names remain part of entity identity, and changed literals still require review.

Evidence: the new PostgreSQL 18 fixture first failed on false enum diffs and then on spurious safety issues. It now verifies unchanged schemas, reserved field names, escaped and Unicode enum literals, defaults, indexes, safe nullable additions, destructive removals, changed enum constraints and independent release baselines. A clean standalone installation found a missing lazy initializer when introspection was the first Drizzle API invoked; that was fixed and covered by independent Node ESM/CommonJS processes inside read-only transactions. Both clean patched installation and frozen installation pass. All 13 workspace check tasks pass (30 unit tests, zero Vite+ findings), and all 22 integration tests pass (148 Bun expectations plus Node assertions). No source import extensions were introduced. Patch-file whitespace diagnostics are from upstream tab-indented unified-diff context.

Development sync/application, protected-target enforcement, watcher coordination, runtime replacement and stop/restart recovery remain U8 work. This checkpoint does not complete U8 or the framework goal.

## Development coordination and watching (U8, partial)

The coordinator invalidates a running revision immediately when a newer edit arrives, debounces the next update, and retains exclusive pipeline ownership until the previous update and its cleanup settle. Each update receives an abort signal and a revision guard for use under the database/publication lock. Failed revisions are recorded without preventing a later edit from recovering. Stopping cancels queued work, aborts active work and waits for cleanup. The watcher observes the project tree, including imported helpers outside the function directory, and excludes Loom state, generated outputs, dependency directories and build outputs. Unknown OS event paths conservatively invalidate the current revision. Closing waits for the native watcher and coordinator to stop.

Candidate activation accepts an abort signal and checks it under the generation lock and immediately before replacing the active symlink. This complements the existing source fingerprint checks. Database transactions and runtime lifecycle integration must still honor the revision guard; the coordinator does not independently make arbitrary callbacks atomic or stop a callback that ignores cancellation.

Evidence: three unit tests failed on the absent coordinator before implementation and now cover rapid successive revisions, serialized work, stale candidate rejection, failed-edit recovery and stop-time cleanup. Two real-filesystem integration tests cover ignored generated events, stop/restart and a fresh consumer whose active generated contracts survive a syntax error and recover after correction. The generation fixture also verifies that an aborted activation preserves the previous link. All 24 integration tests pass (158 Bun expectations plus Node assertions). All 13 workspace check tasks pass with 33 unit tests and zero lint/type findings after an explicit `this: void` annotation corrected a detached-method warning.

The ce-simplify-code reuse, quality and efficiency prompts were run sequentially inline for this changed coordinator/watcher/generation scope, following the repository's no-subagent mapping. No additional behavior-preserving simplifications were warranted (0 reuse, 0 quality, 0 efficiency fixes). Settled architecture decisions were preserved. This is not the final shipping review. Database sync, verified target protection, runtime startup/replacement and abandoned generation-lock recovery remain outstanding before U8 completion.

## Development target and session binding (U8, partial)

Development target inspection uses the pinned Neon configuration runtime's programmatic API. It validates the required provider metadata, requires PostgreSQL 18, rejects protected/default branches and configured production/preview overlaps, and resolves exactly one read-write endpoint for the explicit development branch. The database session helper requests the selected role/database with explicit branch and endpoint IDs and pooling disabled. It rejects mismatched role/database details, pooler hosts and URL identity overrides. Credentials stay inside the connection operation rather than its public receipt.

The helper owns a dedicated migration session and acquires the same namespace advisory lock used by release migrations. It then repeats provider inspection before invoking the database operation, rejecting protection or target changes observed while waiting. Configuration is parsed into a local copy. Abort signals are checked before connecting and after lock acquisition/target revalidation. The existing connection helper bounds database waits and closes the session on every exit. Provider protection changes cannot be made atomic with a PostgreSQL transaction; the contract is a fresh preflight under the database lock, not a cross-service lock.

Evidence: four target-selection tests first failed on the missing API and then passed; a fifth covers malformed/misbound connection details. A PostgreSQL integration test changes protection after the first inspection while holding the migration lock, verifies refusal before the callback, verifies explicit nonpooled credential requests, and proves lock release after failure. All 13 workspace check tasks pass (38 unit tests and zero Vite+ findings). All 25 integration tests pass (163 Bun expectations plus Node assertions). Provider responses in these tests use a typed adapter seam; PostgreSQL sessions are real. No live Neon target acceptance or provisioning is claimed. The [Neon runtime documentation](https://neon.com/docs/reference/config-runtime) and installed 1.6.2 public declarations were inspected; no provider CLI subprocess is used.

Next: versioned development-sync metadata and guarded live-schema application, followed by runtime replacement and abandoned-lock recovery. U8 and the full framework goal remain incomplete.

## Guarded development schema application (U8, partial)

Framework metadata version 2 adds a development ledger without changing the version 1 migration hash. Bootstrap verifies the existing version sequence and hashes before upgrading. Development application prepares the candidate first, obtains the verified target session and namespace lock, validates both development ledgers and catalog evidence, and plans against Drizzle's live introspection. Committed release artifacts retain their independent baseline. Unsafe and nontransactional changes require review and are not applied automatically.

Automatic SQL runs through the native ORM migrator inside an outer transaction. System-field protections and runtime grants use the same helper as release application. The development artifact, provider identity, source version and before/after catalog hashes commit with the SQL and ORM ledger. Source identity and cancellation are checked after planning and before commit. Repeating the same source with no SQL does not append another ledger entry. Runtime activation remains a separate, unfinished lifecycle step.

Evidence: the new integration fixture initially failed on the absent synchronization export. It now verifies initial application, additive changes preserving populated data, idempotent repetition, independent release planning, destructive-change refusal, stale-source rejection, catalog privilege drift, and rollback after source changes while DDL waits on a real PostgreSQL lock. Both the attempted column and ledger entry are absent after rollback. The test refreshes PostgreSQL's statistics snapshot while observing blocked DDL and awaits cancellation before cleanup. Resources left by an earlier failed fixture were removed by their exact owned names. Bootstrap tests verify the version 1 upgrade and runtime denial of development metadata access.

All 13 workspace check tasks pass, including 38 unit tests and zero Vite+ findings. All 26 integration tests pass (175 Bun expectations plus Node assertions). These use PostgreSQL 18 and a typed provider seam; live Neon acceptance remains outstanding. Runtime startup/replacement, the dev CLI and abandoned generation-lock recovery remain before U8 completion; this is an incremental implementation checkpoint, not final framework review.

## Function transaction execution (U9, partial)

The runtime transaction primitive uses read-only repeatable-read transactions for queries and serializable transactions for mutations. It retries the entire callback only for PostgreSQL serialization failures and deadlocks, including errors wrapped by Drizzle. Attempts are bounded (three by default, configurable from one through ten), with capped exponential delay and abort checks before entry, after session acquisition and before commit. Abort during retry delay stops further attempts. Callbacks must not perform external effects; this API does not sandbox application code or cancel an already-running SQL statement.

Evidence: the PostgreSQL fixture first failed on the missing transaction export. It now verifies a stable query snapshot across an independently committed update, query-write denial, rollback without retry for an ordinary callback error, successful recovery from a forced serialization conflict, exact retry exhaustion, cancellation rollback, a forced two-session advisory-lock deadlock and complete pool release. PostgreSQL error assertions inspect Drizzle's underlying cause codes. The focused fixture passes 13 expectations plus Node assertions; all 13 workspace check tasks pass with 38 unit tests and zero Vite+ findings. The isolation/retry contract was checked against the [PostgreSQL 18 isolation documentation](https://www.postgresql.org/docs/18/transaction-iso.html).

This primitive does not yet dispatch registered functions, validate returned values, persist idempotency results, or enforce identity. Those remain U9/U10 work and must precede full runtime/development acceptance.

The full integration suite also passes: 27 tests across 15 files, with 188 Bun expectations plus Node assertions. No final framework or shipping acceptance is claimed.

## Validated function execution (U9, partial)

Registered database functions now execute through the transaction primitive. Standard Schema argument validation completes before acquiring the transaction, and result validation plus the existing wire encoder execute inside its callback before commit. Validation failures use fixed error messages; validator exceptions retain their causes for trusted server diagnostics. HTTP error serialization and request IDs still belong to the unfinished dispatcher. The argument input is captured independently of its caller, and each retry gets a fresh validated value, so handler mutation of an earlier argument object cannot change the next attempt.

Evidence: two unit tests first failed on the absent preparation export. They now cover Zod argument transforms with Valibot result transforms, invalid argument rejection before handlers, invalid results, unsupported symbols, and bigint/date encoding. The existing real PostgreSQL transaction fixture was extended and failed on the missing database-function executor before implementation. It verifies that both result-validation and encoding failures roll back domain writes, and that a forced serialization retry sees the original argument value and leaves caller input unchanged. A pinned Valibot test dependency was added to the integration workspace.

All 13 workspace check tasks pass, with 40 unit tests and no Vite+ findings. All 27 integration tests pass (193 Bun expectations plus Node assertions). The ce-simplify-code reuse, quality and efficiency prompts were applied sequentially inline to this execution/encoding change, preserving the plan's settled decisions: zero applied findings in each category and zero skipped findings. Validation reuses the existing encoder; the initial validated argument is consumed once, and only subsequent invocations repeat validation.

Public/internal dispatch, active-context lifetime enforcement, internal helper transaction reuse, request identity and persistent idempotency remain outstanding. These trusted server execution helpers do not yet constitute the public runtime or complete U9.

## Database invocation lifetime (U9, partial)

Function execution now accepts Loom's `DatabaseConnection` rather than an arbitrary Drizzle database. The connection owns transaction construction with the same compiled relations and pool. Each transaction uses a native Drizzle database with a private invocation token and execution guard. AsyncLocalStorage binds application queries to that invocation; after its callback settles, only Drizzle's transaction-finalization statements are allowed, and after finalization every use of that transaction is rejected. This checks actual query execution, including saved lazy builders and prepared queries. The root connection remains a trusted server escape hatch and is not supplied to function handlers.

Evidence: a characterization test reproduced a deferred update succeeding after its owning transaction had committed. A second test reproduced one live invocation using another invocation's transaction. Both now reject before SQL execution. Existing snapshot, retry, rollback, cancellation and pool-release checks remain green. The relation fixture now runs its self/multiple-target/junction RQB v2 query through the guarded execution path. Drizzle's [custom logger API](https://orm.drizzle.team/docs/goodies#logging) supplies the hook; execution ordering and propagation were checked in the pinned driver's installed source and integration tests. The hook writes no SQL or parameter logs. This is lifecycle enforcement for trusted application code, not a sandbox against code opening its own connection or issuing transaction-control SQL.

All 13 workspace checks pass with 40 unit tests and zero Vite+ findings. The full 27-test integration suite passes (194 Bun expectations plus Node assertions). A subsequent Node 24 fixture verifies real PostgreSQL queries and rejection of a saved builder after completion in a separate process; all three Node compatibility tests pass, bringing the integration inventory to 28. The new Node fixture was followed by another successful workspace check. An initial test typecheck error from passing a Drizzle identifier directly to `from()` was corrected to an SQL expression without weakening types.

Internal helper transaction reuse, public/internal dispatch, identity and persistent idempotency remain U9/U10 work. U8 runtime replacement still depends on these capabilities.

## Internal mutation transaction reuse (U9, partial)

Trusted handlers can call `runInternalMutation` with their active context and an internal mutation registration. The helper uses the exact parent context and transaction, validates arguments/results through the same execution path, and does not acquire another connection or commit independently. A private scope records invocation ownership, active state, pending helper calls and failures. Queries cannot call mutation helpers; helper calls after completion are rejected even if their handler would not execute SQL. Any helper failure prevents parent commit, including a result-validation failure caught by application code.

If a handler returns while helpers are still running, execution rejects the unfinished invocation and drains those helpers before releasing the transaction, then rolls back. This avoids returning a pooled session while helper work still owns it. Application handlers must await their work; the framework cannot interrupt arbitrary user promises or sandbox unrelated external effects. These calls currently accept direct server registrations and return validated wire values; generated-reference routing belongs to the remaining dispatcher work.

Evidence: the extended PostgreSQL fixture first failed on the absent helper export. It now checks parent visibility of helper writes, parent-failure rollback, caught invalid-helper-output rollback, query-to-mutation refusal, calls through a saved completed context, and cleanup/rollback of an explicitly unawaited helper. Existing transaction and connection-lifetime checks remain green. A lint warning for an unnecessary iterable-to-array conversion was fixed without relaxing rules. All 13 workspace checks pass with 40 unit tests and zero Vite+ findings; all 28 integration tests pass (197 Bun expectations plus Node assertions).

Public/internal dispatch, action contexts, identity, persistent idempotency and their acceptance tests remain. Neither U9 nor the full framework goal is complete.

## Registered function dispatch (U9, partial)

The dispatcher captures an immutable registry version and provides separate public and internal server capabilities. Public calls cannot resolve internal registrations; both capabilities check function kind and contract version. A required authorization hook receives verified identity separately from arguments. Database authorization runs inside the invocation transaction and repeats on transaction retries. Action authorization runs before its handler without a database context. Actions receive a request ID and cancellation signal and never receive automatic transaction retries. Registration context types follow the function kind, including registrations declared inline in a dispatcher registry.

Responses attach request IDs and expose fixed error codes/messages. The `loom.function.failure` diagnostics channel emits only the request ID, known function name, version and code; it excludes arguments, identity, validator details and exception causes. This remains a trusted server dispatcher: JWT verification, an untrusted HTTP envelope decoder, generated-reference invocation helpers and persistent mutation idempotency are still outstanding. The action context will need its internal call helpers before the complete action API is available.

Evidence: a real PostgreSQL fixture verifies public/internal visibility, version and kind checks, argument rejection, authorization denial, read-only query and read-write mutation modes, action context shape, request IDs, redacted diagnostics, pool release and action failure without retry even when its error carries SQLSTATE 40001. A cancellation characterization reproduced handler entry after authorization cancelled the request; the executor now checks cancellation again before entering the handler. Type fixtures prove that actions lack database access and registered argument types remain precise. Typechecking also caught contextual inference incorrectly combining action and database contexts for inline registrations; registration factories now use the context fixed by their kind.

All 13 workspace check tasks pass with 40 unit tests and zero Vite+ findings. All 29 integration tests pass (218 Bun expectations plus Node assertions). The ce-simplify-code reuse, quality and efficiency reviews ran sequentially inline, as required by the workspace's agent mapping, against this dispatch checkpoint: zero simplification findings applied and zero skipped. The cancellation and context-inference fixes were correctness fixes verified separately. U9, U8 runtime activation and the full framework goal remain incomplete.

## Persistent mutation replay (U9, partial)

Mutation dispatch now requires deployment/metadata configuration and a bounded idempotency key. SHA-256 scope hashes include the deployment, verified issuer/subject/tenant (or the anonymous partition), function name and contract version. Argument fingerprints preserve array order and sort object keys recursively; non-JSON numbers and sparse arrays are rejected. Keys and identity strings are not stored in plaintext in the replay ledger. Arguments are validated and authorization runs before looking up a saved result, including on every serialization retry. Reusing a key with different arguments returns a conflict. Recreating a dispatcher still finds the database-backed saved result and assigns the retry its own request ID.

Framework metadata version 3 adds `mutation_results` while preserving both prior migration hashes. Runtime roles receive metadata schema usage and only SELECT/INSERT on this table; migration/development history and DDL remain inaccessible. The validated, encoded result is inserted in the same serializable transaction as domain writes. Concurrent insert conflicts roll back the losing transaction and use the existing bounded retry path with a fresh snapshot and authorization. This follows PostgreSQL's [whole-transaction serialization retry contract](https://www.postgresql.org/docs/18/transaction-iso.html). Handlers can execute more than once and still must not perform external effects.

The replay window is 86,400 seconds from ledger insertion, measured by PostgreSQL's clock. Expired keys return `IDEMPOTENCY_EXPIRED` and never execute again in the same scope. Records are retained to enforce this refusal; automatic compaction of expired result payloads and capacity/retention maintenance remain unfinished work. There is no claim of bounded ledger storage yet. Direct internal mutation helpers still share their parent's transaction and replay record.

Evidence: the PostgreSQL fixture uses an actual restricted runtime login. It verifies replay through a new dispatcher, canonical object-key ordering, changed-argument conflicts, invalid keys, revocation before replay, issuer/subject/tenant/anonymous/deployment/version separation, rollback of domain writes and replay records after invalid output, and expiry refusal. Two races prove one committed effect: concurrent counter updates and independent append-only domain inserts that compete only at the replay ledger. Bootstrap verifies upgrades from versions 1 and 2, unchanged historical hashes, SELECT availability, DELETE denial and continued history/DDL denial. Unit tests cover identifier/key bounds and non-JSON inputs. An initial run used stale compiled exports after a typecheck failure; the subsequent build and test runs below use the current emitted packages.

All 13 workspace check tasks pass with 42 unit tests and zero Vite+ findings. All 30 integration tests pass (253 Bun expectations plus Node assertions). Inline ce-simplify-code review found no reuse or quality changes; one efficiency change hoisted recursive fingerprint validators out of the per-value path. Zero findings were skipped. The final suite includes that change. HTTP/JWT integration, generated-reference action helpers, scheduled-write rollback acceptance and retention maintenance remain outstanding, along with the remaining framework phases. This is an incremental checkpoint, not final review or release acceptance.

## Verified identity and transaction-local authority (U10, partial)

Database and action contexts now expose immutable identity, request ID and cancellation signal. The dispatcher supplies these separately from arguments. Before database authorization, execution binds the captured identity through `set_config('loom.identity', json, true)` on every transaction attempt. The local setting is cleared by commit or rollback, and anonymous calls explicitly bind JSON null. SQL policies can read fields from `nullif(current_setting('loom.identity', true), '')::jsonb`. This uses PostgreSQL's [transaction-local configuration semantics](https://www.postgresql.org/docs/18/functions-admin.html). Raw SQL remains trusted server code: a handler deliberately changing its own SQL context is outside the application-code isolation guarantee.

The new `createJwtVerifier` uses pinned jose 6.2.12 for signature and claim validation, following its [JWT verification API](https://github.com/panva/jose/blob/main/docs/jwt/verify/functions/jwtVerify.md). It accepts explicitly configured local public JWKS or HTTPS remote JWKS, captures configuration independently of later caller mutation, and routes unverified issuer claims only to known issuers. Remote destinations never come from token contents. Remote resolvers bound requests to five seconds and use a thirty-second cooldown and ten-minute cache. Accepted algorithms are RS256, ES256 and EdDSA. Issuer, subject and expiration are required; audience is enforced when configured, and an optional configured tenant claim must then be present and nonempty. The verifier returns only identity and expiration, with fixed authentication errors and no token/claim logging.

Evidence: actual ES256 signatures verify successfully. Unit tests reject expired and not-yet-valid tokens, wrong issuer/audience, missing expiration/tenant, invalid subjects/tenants, another signing key, symmetric-algorithm substitution, malformed/oversized tokens, duplicate issuers and unsafe remote URLs. A PostgreSQL fixture uses a non-owner runtime login with a one-connection pool and real RLS policies. Alice and Bob reuse the same backend PID while seeing only their own rows. Forged document IDs, issuer changes, tenant changes and anonymous identity cannot retrieve protected rows. A prohibited tenant update rolls back; the pooled session has no residual identity, raw reads remain filtered and DDL is denied. Authorization itself sees the transaction-local identity, and action handlers receive the corresponding principal without a database context.

All 13 workspace checks pass with 44 unit tests and zero Vite+ findings. All 31 integration tests pass (278 Bun expectations plus Node assertions), including Node 24 imports after adding jose. Inline ce-simplify-code reviews applied one quality simplification (the issuer map stores verifier functions directly), no reuse or efficiency changes, and skipped one low-value per-call schema allocation change. The final checks include the simplification. HTTP origin enforcement and JWT-to-dispatch wiring, Neon Auth integration, remote JWKS rotation/outage acceptance and durable one-use WebSocket tickets remain unfinished. U10 and the full framework goal remain active.

## Authenticated public HTTP calls (U9–U11, partial)

The Neon adapter now exposes a Hono application with `/api/loom/call`. Its strict, versioned JSON envelope rejects extra fields and routes only through the dispatcher's public capability. Verified bearer identity is supplied separately from arguments; anonymous access requires explicit configuration, and invalid supplied credentials never fall back to anonymous. Expiration is checked after verification and again after reading the body. Origin policy uses exact configured origins, HTTPS except for local development, restricted preflight headers and no cookie credentials. Responses include request IDs, fixed errors, `no-store` and `Vary: Origin`.

The body reader counts actual streamed bytes even when Content-Length understates the payload, rejects malformed UTF-8 and cancels stalled reads at the configured deadline. Cancellation propagates into dispatch. Deadlines remain cooperative: an arbitrary verifier or handler promise that ignores cancellation is not forcibly interrupted. Route handling follows the [Hono application API](https://hono.dev/docs/api/hono); HTTP response handling is confined to this route so future WebSocket upgrades can remain separate. Response-size limits, connection tickets and WebSocket lifecycle enforcement are still pending.

Evidence: unit coverage verifies envelope and protocol rejection, exact origin matching, preflight, malformed authentication, understated Content-Length, cancellation of a stalled stream and token expiration during body upload. A real Bun HTTP server with signed ES256 tokens and a restricted PostgreSQL runtime login proves that forged owner arguments cannot replace verified identity, a discarded mutation response can be retried with one committed write, and authorization revocation prevents replay. Internal registrations remain inaccessible and handler secrets do not enter public errors. A separate Node 24 process imports compiled exports and exercises the Hono request protocol without Bun globals; this is not deployed Neon or live Node TCP acceptance.

All 13 workspace check tasks pass, including 46 unit tests and zero Vite+ findings. All 32 integration tests pass (290 Bun expectations plus Node assertions). Inline ce-simplify-code review applied one reuse change by sharing the existing JSON validator; no quality or efficiency changes and no skipped findings. These checks include that change. Neon Auth configuration, remote JWKS acceptance, durable tickets, the complete client and runtime activation remain unfinished. This is an incremental checkpoint, not final framework review.

## Neon Auth verifier adapter (U10, partial)

`createNeonAuthVerifier` accepts explicit branch auth base/JWKS URLs and optional audience/tenant configuration. The base URL's origin becomes the trusted issuer, matching Neon's [Functions authentication contract](https://neon.com/docs/compute/functions/authentication). The adapter rejects unsafe base URLs before reducing them to an origin and reuses the existing JWT verifier for JWKS validation, algorithms, claim checks and cache bounds. It does not read ambient environment variables; runtime wiring can pass `NEON_AUTH_BASE_URL` and `NEON_AUTH_JWKS_URL` explicitly.

Evidence: the configuration test first failed on the missing adapter. Two tests now cover unsafe URLs, malformed tokens, audience/tenant validation, actual EdDSA signatures, origin-versus-path issuer matching, other-branch rejection and one-fetch key caching. Only the fetch boundary is replaced with a controlled JWKS response; JOSE parsing and cryptography run normally. This is not live provider, TLS, key rotation or outage acceptance. The Node 24 compiled-export/HTTP smoke test also passes; its two database-dependent siblings were skipped in that focused invocation because no database URL was supplied. Their earlier full-suite result remains recorded above.

All 13 workspace checks pass with 48 unit tests and zero Vite+ findings. Anti-slop rejected conditional empty-object spreads and an anonymous widened object declaration during implementation; both were corrected without changing rule severity. Remote JWKS rotation/outage acceptance, durable one-use connection tickets, runtime configuration integration and the remaining framework phases are still pending.

## Durable connection tickets (U10, partial)

The public HTTP app can now mint a connection ticket at `/api/loom/ticket` after bearer verification and an explicit allowed Origin. This endpoint requires authentication even when ordinary function calls permit anonymous users. Its strict body contains only the protocol version; callers cannot supply an identity. It shares the bounded body reader, deadline, fixed errors and no-store/CORS behavior with function calls. The route is unavailable unless a ticket issuer is configured.

The trusted ticket store generates 256 random bits and persists only their SHA-256 hash, the deployment, exact origin, verified identity and expiration. Tickets default to thirty seconds, allow one to sixty seconds, and cannot outlive the authenticated session. Redemption atomically deletes a matching unexpired row and returns the verified session, using PostgreSQL's [DELETE RETURNING semantics](https://www.postgresql.org/docs/18/sql-delete.html). Different origins/deployments cannot consume the ticket, and concurrent redemption has one winner. Losing the redemption response consumes the credential; the client must obtain a new ticket. No ticket value enters framework diagnostics or SQL text.

Framework metadata version 4 adds the ticket table and expiration index while preserving versions 1–3 and their hashes. Runtime privileges permit SELECT/INSERT/DELETE for redemption but deny UPDATE and metadata DDL. As with other runtime SQL, these credentials are trusted server authority, not a sandbox against application code deliberately forging metadata. Expired unredeemed rows are refused but not automatically pruned yet; retention/capacity maintenance remains pending.

Evidence: the new PostgreSQL fixture first failed on the missing ticket-store export, and the HTTP fixture first returned 404 for issuance. Both now pass through real restricted database credentials. Tests cover persisted hash secrecy, fresh store instances, concurrent single-use redemption, origin/deployment separation, replay, session-capped expiry, expired/forged credentials, configuration bounds and runtime UPDATE denial. A held row lock reproduced redemption accepting a ticket that expired while waiting. The SQL now rechecks the database clock after deletion; the test observes the blocked query before releasing the lock after expiration and verifies rejection. Real signed-token HTTP requests mint and redeem tickets while denying forged/missing credentials, disallowed/missing origins and forged identity envelopes. Unit coverage additionally checks anonymous-call configuration and preflight.

All 13 workspace checks pass with 49 unit tests and zero Vite+ findings. All 33 integration tests pass (317 Bun expectations plus Node assertions). Inline ce-simplify-code reuse, quality and efficiency reviews found no behavior-preserving changes to apply or skip; the expiry correction was a separately verified correctness fix. Initial Bun assertion type warnings were removed by using the existing Node assertion pattern without suppressions. WebSocket upgrade wiring, connection expiry/reauthorization, remote JWKS rotation/outage acceptance, runtime activation and later framework phases remain incomplete.

## Typed HTTP client transport (U11, partial)

The browser client now exposes `createClient().call(reference, args, options)` for public generated references. Argument inference comes from the reference, and output types map bigint and Date to their encoded string representations. It uses the existing wire encoder and strict response-envelope validation. Server validators remain responsible for the function contract; arbitrary Standard Schema validators must accept their declared wire inputs rather than assuming automatic reconstruction of server objects from JSON. Stable client errors expose codes and server request IDs without retaining transport exception details.

Each mutation captures its serialized arguments and one idempotency key before the first asynchronous operation. Network/body-stream failures and gateway 502/503/504 responses can retry queries and mutations, with bounded attempts and exponential delay. Actions never receive these transport retries. One HTTP 401 can request fresh credentials, including for actions because the authentication boundary rejects before dispatch. An auth adapter supplies the token and stable issuer/user/tenant partition; a changed partition stops a pending retry before another request. Callers can provide their original mutation key when explicitly recovering an uncertain result across separate calls. This does not yet implement identity-partitioned caching or cache invalidation.

Requests omit cookies, reject redirects, use no-store and require HTTPS except for local development. Responses count actual streamed bytes independently of Content-Length. Abort signals and a per-call deadline cancel fetch, stalled body reads and retry waits. User-provided auth/fetch callbacks must cooperate with the supplied signal; arbitrary promises are not forcibly interrupted. Invalid envelopes, oversized responses and application errors do not receive automatic retries. Local wire validation rejects non-finite arguments before contacting the server.

Evidence: the initial three client tests failed on the missing client factory. Five tests now verify captured arguments/key reuse after a lost response, one auth refresh, identity-change rejection, no action transport retries, exact protocol/invalid-response errors, caller cancellation, understated Content-Length, stalled-read timeout, cancellation during backoff and server error/request-ID preservation. Type fixtures reject wrong argument types and internal references, and distinguish encoded strings from native bigint/Date outputs. The real HTTP/PostgreSQL fixture now discards a committed response through the client's fetch boundary and verifies that its automatic retry produces exactly one new domain row. A browser-target build of the compiled client succeeds without Node import specifiers or database URL identifiers; Node 24 imports remain valid. Packed browser execution and React acceptance remain outstanding.

All 13 workspace checks pass with 54 unit tests and zero Vite+ findings. All 33 integration tests pass (324 Bun expectations plus Node assertions). The focused client tests pass again after strengthening exact error-code assertions. Inline ce-simplify-code review applied no changes; one proposed reuse of the server's body reader was skipped because its error/transport boundaries differ and a generic abstraction was not justified yet. Browser typing caught Bun's extra `fetch.preconnect` property leaking into callback requirements; the injected transport now uses the actual callable contract. Cache partitions, React lifecycle state, ticket acquisition/WebSocket reconnects, development runtime activation and the remaining framework phases are still pending.

## Remote JWKS rotation and outage acceptance (U10, partial)

The configured remote verifier and Neon Auth adapter now have a real HTTPS compatibility fixture running in isolated Node 24 processes. A temporary self-signed certificate is rejected without its explicit test CA, then accepted by a second child using `NODE_EXTRA_CA_CERTS` with TLS verification enabled. The endpoint receives neither user bearer tokens nor cookies. The test uses actual EdDSA signatures, HTTPS requests and JOSE verification; only Date is controlled through Node's [Date-only mock clock](https://nodejs.org/docs/latest-v24.x/api/test.html#dates). Network timers remain real, including the five-second timeout.

The fixture verifies a shared initial JWKS request for concurrent callers, rejection of an unknown key during the thirty-second cooldown, successful rotation after that cooldown, removal of the old key after refresh, forged-signature rejection and refusal of an unconfigured issuer. Fresh cached keys remain usable during a provider outage. Once the ten-minute cache expires, verification fails closed; recovery refetches successfully. Malformed JSON, redirects and a response that sends headers plus partial JSON and then stalls all return fixed authentication errors. Redirect targets are never contacted. These semantics imply bounded cache staleness, not immediate signing-key revocation or continuous provider availability.

This was characterization of the existing pinned implementation: the first HTTPS run passed, so no production behavior was changed. Typechecking exposed a Node/Bun event-helper typing mismatch in the fixture; the server now awaits its own listening callback directly. All 13 workspace checks pass with 54 unit tests and zero Vite+ findings. The focused HTTPS test passes with the real stalled-body timeout. All 34 integration tests pass (324 Bun expectations plus Node assertions), including the new isolated HTTPS fixture alongside the PostgreSQL suites. Inline reuse, quality and efficiency review found no simplification changes to apply or skip. Remote-JWKS component acceptance is now covered; live Neon Auth acceptance still requires an explicit cloud target, and WebSocket lifecycle, client caching/React integration, runtime activation and later phases remain incomplete.

## Identity-partitioned query cache (U11, partial)

`createQueryCache` now caches public query results by deployment, identity partition, function name/version and canonical wire arguments. Concurrent equivalent reads share a request, callers receive independent result copies, failures are removed, and bounded LRU eviction cancels pending work. The canonical serializer moved unchanged from server idempotency into the shared validation directory, preserving persisted mutation fingerprints while keeping the client browser-safe.

Auth adapters must synchronously call `setIdentity` on user/tenant changes or sign-out. It clears entries and aborts pending requests; generation and entry checks reject late results even if an injected transport ignores cancellation. The client also checks the expected identity before sending the request. This cache has no TTL, reactive observers or automatic mutation/live-query invalidation yet: consumers must explicitly clear it when results become stale. React and WebSocket integration remain pending.

Evidence: the initial cache tests failed on the missing factory. A new cancellation regression then reproduced an abort listener reusing the cancelled entry. Clearing the map before notifying listeners and installing replacement entries before eviction notifications fixes reentrant reads. Requests are registered before authentication callbacks execute, and contract/argument snapshots preserve the original request across deferred execution. Five unit tests cover canonical deduplication, result isolation, versions, identity changes, late responses, failures, eviction, clearing, reentrant cancellation and request capture. Type fixtures reject internal references, mutations and wrong arguments. The real signed-JWT HTTP/PostgreSQL test verifies shared reads, sign-out rejection before transport and recovery from authorization denial.

All 13 workspace checks pass with 59 unit tests and zero Vite+ findings. All 34 integration tests pass (328 Bun expectations plus Node assertions), including mutation replay after the canonical helper relocation. Inline ce-simplify-code reuse, quality and efficiency passes found no further changes to apply or skip. This checkpoint does not complete U11 or the framework plan; live invalidation, React lifecycle, runtime activation and later phases remain outstanding.

## Durable table revisions (U12, partial)

Framework metadata version 5 adds a revision row per application namespace/table and a migration-owned trigger function. Release migrations and development synchronization install an AFTER statement trigger for INSERT, UPDATE, DELETE and TRUNCATE on every compiled application table, in the same transaction as application DDL and its history receipt. Installation advances existing revisions rather than resetting them; old rows remain when tables disappear. Statement triggers conservatively invalidate even when a statement changes no rows, consistent with PostgreSQL's [trigger semantics](https://www.postgresql.org/docs/18/sql-createtrigger.html).

The trigger increments its revision in the writer's transaction. Concurrent writers contend on the same revision row, so a later committed revision cannot overtake an earlier uncommitted write to that table. Rollbacks undo both changes. Repeatable-read evaluation can observe data and revisions from the same snapshot. Multi-table writers can still deadlock and require the existing bounded transaction retry policy; hot-table contention remains an accepted limit to measure in U17. Installation obtains the table's DDL lock before updating its revision row, avoiding an inverted lock order with ordinary writers.

Runtime credentials receive SELECT on revision metadata but cannot insert, update or delete it or invoke the trigger function directly. The function uses SECURITY DEFINER with a fixed pg_catalog search path and qualified metadata access, following PostgreSQL's [security-definer guidance](https://www.postgresql.org/docs/18/sql-createfunction.html). Missing revision metadata aborts the domain write rather than silently losing invalidation. Raw SQL executed by administrative roles can still bypass protections deliberately; those roles remain trusted authority.

Evidence: the new PostgreSQL fixture first failed on the missing installer export. It now verifies bulk insertion, update, deletion, truncation, rollback, concurrent revision-row blocking, snapshot consistency, runtime permission boundaries, reinstall advancement and refusal of writes with missing revision rows. Bootstrap tests reconstruct version 4 and upgrade to version 5 without changing previous hashes, alongside older upgrade paths. Existing real release/development fixtures now assert that their installed triggers advance revisions after application writes. All 13 workspace checks pass with 59 unit tests and zero Vite+ findings; all 35 integration tests pass (347 Bun expectations plus Node assertions before the two additional path assertions, which also pass in focused runs). Inline reuse, quality and efficiency review found no further simplifications to apply or skip.

This is the durable invalidation foundation, not completed subscriptions. Snapshot evaluation APIs, the isolate poller, schema-generation reconciliation, WebSocket delivery/limits, reconnect behavior and React hooks remain pending, along with the other unfinished framework phases.

## Authorized live-query snapshot evaluation (U12, partial)

`createRevisionReader` captures a validated, bounded table set for one application namespace and returns immutable decimal-string revisions, preserving PostgreSQL bigint precision. Missing tracked rows fail closed. Generation composition must supply all application and database-backed authorization tables; arbitrary untracked/external SQL and time-dependent handlers still cannot claim live-query eligibility. Automatic generation reconciliation is not implemented yet.

`evaluateDatabaseQuery` shares the existing transaction/validation/lifetime implementation with ordinary calls and captures revisions before its read-only repeatable-read transaction finishes. Authorization is required by this primitive's API. The dispatcher's new `evaluate` capability adds public-query-only visibility, contract version checks, verified identity propagation, fixed errors and request IDs. It is disabled unless a revision reader is configured. Ordinary public/internal call response contracts remain unchanged. This capability is for the forthcoming subscription service and is not exposed as a new HTTP route.

Evidence: the new restricted-role PostgreSQL fixture first failed on the missing revision-reader export. Its first implementation exposed Drizzle's array interpolation producing a record rather than a PostgreSQL array; the query now uses the parameterized IN-list representation. The fixture pauses evaluation after authorization, commits a concurrent writer, then proves the original result and revisions share the old snapshot while the next evaluation sees both new values. Authorization-table changes advance their revision and deny the next evaluation. It also checks stale contracts, internal/action rejection, invalid arguments, disabled evaluation, missing metadata, configuration capture/validation, bigint precision and pool cleanup.

All 13 workspace checks pass with 59 unit tests and zero Vite+ findings. The full integration suite passes all 36 tests (361 Bun expectations plus Node assertions); the strengthened evaluation fixture additionally passes its three configuration/precision assertions. Inline reuse, quality and efficiency review found no additional changes to apply or skip; ordinary execution and snapshot evaluation reuse one transaction implementation. Isolate polling, subscription ordering/backpressure, WebSocket authority/expiry, client reconnects, React bindings and remaining framework phases are still pending.

## Shared subscription polling and lifecycle (U12, partial)

`createSubscriptionPoller` now owns one polling cycle for a generation's conservative application/authorization table set. Concurrent poll requests join the same cycle, and unchanged revision vectors skip query evaluation. It runs at most the configured number of evaluations concurrently (default four, maximum twenty), retains at most 1,000 subscriptions, and has no growing invalidation queue. Each subscription captures its call and verified session, receives increasing sequence numbers, and has at most one evaluation in flight. Changes during an evaluation remain visible to the following cycle. A cycle waits for its evaluations to settle before starting the next interval; slow queries therefore affect refresh latency, and evaluator cancellation remains cooperative.

Session-expiration timers operate independently of the polling cycle. Unsubscribe, expiry and shutdown remove entries before aborting work; late responses cannot publish. Query/authorization errors publish a final fixed dispatcher error and close the subscription. Revision-read failures request resynchronization. The transport sink must synchronously enqueue messages and report a full buffer by returning false; the poller then closes that subscription with RESYNC_REQUIRED. Actual WebSocket byte/message limits and transport close behavior remain to be implemented. Stopping the generation closes its subscriptions and drains active work.

Evidence: three initial unit tests failed on the missing poller factory. Four now cover shared reads, unchanged-revision suppression, sequence ordering, concurrency/subscription limits, cancellation, late-result rejection, expiry during evaluation, read failure, buffer refusal, automatic timer polling, coalescing during a slow evaluation and shutdown. The real restricted-role PostgreSQL/dispatcher fixture adds the poller to the chain: a direct SQL insert refreshes an initially empty result, rollback causes no publication, and changing the tracked authorization table produces FORBIDDEN and closes the subscription.

All 13 workspace checks pass with 63 unit tests and zero Vite+ findings. The full integration suite passes all 36 tests (369 Bun expectations plus Node assertions); the focused evaluation/poller integration passes again after cleanup changes. Inline simplification consolidated the two snapshot-based close passes into one helper (reuse: one; quality/efficiency: zero; skipped: zero), preserving callback reentrancy semantics rather than iterating a mutable live set. No final framework review or delivery is claimed. Two independent API-process acceptance, generation reconciliation, WebSocket wiring/limits, client reconnects, React bindings and the remaining plan phases are still pending.

## WebSocket protocol and native Neon route (U10–U12, partial)

`createWebSocketSession` connects a verified session to the shared subscription poller. Protocol 1 accepts strict text-only subscribe/unsubscribe/pong envelopes, rejects caller-supplied identity, and sends ordered results without exposing internal revision vectors. Duplicate IDs close the connection, unsubscribe releases its work, and protocol mismatches close with PROTOCOL_MISMATCH. Defaults bound incoming frames to 64 KiB, buffered output to 1 MiB, subscriptions to 32 per connection and incoming messages to 100 per second. Ping/pong heartbeats detect quiet disconnections; authentication expiry closes even a connection with no subscriptions. Disconnect and shutdown clear timers and release all subscriptions.

`createNeonRealtime` mounts `/api/loom/socket` through the pinned `@neon/functions/hono` native adapter. It requires an exact allowed Origin, an Upgrade request and exactly two offered subprotocols: `loom.v1` and `loom.ticket.<one-use-ticket>`. Only `loom.v1` is selected; the ticket is never placed in a URL or framework error. Ticket redemption precedes upgrade, and pending redemptions count toward the connection limit (default 100). Handshake and unopened-upgrade reservations expire after five seconds. A timeout may consume a ticket if its database redemption later completes; reconnect must mint a fresh ticket. The native upgrade response is returned unchanged, as required by [Neon's Hono WebSocket integration](https://neon.com/docs/compute/functions/websockets). Mount this route outside response-rewriting middleware. Provider/infrastructure header logging still needs deployment configuration; framework code does not log the credential.

Evidence: two initial protocol tests failed on the missing session factory. Five unit tests now verify strict messages, result ordering, unsubscribe, bounded buffering, protocol errors, binary rejection, expired empty sessions, heartbeat/pong deadlines, frame/rate limits, origin/subprotocol rejection, pending-handshake capacity and reservation release. The actual Neon helper is exercised off-platform and fails closed with 503 rather than pretending to upgrade. A separate Bun-hosted integration uses real WebSocket frames to verify result delivery and duplicate-subscription closure through the production session controller. That fixture supplies a trusted test identity and does not claim Neon handshake or cloud acceptance.

All 13 workspace checks pass with 68 unit tests and zero Vite+ findings. The full integration suite passes all 37 tests (371 Bun expectations plus Node assertions); the focused socket test passes again after the protocol-mismatch refinement. Anti-slop checks caught overly broad unknown parameters; the socket boundary now names the actual wire event types, and outbound values use the existing JSON domain type without weakening validation. Inline reuse, quality and efficiency review found no additional changes to apply or skip. Native successful upgrade acceptance, two independent API processes, runtime composition, client reconnects/React, generation replacement and remaining framework phases are still pending.

## Browser connection-ticket acquisition (U11–U12, partial)

The browser-safe HTTP client now exposes `ticket({ identityKey, signal })`. Each call obtains a new ticket through `/api/loom/ticket`, using the existing bounded transport attempts, response-byte limit, deadline, cancellation and one-time token refresh. The expected identity partition is mandatory and checked before sending credentials. Ticket payloads must contain the exact credential shape and a finite future Unix expiration; fractional PostgreSQL timestamps are supported. No ticket cache is introduced. Browsers supply Origin themselves; native consumers must supply an allowed Origin through their fetch adapter.

The shared request helper preserves mutation argument/key capture, action retry restrictions and server error handling. Two new tests first failed because the ticket method was absent; they now verify fresh issuance, forced token refresh, identity mismatch and malformed/expired credential rejection. The real HTTP integration exercises the client through JWT verification and restricted-role PostgreSQL ticket issuance, then confirms each credential redeems once. All 13 workspace checks pass with 70 unit tests and zero lint/type/format findings. Reconnect scheduling, socket epoch ownership, React observers and remaining framework phases are still pending; this is not final delivery.

The full integration suite also passes all 37 tests (374 Bun expectations plus Node assertions). Inline review traced both request callers through authentication, retry and decoding: the transport loop is shared once, and function-specific validation remains at the call boundary. No additional simplification changes or skipped findings were identified.

## Browser live-connection lifecycle (U11–U12, partial)

`createRealtimeConnection` obtains a fresh HTTP ticket for every socket attempt and offers it only in the designated subprotocol, retaining the configured backend URL prefix. Exponential retry delays use equal jitter and cap at 30 seconds; a connection must remain ready for 30 seconds before its failure counter resets. A 30-second deadline bounds ticket acquisition/handshake, and a 60-second silence watchdog complements automatic ping/pong handling. Outbound buffered data is capped at 64 KiB and incoming frames at 1 MiB. Invalid envelopes, protocol mismatches and terminal authentication/policy failures stop automatic reconnection.

Every asynchronous callback belongs to a connection generation. Sign-out, identity replacement, disconnect and stop invalidate that generation before aborting requests or closing the socket. Late ticket responses and retained old event callbacks therefore cannot publish into a replacement connection. Cleanup also handles auth callbacks that synchronously change identity during abort. Observers cannot prevent cleanup by throwing. The transport retains no query values: its consumer must clear identity-scoped results and resubscribe on each ready event; the shared live cache and React layer are still pending.

Evidence: two initial tests failed on the absent connection factory. Four unit tests now cover fresh-ticket reconnects, heartbeat replies, late socket/ticket events, sign-out, handshake timeout, buffer overflow, terminal protocol failure and reentrant identity changes during ready/abort notifications. The real WebSocket integration now also closes the production server session with RESTART, resubscribes on the replacement connection and receives a fresh result with a second ticket acquisition. That fixture uses a trusted test identity; HTTP/JWT/durable ticket authority is separately exercised in the PostgreSQL HTTP fixture. Native Neon successful-upgrade and full browser acceptance remain pending.

All 13 workspace checks pass with 74 unit tests and no Vite+ findings; all 37 integration tests pass (376 Bun expectations plus Node assertions). Inline reuse/quality/efficiency review kept one cleanup path and added generation checks after potentially reentrant abort and socket-factory callbacks. No remaining findings were skipped. Per-subscription sequence ownership, shared reactive cache, React bindings and the rest of the framework plan remain in scope and incomplete.

## Shared live-query stores (U11–U12, partial)

`createLiveQueryClient` exposes typed, side-effect-free `query(reference, args)` stores with stable `getSnapshot` and observer registration. Only observed stores occupy capacity or open a socket. Identical deployment/identity/function-version/canonical-argument keys share one server subscription; the last observer releases it, and removing the final active query closes the connection. Arguments are captured when the store is created. Defaults permit 32 distinct active queries, and oversized subscribe frames are rejected before opening a socket rather than retried forever.

Snapshots distinguish loading, success, error, signed-out and stopped. Successful values are deeply frozen. New connections automatically resubscribe with new IDs and reset sequence ownership; duplicates/older sequences are ignored, and a sequence gap closes the affected subscription with a local error. Server errors retain their code and request ID after the subscription closes. Identity changes rekey and clear all snapshots before abort or observer callbacks, including callbacks that reenter identity changes or remove other observers. Old connection events cannot restore cleared values.

Evidence: two initial unit tests failed on the missing factory. Five tests now cover shared subscription/ref-count ownership, duplicate callback registrations, lazy connection creation, captured arguments, capacity and frame-size rejection, server errors, sequence ordering/gaps, sign-out clearing and reentrant observers. Type fixtures verify argument/output inference and reject mutation/internal references. The real WebSocket restart fixture now uses the shared store, with automatic resubscription and fresh result publication after server restart. It still uses a trusted test session; browser and native Neon acceptance remain separate pending gates. React bindings and the remaining framework phases are not complete.

All 13 workspace checks pass with 79 unit tests and zero Vite+ findings. All 37 integration tests pass (376 Bun expectations plus Node assertions); the focused real-socket test passes again after the frame-size guard. Inline review consolidated state resets and observer notification, removed repeated recursive validation while freezing already validated JSON, and checked callback reentrancy and cleanup. No findings were deferred from this slice; no final framework review or delivery is claimed.

## React bindings and packed browser consumer (U11, U12 partial acceptance)

The optional React entrypoint now exports `LoomProvider`, `useQuery`, `useMutation`, `useAction` and `useLoomClient`. React 19 is an optional peer; server/client-only consumers do not need it. The provider receives application-owned HTTP and live clients. Auth adapters must update the live identity synchronously rather than waiting for a React effect. Query hooks use [React's external-store contract](https://react.dev/reference/react/useSyncExternalStore), preserve stable subscriptions for canonical-equivalent inline arguments, and return the store's loading/success/error/sign-out states. SSR returns a fixed loading snapshot and opens no connection. Mutation/action callbacks retain generated arguments, wire result types, cancellation and the HTTP client's retry semantics.

Evidence: SSR tests first failed on missing React exports. Both now pass, including the missing-provider diagnostic and zero auth requests during rendering. Type fixtures check bigint/date wire outputs and reject incorrect arguments, internal references and incorrect function kinds. Store methods explicitly declare no `this` dependency so external-store subscription binding is both type-safe and lint-clean.

`bun run --cwd packages/e2e test:browser` runs Chromium through the pinned Playwright library. Its consumer is copied into a temporary directory, imports a freshly packed/extracted core tarball through the package exports, and has only React, React DOM and Valibot dependencies available for browser bundling. The browser exercises StrictMode, duplicate query consumers with inline arguments, parent rerenders without resubscription, mutation-driven live updates, server restart/reconnect, action results, sign-out, a different signed-in identity, and query unmount/remount. It reports no page errors. This fixture deliberately supplies trusted test identities and in-memory query state; JWT/PostgreSQL authority has separate integration tests, and native Neon/browser end-to-end deployment acceptance remains pending. Browser installation uses `bunx playwright install chromium`.

All 13 workspace checks pass with 81 unit tests and no Vite+ findings. React Doctor scanned the workspace and scored 92/100 with one existing server warning: it suggested moving a guard ahead of the awaited handler. That guard checks for unawaited internal mutations after the handler runs, so moving it earlier would weaken transaction correctness; no change was applied. Vite+ emits a module-directive advisory, but the emitted React index/hooks/provider files all retain `"use client"`. The complete framework review and remaining implementation phases are still pending.

The full database integration suite also passes all 37 tests (376 Bun expectations plus Node assertions). Inline review kept the hooks thin over the already-tested HTTP/live clients and confirmed that query creation stays side-effect-free during render; no additional simplification changes were needed. The packed browser fixture is a local acceptance checkpoint, not a claim that the deployment, jobs, storage, recovery, documentation or release phases are finished.

## Composed Neon application lifecycle (U10, U12 partial)

`createNeonApplication` composes public HTTP and optional native Neon realtime routes behind one `fetch` entrypoint. Realtime requests and upgrade responses remain unchanged. Both routes share the configured Origin policy; callers still supply matching dispatcher, ticket capabilities and the generation's poller. `stop()` rejects new requests, closes realtime sessions, aborts HTTP requests and drains outstanding request/dispatcher work and polling before callers close their database. Repeated stop calls share the same promise, registered before shutdown callbacks can reenter it.

Cancellation is cooperative: the HTTP adapter awaits its verifier and handler rather than independently returning on its deadline. A handler that ignores cancellation can delay shutdown. Database construction, deployment/branch activation validation, tracked-table derivation and bounded operational shutdown remain separate pending work; this wrapper does not establish those guarantees.

Evidence: the initial lifecycle test failed on the absent application export. Two unit tests now cover abort/drain ordering, refusal of new work, idempotent stop, native socket routing and poller shutdown. The existing real JWT/restricted-role PostgreSQL HTTP fixture now uses this application and stops it before database cleanup. All 13 workspace checks pass with 83 unit tests and zero Vite+ findings. After builds completed, all 37 integration tests pass (376 Bun expectations plus Node assertions). An earlier concurrent build/integration invocation encountered transient missing package output; the serial rerun resolves that test-environment race. Inline review traced request registration, callback reentrancy and provider response ownership; no additional simplification was needed. Full framework review and delivery remain pending.

## Durable queue storage and fenced leases (U13, partial)

Framework metadata migration 6 adds deployment-scoped jobs, unique deduplication keys, function/build payloads, verified identity snapshots, due times, retry limits, lease ownership/fencing, cancellation, results and fixed failure codes. Existing migration hashes remain unchanged; upgrade coverage now includes version 5 to 6. Runtime credentials can select/insert/update jobs but cannot delete retained work or perform DDL.

`createJobQueue` is a trusted server primitive. `enqueue` validates a registered mutation/action and its arguments against the captured registry version, then inserts through the supplied database transaction. Mutation handlers must await it with `context.db` so domain changes, jobs and output validation share their transaction. This primitive is not a browser authorization boundary or the final generated scheduler API. Deployment identity scopes deduplication; the same key with different function, identity, arguments, due time or retry policy fails. The original UUID remains after success/failure, and claimed mutations carry that UUID as their idempotency key. Defaults permit one attempt; explicit policies allow up to ten attempts and a fixed retry delay of zero to 3,600 seconds. Arguments are limited to 64 KiB and saved results to 1 MiB.

Claims use PostgreSQL row locks with `SKIP LOCKED`, following its [queue-consumer locking guidance](https://www.postgresql.org/docs/18/sql-select.html). Each claim increments a fencing token and attempt count; leases last one to 300 seconds. Abandoned final attempts become retained failures, with at most 100 terminal leases reaped per claim. Complete/fail/renew reject expired or superseded leases. A contention regression initially showed that a clock predicate could run before a row-lock wait; a second materialized CTE now checks eligibility only after receiving the locked row. The test holds the lock until expiration and verifies that acknowledgement fails.

Cancellation prevents pending execution immediately. For a running job it records a request and refuses renewal; failure or lease expiry then records cancellation. A successful completion can still win because cancellation cannot undo already completed effects. Inspection retains terminal state/result/error, and failed jobs are not silently recreated under the same deduplication key.

Evidence: the first integration run failed on the missing queue export. The restricted-role PostgreSQL fixture now verifies domain-plus-job rollback after invalid mutation output, input/build rejection, concurrent deduplication and claims, deployment isolation, skipped locked rows, lease recovery/fencing, post-lock expiry, retry exhaustion, abandoned final attempts, future due times and both cancellation outcomes. No mock queue tests were added: these guarantees depend on database transactions and locking. All 13 workspace checks pass with 83 unit tests and zero Vite+ findings; all 38 integration tests pass (419 Bun expectations plus Node assertions). Inline reuse/quality/efficiency review used the ce-simplify-code rubrics; no behavior-preserving changes or skipped findings remained.

Worker execution, real process-termination recovery, mutation-commit-before-ack replay proof, generated scheduling helpers, cron delivery adapters, explicit replay, branch activation/quarantine and subsequent phases remain pending. Queue factory construction alone does not authorize execution of copied jobs, and this slice does not claim U13 completion or final framework review.

## Worker dispatch and process-crash recovery (U13, partial)

`createJobWorker` connects durable claims to the internal dispatcher. A run executes one job at a time, defaults to ten jobs and accepts a maximum of 100; overlapping calls on one instance join the same pass. Each claim receives an immediate lease check before dispatch, then renewal at one-third of the lease duration. An independent deadline aborts work even if a renewal request stalls. Renewal and dispatcher failures expose fixed codes. Shutdown prevents new work, aborts the active handler and drains owned execution/renewal operations; handlers and database cancellation remain cooperative, so the count bound is not a wall-clock execution guarantee.

Worker construction requires `assertActive(signal)`. It runs before claims and during lease checks; failure prevents further execution. The adapter must implement actual branch/deployment verification. Local tests deliberately use a trusted fixture callback, not copied metadata, and do not establish cloud activation or quarantine acceptance.

Scheduled handlers and authorization callbacks receive immutable `context.job` metadata: the logical job UUID and attempt number. Only trusted internal dispatch supplies it. The UUID remains stable across lease owners and explicit action retries, providing an external idempotency key. Mutation dispatch uses the same UUID for its durable replay key, still reauthorizing before saved-result replay. Actions retain their separate execution semantics: PostgreSQL-shaped errors do not trigger transaction retries; only the job's explicit retry policy can run an action again.

Evidence: four initial worker tests failed on the absent factory; five now cover bounded/coalesced passes, activation refusal, lease loss, stalled renewal, cancellation, timer cleanup, redacted infrastructure failures and shutdown during a pending claim. The job-metadata assertion also failed before internal dispatch supplied it. A real Node 24 child uses the compiled server package, a restricted database role and the production queue/dispatcher/worker. The parent terminates it with SIGKILL after claim and, separately, after mutation commit before acknowledgement. After real lease expiration, replacement execution completes both jobs and the committed mutation does not write twice. The fixture also verifies transaction identity, immutable job context, access revocation, incompatible-version rejection, default one-attempt actions, stable IDs across explicit retries, heartbeat exclusion of a competing claim and cooperative cancellation of a running action.

All 13 workspace checks pass with 88 unit tests and no lint/type findings. All 39 integration tests passed (446 Bun expectations plus Node assertions); after strengthening the running-action cases, the focused worker integration passes with 34 expectations, and e2e typecheck plus Vite+ checks pass again. Inline ce-simplify-code review retained a single renewal/cleanup path and found no additional reuse, quality or efficiency changes to apply or skip. Generated scheduling helpers, cron adapter wiring, explicit replay, concrete activation/quarantine checks, deployment and later phases remain incomplete; no final framework review or delivery is claimed.

## Explicit failed-job replay (U13, partial)

Queue inspection now returns the current fencing token. The trusted operator capability `replay(id, expectedToken, dueAt)` requeues only a failed job at that exact token. It increments fencing, resets the bounded attempt counter and preserves the logical UUID, original build/function, arguments, identity, deduplication fingerprint and retry policy. Concurrent or stale requests cannot replay a later failure accidentally. Succeeded, pending, running and cancelled jobs are not replayable through this operation.

Framework migration 7 records the previous attempt count, fencing token, error and result together with replay time and requested due time. Audit insertion and state transition share one SQL statement; an audit-write failure rolls both back. Runtime privileges allow audit reads/inserts but prohibit updates/deletes. The version-6 upgrade fixture confirms earlier migration hashes stay unchanged.

Evidence: the strengthened queue fixture first failed on the missing replay method. It now verifies concurrent replay exclusion, stale-token refusal after another failed run, preserved job/idempotency IDs, rejection of old acknowledgements, deployment isolation, success-state refusal, retained failure evidence and atomic rollback when audit insertion is denied. The Node process fixture adds a mutation that commits before SIGKILL with no automatic attempts remaining: lease cleanup retains a failure, explicit replay reuses its UUID, and the worker returns the saved mutation result without another domain write. Replay does not extend the existing mutation-result retention window or authorize an incompatible build.

All 13 workspace checks pass with 88 unit tests and zero lint/type findings. All 39 integration tests pass with 477 Bun expectations plus Node assertions. Inline reuse/quality/efficiency review kept replay distinct from expiring lease acknowledgement, reused existing identifier validation, and captured the audit table alongside the queue table; no additional simplification findings remained. Operator CLI exposure, generated scheduling helpers, cron/provider wiring, activation/quarantine, deployment and subsequent phases are still pending. This is not completion of U13 or final framework delivery.

## Typed transactional scheduling (U13, partial)

Database function contexts now expose `scheduler.runAt(timestamp, internalReference, args, policy?)` and `scheduler.runAfter(delayMs, internalReference, args, policy?)`. Generated references supply argument types; queries and public references cannot be scheduled. Runtime validation checks the actual registered definition as well as the reference, preventing a forged visibility field from scheduling a public function. Absolute timestamps accept valid Dates or integer milliseconds; delays must be nonnegative safe integers and produce a valid Date. Retry policy remains bounded by the queue, and omitted deduplication keys receive a UUID.

The dispatcher passes its configured queue into mutation execution. Scheduling uses the invocation's database transaction and captured identity. Query invocations reject scheduling; actions have no transactional scheduler. Shared mutation-helper tracking rejects escaped calls and unfinished work and dooms the parent after a caught scheduling failure. An unfinished operation may reach its database insert while cleanup drains it, but the transaction rolls back and its returned promise rejects as inactive. Mutation replay returns the original scheduled job ID without enqueueing another job.

Evidence: the existing rollback fixture first failed because `context.scheduler` was absent. It now exercises the helper through the real restricted-role PostgreSQL connection and dispatcher, including output-validation rollback, explicit-key deduplication, future due time, captured identity, mutation replay, query refusal, missing configuration, forged public-function references, caught-error rollback, saved-context refusal and detached asynchronous validation. Positive/negative TypeScript fixtures check reference argument inference, allowed kinds/visibility and the action-context boundary. The preparation-only unit fixture supplies an intentionally unavailable scheduler.

All 13 workspace checks pass with 88 unit tests and zero Vite+ lint/type findings. After builds completed, all 39 PostgreSQL integration tests pass (479 Bun expectations plus Node assertions). Inline ce-simplify-code reuse, quality and efficiency review found no additional changes to apply or findings to skip: pending-work ownership is shared with internal mutations, while queue persistence remains in its existing implementation. Cron occurrence delivery, provider wake wiring, operator CLI, activation/quarantine and later units remain incomplete; this does not complete U13 or final framework review.

## Cron declarations and durable occurrences (U13, partial)

`cron(expression, internalReference, args, policy?)` captures typed function arguments and a bounded retry policy for a five-field numeric UTC schedule. Declaration validation rejects malformed fields, out-of-range values, reversed ranges, zero steps, macros and named days/months. The provider evaluates the schedule; Loom does not calculate missed runs locally.

`createCronDispatcher` captures the configured declarations and persists each received occurrence through the existing queue. Its trusted entrypoint accepts a configured cron name and a valid Date, requires a branch/deployment activation callback, and uses a deployment-scoped key derived from the name and normalized occurrence time. Concurrent deliveries and delivery after completion return the original job UUID. Changing the arguments or build for an already recorded occurrence fails with a deduplication conflict instead of executing different work under that key. Cron work has no end-user identity; the worker's authorization callback must permit the intended internal handler. No missing occurrence is synthesized or backfilled.

The current [Neon schedule guide](https://neon.com/docs/compute/functions/triggers/schedule) and [trigger envelope contract](https://neon.com/docs/compute/functions/triggers/overview) were read directly for this slice. They describe UTC minute-level schedules and occurrence payloads. The future HTTP adapter must verify the provider-delivered header at the Neon edge and bind its trigger identity to the configured cron. The core dispatcher is a trusted persistence capability, not an HTTP authentication boundary.

Evidence: the unit test first failed on the missing `cron` export and the integration fixture failed on the missing dispatcher export. The restricted-role PostgreSQL test now sends one occurrence concurrently through independent dispatchers, executes it with the production worker and mutation dispatcher, redelivers it after success, and verifies one retained queue row and one application write. It also covers separate named crons at the same time, captured declarations, unknown names, invalid dates, activation refusal, cancellation before dispatch, changed arguments and incompatible-build redelivery. Type fixtures reject query/public references and incorrectly typed arguments.

All 13 workspace checks pass with 89 unit tests and zero Vite+ findings. The focused PostgreSQL cron integration passes; after strengthening changed-argument/build cases, e2e typecheck and static checks also pass. Inline reuse/quality/efficiency review retained the existing queue validation and deduplication implementation; no additional simplification changes or skipped findings remained. Provider HTTP routing, worker wake deployment, real activation/quarantine and later units remain pending.

## Neon schedule delivery and worker wake endpoint (U13, partial)

`createNeonTriggers` accepts scheduled POST deliveries at `/api/loom/triggers`. It reuses `@neon/functions/triggers` to parse the versioned envelope and check header/body invocation-ID consistency, then matches both trigger ID and name against captured configuration. A cron binding persists its occurrence before waking the worker; a wake binding only runs the bounded worker pass. Unknown bindings, missing headers, malformed timestamps/JSON/UTF-8, wrong methods/media types and bodies over 64 KiB are rejected without dispatch. Infrastructure errors return a fixed diagnostic without their underlying message.

This adapter relies on the [documented Neon edge contract](https://neon.com/docs/compute/functions/triggers/overview#confirming-a-request-came-from-neon): client-supplied `X-Neon-*` headers are stripped before invocation. The header value is not a secret and this route must not be exposed behind an arbitrary proxy that accepts forged headers. The runtime assembly still needs actual branch/deployment activation verification and deployed acceptance; local Request tests cannot prove the edge's stripping behavior.

`createNeonApplication` optionally mounts the endpoint, owns its request lifetime and stops/drains its worker with the rest of the generation. Requests await worker execution directly; no `waitUntil` or in-memory timer provides durability. The 30-second ingress deadline cancels body reading and cron persistence cooperatively. It is not a hard worker execution deadline: shared worker execution drains under worker shutdown, retaining the existing handler-cancellation limitations. Public HTTP and triggers now share the same bounded, fatal-UTF-8 request reader without changing public protocol errors.

Evidence: three endpoint tests first failed on the missing factory; the new application test then failed because trigger requests did not reach the worker. Four endpoint tests now cover configured cron/wake delivery, header/binding refusal, invalid/oversized input, cancellation and redacted persistence failure. The application test verifies worker stop and request-drain ordering. The restricted-role PostgreSQL cron fixture now delivers concurrent HTTP requests through the composed application, production cron dispatcher, queue, worker and mutation dispatcher, then redelivers after completion and verifies one write for the received occurrence.

The full integration run exposed a pre-existing ticket-test clock mismatch: PostgreSQL's microsecond clock was 1.5 ms ahead of the host clock used by the assertion. The test now brackets issuance with database timestamps and verifies the full 30-second lifetime against that authoritative clock; no production expiry behavior changed. Inline reuse/quality/efficiency review retained the SDK parser and extracted the existing shared body reader; no further changes or skipped findings remained. Provider provisioning, live trigger acceptance, concrete activation/quarantine and subsequent units remain pending.

Final verification for this slice: all 13 workspace checks pass with 94 unit tests and zero Vite+ findings. All 40 integration tests pass (479 Bun expectations plus Node assertions), including the corrected ticket lifetime check.

## Durable provider invocation receipts (U13, KTD15 partial)

The requirement audit found that occurrence keys prevented duplicate jobs but did not retain provider invocation IDs. Framework migration 8 now adds deployment-scoped trigger receipts containing invocation ID, trigger ID/name, occurrence time, delivery kind, fingerprint and an optional job link. Runtime credentials can read/insert receipts but cannot update/delete them. Earlier framework migration hashes remain unchanged, including the explicit version-7 upgrade fixture.

The Neon adapter forwards the parsed provider identity into cron dispatch. A cron job and its receipt commit in one database transaction before the worker runs. Reusing an invocation ID for another trigger, occurrence, delivery kind or job is rejected; any newly inserted job rolls back. Wake delivery records a receipt after activation verification and before executing the worker. Repeated identical wake receipts may wake the worker again because the durable queue owns claim/acknowledgement idempotency; a receipt is not a claim that execution finished.

Receipt insertion uses `ON CONFLICT DO NOTHING`, followed by a separate read to observe a concurrent winner. Both paths compare the fingerprint and job link. Direct trusted cron calls may omit provider metadata; provider HTTP calls always supply it. No process-local receipt map or background task owns this guarantee.

Evidence: the strengthened integration test initially failed because the receipt table was absent. It now verifies concurrent wake delivery, conflicting invocation payloads, atomic rollback when receipt insertion is denied, retained HTTP invocation/job association, and forbidden runtime receipt mutation/deletion. Endpoint tests assert forwarding of the provider identity and wake recording. All 13 workspace checks pass with 94 unit tests and zero Vite+ findings; all 40 PostgreSQL integration tests pass (480 Bun expectations plus Node assertions). Inline reuse/quality/efficiency review retained one receipt write/compare implementation for cron and wake paths; no further changes or skipped findings remained.

Runtime generation assembly and provider deployment remain pending. The assembly review also identified checks to resolve before claiming consumer acceptance: generated internal-reference imports participate in project bundling and must not cause version-regeneration cycles, and the configuration's job limits/defaults must agree with the implemented queue/worker limits when wired together. These are open integration checks, not claims of verified runtime behavior. Concrete branch activation/quarantine, cloud acceptance and later units remain incomplete.

## Stable internal imports and immutable registries (U6/U13 integration)

Project discovery now resolves extensionless `_generated/api` and `_generated/internal` imports without requiring an earlier generation. The project bundle imports a separate version module, so its hash does not include a prior generated version. The final contract hash includes that bundle, executable configuration and the existing package/lock inputs. Each cached project receives its own version module before evaluation. Module-scope reference capture defers definition access until function modules finish initializing; captured missing or wrong-visibility references fail generation. After initialization, reference collections contain the discovered functions and are frozen.

Generated registries now import an immutable bundled project within their generation directory. They no longer import live authoring files that can change after generation or resolve internal references through an older active symlink. Candidate handlers therefore use candidate references before activation, while older loaded handlers and generated registries retain their own version. Public browser references remain data-only. The [Bun plugin API](https://bun.sh/docs/bundler/plugins) supplies virtual import resolution; no existing generated directory becomes an input to the supported stable reference imports.

Cache publication stages both project and version files before atomically renaming the directory, verifies existing contents and removes staging files. During verification, direct loading of fresh consumers exposed an ordering regression: Bun's virtual entry parent must exist before bundling for external package resolution. That ordering is restored. The fixture now loads a fresh project concurrently before generating it, in addition to the existing concurrent generation coverage.

Evidence: the consumer regression first failed because `_generated/internal` did not exist. A second assertion then reproduced a prepared registry using the active generation's version. Both now pass, along with repeated generate/prepare stability, candidate activation, missing/wrong-visibility refusal without activation changes, reference collection enumeration/freezing, TypeScript 7 extensionless consumer imports, and Node 24 execution of both old and candidate registries after source changes. All 13 workspace checks pass with 94 unit tests and zero Vite+ findings. All 41 integration tests pass with 498 Bun expectations plus Node assertions.

Inline ce-simplify-code review applied one reuse improvement (one normalized function-module list) and one efficiency improvement (indexed module lookup during registry emission); quality review found no further changes, and no findings were skipped. Runtime assembly, configuration/job-limit reconciliation, concrete activation/quarantine, provider deployment and later phases remain pending. This slice does not complete the framework or its final review and delivery gates.

## Configuration and durable job policy alignment (U6/U13 integration)

`jobs.maxAttempts` is a ceiling for newly scheduled work, defaults to the supported maximum of 10, and accepts 1–10. It does not grant retries: each job still defaults to one attempt, including external actions. A schedule or cron must explicitly request additional attempts within the configured ceiling. Existing persisted jobs retain their recorded policy across claims and explicit replay.

The core's exported, frozen `jobLimits` supplies the same bounds to queue/worker validation and tooling configuration. `jobs.leaseMs` accepts 1,000–300,000 milliseconds and defaults to 60,000. `jobs.retryBaseMs` accepts 0–3,600,000 milliseconds and defaults to 1,000; the implemented policy uses this as a fixed retry delay. Both settings require exact whole-second values so runtime assembly can convert units without rounding or silently changing policy.

Queue construction accepts the attempt ceiling and default delay. Enqueue captures and validates effective policy before preparing arguments or inserting a row. Per-job delay overrides, including zero, take precedence. Cron declarations preserve an omitted delay through dispatch so the queue default applies; omitted attempts remain one. Retention cleanup is still pending, and `retentionDays` is not evidence of implemented cleanup.

Evidence: strengthened configuration/cron tests first failed on the old default and prematurely resolved delay. The PostgreSQL queue fixture first failed because a configured seven-second delay persisted as one second. It now verifies configuration-to-queue conversion, stored effective policy, a default action exhausting after one failure, explicit zero-delay retries, configured-cap refusal, cron delay inheritance and invalid queue settings. All 13 workspace checks pass with 95 unit tests and zero Vite+ findings; all 41 integration tests pass with 506 Bun expectations plus Node assertions. Inline reuse/quality/efficiency review retained the existing policy validators and shared bounds; no additional changes or skipped findings remained.

The configuration contract is ready for runtime assembly, which must pass these values into the actual queue and worker. Optional authoring-module loading, activation/quarantine, deployment, retention and later plan units remain incomplete.

## Cron authoring modules in generated runtimes (U6/U13 integration)

Optional `backend/crons.ts` now participates in project loading and the immutable generation bundle. Its default export is a record of named `cron(...)` declarations; an absent file supplies an empty record. Resolution uses the existing project-path boundary. The loader validates declaration names and shapes, then requires each target to match an internal function's kind and the candidate contract version. It also rejects declarations above the configured attempt ceiling.

Generated server registries export their bundled cron declarations alongside functions. A cron-only edit changes the build version, while candidate declarations resolve internal references to their own version before activation. First-time generation needs no prior `_generated` directory. Browser references remain separate from these server declarations.

The core's `isCronDeclarations` guard reuses declaration validators but requires an already materialized attempt count. A type guard cannot claim it inserted a default into the original value. Loader results clone declarations before exposing them; cron dispatch still captures and validates its own configuration. This does not invoke handlers, provision schedules or activate workers.

Evidence: the strengthened consumer fixture first failed because its cron module was absent from the loaded project. It now checks first generation, stable regeneration, candidate references, cron-only stale detection, invalid declaration names, stale/incorrect-kind references, configured retry-ceiling refusal and preservation of the active generation after failure. Fresh Node 24 processes verify cron and handler versions in both old and candidate artifacts after authoring files change. Existing no-cron consumers continue to generate successfully. Guard tests cover valid declarations, invalid names, absent attempt counts and invalid module values.

All 13 workspace checks pass with 95 unit tests and zero Vite+ findings. All 41 integration tests pass with 512 Bun expectations plus Node assertions. Inline reuse/quality/efficiency review found no further changes or skipped findings; the stricter guard schema remains necessary for sound narrowing. Relations/auth/storage module loading, runtime construction, provider activation/deployment and later phases remain pending.

## Native relation modules in generated runtimes (U5/U6 integration)

Project loading now includes optional `backend/relations.ts`. Its default export is native Drizzle Relations v2 built from the project's compiled schema tables. Without that file, the bundle calls native `defineRelations(schema.tables)` to make every compiled table available without named relationships. Initialized consumers include the pinned Drizzle dependency needed for both paths.

The loader validates the native relation shape without replacing table or relation instances. It then uses the same table-identity, namespace and relation-target checks as database connection setup, extracted into a shared validator. Invalid declarations fail before generation activation or any database connection. Generated server registries export their bundled schema and relations; relation-only edits change the immutable generation version. Owned TypeScript imports, including the CLI export and the relation fixture's schema import, remain extensionless under TypeScript 7 bundler resolution.

Evidence: the new consumer regression first failed because generated registries lacked relations. It now covers the absent-file default, a native self-relation, exact compiled-table identity, stale detection after a relation-only edit, candidate preparation without activation, malformed exports and separately compiled tables rejected without replacing the active generation. A fresh Node 24 process loads the prepared artifact after the authoring file has become invalid and verifies its original table identities. Existing migration and development consumer fixtures use the initialized dependency set.

All 13 workspace checks pass with 95 unit tests and zero Vite+ findings. All 42 PostgreSQL integration tests pass with 523 Bun expectations plus Node assertions. Inline ce-simplify-code reuse, quality and efficiency review retained the shared validator and optional-module loader; no further changes or skipped findings remained. Auth/storage module loading, runtime construction, concrete activation/quarantine, deployment and later plan phases remain incomplete.

## Authentication configuration and authoring modules (U6/U10 integration)

`loom.config.ts` now supplies explicit trusted key locations through `auth.issuers: [{ issuer, jwksUrl, tenantClaim? }]`, with a shared optional `auth.audience` and `auth.origins`. This replaces the unconsumed issuer-string array, which could not identify a JWKS endpoint. No repository consumer used that earlier shape. Configuration rejects duplicate issuers, more than sixteen issuers, unsafe issuer/key URLs, empty audience/tenant names and noncanonical or nonlocal insecure origins. The loader and runtime use the same validator and existing verifier/origin rules.

Optional `backend/auth.ts` exports `defineAuth({ authorize, allowAnonymous? })`. The callback receives the dispatcher's authorization context, including its transaction-scoped database for query/mutation policies. `allowAnonymous` defaults to false and must be explicitly enabled; it does not bypass the authorization callback. `defineAuth()` denies every function. Missing auth modules use that denial policy, and malformed or unbranded exports are rejected before generation activation.

`createAuthentication(config, definition?)` combines the declaration with the existing JWT verifier and captures the origin allowlist. With no issuers it rejects bearer tokens; with no declaration it denies function access. Remote JWKS construction remains offline and fetches keys only when verifying a token. Declarations are branded/frozen, capture their callbacks and flags, and are included in immutable generated server registries. Auth-only edits change the generation version, while prepared generations retain their policy after source changes.

Evidence: the configuration regression first rejected explicit JWKS descriptors, the declaration tests first failed on missing APIs, and the consumer fixture first showed an auth-only edit leaving the version unchanged. Passing coverage now includes default denial, explicit anonymous policy, mutation-resistant captured options, rejection of forged declarations, audience/tenant checks through signed tokens and configured JWKS, zero network calls at construction, malformed-module refusal without active-generation changes, and fresh Node 24 execution of an older prepared authorization policy after source edits. Existing HTTP/JWKS/tenant and worker tests continue to pass.

All 13 workspace checks pass with 98 unit tests and zero Vite+ findings. All 43 PostgreSQL integration tests pass with 530 Bun expectations plus Node assertions. Inline reuse/quality/efficiency review found no further changes or skipped findings: shared validation and the existing optional-module loader cover this slice. Runtime assembly must still connect this authentication result to the HTTP service and dispatcher. Storage modules, concrete activation/quarantine, deployment and later plan phases remain incomplete.

## Generation runtime construction and ownership (U6/U9/U10/U12/U13 integration)

`createRuntime` now connects one generation's compiled schema, native relations and function registry to its authorization policy, durable queue, dispatcher, worker, cron dispatcher, connection tickets and shared subscription poller. It owns the database pool and does not perform migrations. It accepts explicit runtime credentials, deployment identity, metadata namespace and an activation verifier. Activation is required before opening the connection and before dispatch, revision reads, tickets, claims/renewals and cron persistence. This callback remains a required trusted integration point; a concrete external branch proof and quarantine implementation are still pending.

Authentication, realtime and job settings now share one core validator with `defineConfig`. Runtime construction passes the configured attempt ceiling, fixed retry delay and lease duration into queue/worker behavior with exact millisecond-to-second conversion. It tracks all compiled application table SQL names for live queries, including authorization data declared in that schema. Empty application schemas have an empty revision set. External untracked SQL remains outside the subscription contract. Subscription evaluation rejects an oversized serialized response, and the returned transport settings carry the heartbeat, subscription and buffer limits. The heartbeat maximum is now 30 seconds, matching the WebSocket transport rather than accepting an unusable 120-second setting. Job retention cleanup remains unimplemented.

Owned async operations capture their inputs before awaiting activation, combine caller cancellation with shutdown and register pending work before invoking callbacks. The registry map, cron declarations and configuration are captured at construction. Shutdown is idempotent, refuses new operations, aborts active dispatch, stops workers/polling, drains owned operations and closes the database last. Constructor failures after connection creation close the pool. The queue and raw connection are not returned as untracked capabilities.

Evidence: the runtime integration initially failed on the missing constructor, and the strengthened configuration test reproduced the unsupported heartbeat allowance. The first assembled runtime then exposed identity mutation across the activation await; a caller changing the identity immediately after invocation affected authorization. Capturing inputs before yielding fixed that regression. The PostgreSQL fixture now checks anonymous denial, transactional scheduling and replay, persisted retry policy, the actual configured lease duration, worker execution, query/revision evaluation, subscription capacity and result bounds, durable tickets, activation refusal and cancellation during shutdown. PostgreSQL session counts verify pool cleanup after invalid-cron startup and after shutdown.

All 13 workspace checks pass with 98 unit tests and zero Vite+ findings. All 44 integration tests pass with 546 Bun expectations plus Node assertions; the runtime fixture passed again after simplification. Inline reuse/quality/efficiency review applied one quality improvement: wrapper methods inherit their existing capability contracts through contextual typing instead of repeating tuple signatures. No other findings or skipped findings remained. Generated service/worker entries, HTTP composition, concrete activation/quarantine, storage, deployment and later phases remain incomplete.

## Generated service and worker factories (U6/U14 integration)

Immutable generations now contain service and worker factories plus typed stable exports. Each factory captures that generation's schema, relations, authorization, cron declarations, function registry, version, metadata namespace and runtime configuration. Connection credentials, deployment identity and activation verification remain explicit startup inputs; workers also require provider trigger bindings. Runtime configuration contributes to the generation hash, and the artifact contract epoch advances to prevent reuse of an older directory layout. Each call receives fresh configuration values.

The Neon service exposes public HTTP and native WebSockets without provider trigger routes. The worker exposes only the configured trigger path. Both factories own application shutdown followed by runtime shutdown and database release, refuse requests after stopping, and release the runtime if application construction fails. These are factory artifacts, not autonomous provider deployment bootstraps; concrete branch activation and quarantine remain pending.

Evidence: the new consumer fixture initially failed on the missing runtime artifact. It now verifies captured settings, fresh configuration per call, TypeScript 7 connection-option requirements and fresh Node imports after authoring files become invalid. The PostgreSQL runtime fixture exercises separate service and worker pools through real HTTP servers: public/internal/trigger route separation, scheduling through the service, execution through the worker, startup failure cleanup, idempotent shutdown and a stalled body returning cancellation. Connection cleanup checks use a bounded two-second observation window because the installed pg-pool initiates client termination before resolving end(); PostgreSQL removes its backend asynchronously.

All 13 workspace checks pass with 98 unit tests and zero Vite+ findings. All 45 integration tests pass with 568 Bun expectations plus Node assertions. Inline ce-simplify-code reuse, quality and efficiency reviews found no further changes or skipped findings. Owned TypeScript imports remain extensionless. Concrete activation/quarantine, internal invocation helpers, storage, provider deployment, recovery and later acceptance/documentation/release phases remain incomplete.

## Deployment target inspection (U14 prerequisite)

`inspectDeploymentTarget` now resolves an explicit preview or production environment against the pinned Neon programmatic API. It reads project, branch and endpoint state without mutations and returns a frozen identity record containing the project, branch ID/name, writable endpoint, PostgreSQL version and protection state. It refuses missing or shared environment selections, mismatched projects, unsupported PostgreSQL, ambiguous branch/endpoint resolution and protection drift. Preview deployment also refuses default or protected branches. Production can select a protected/default branch only with matching configured protection. Provider errors and malformed response diagnostics exclude downstream response values.

The four new unit tests first failed on the missing export. All 13 workspace checks now pass with 102 unit tests and zero Vite+ findings. Tests use the public tooling export and the pinned provider interface; they cover successful preview and production selection, each refusal and provider failure. No live provider resources were mutated. Cloud verification, provider reconciliation, activation/quarantine and deployment receipts remain pending. Neon documents `NEON_BRANCH` as an injected branch name, not an immutable branch ID; runtime activation must bind externally observed identity rather than trusting copied activation metadata or invented provider variables. See https://neon.com/docs/compute/functions/environment-variables and https://neon.com/docs/reference/config-runtime.

Current-state audit correction: the earlier remaining-work note listed internal invocation helpers too broadly. `runInternalMutation` already shares the parent transaction, with real PostgreSQL tests covering rollback, caught child failure, expired/cross-invocation context and unawaited work. U9 does not require a new family of generated-reference invocation methods. No redundant helper API was added.

## Runtime activation binding and grant verification (U14 foundation)

Framework metadata version 9 adds deployment activation grants keyed by deployment and build version. Grants bind the provider project/branch, direct endpoint host and database name to a SHA-256 token hash and default to `quarantined`. The runtime role receives SELECT only; migrations retain grant-writing authority. Upgrade tests preserve all prior migration hashes and now include the version 8-to-9 path.

`createNeonActivationVerifier` compares a trusted deployment binding with Neon's injected branch name and database destination, then checks the active database grant and the hash of `LOOM_ACTIVATION_TOKEN`. It accepts the direct and pooled hostname forms for the same endpoint, requires the actual runtime connection to address the same database/port, and refuses connection-string identity overrides. It also checks that the running build, deployment identity and metadata namespace match the verifier binding. Denials redact provider, database and token details.

Runtime activation now has two startup stages: external verification before opening a connection, followed by verification with the owned database before capabilities are constructed. Subsequent activation boundaries receive the same database and generation context. Failed post-connection activation closes the pool. Existing custom callbacks remain compatible, but production entry wiring must use the concrete verifier. A grant row by itself does not authorize a runtime.

The PostgreSQL fixture first failed on the missing verifier export. It now covers missing grants, valid startup under a non-owner runtime login, a simulated cloned branch retaining the original grant/token, changed database destinations, wrong tokens, port and query-string overrides, mismatched runtime builds and quarantined grants. It confirms the runtime role can read but cannot update grant state. These are local integration proofs; disposable Neon clone acceptance remains pending.

The binding must come from provider-verified deployment data. Deployment tooling must preserve Neon's injected `NEON_BRANCH` and `DATABASE_URL` values, issue a fresh branch-specific token, quarantine copied jobs and triggers, and only activate after environment-specific secrets and compatible code are installed. Those deployment operations and receipts are not implemented by this verifier. See https://neon.com/docs/compute/functions/environment-variables for the injected identity contract.

All 13 workspace checks pass with 102 unit tests and zero Vite+ findings. All 46 integration tests pass with 570 Bun expectations plus Node assertions. Inline reuse/quality/efficiency review added runtime-to-binding consistency checks and eliminated repeated URL parsing; no skipped findings remain in this slice. The broader deployment, storage, recovery, capacity, documentation, examples and release work remains incomplete.

## Verified deployment connections and preview database quarantine (U14)

`withDeploymentConnection` now resolves the selected target and its migration credentials through the pinned provider API, requires the requested role/database and direct endpoint identity, refuses query-string identity overrides, and owns a dedicated PostgreSQL session. It takes a deployment advisory lock scoped to the metadata namespace and rereads provider state after acquiring the lock. Project, branch ID/name, endpoint and protection changes prevent the operation from running. The connection helper returns the operation's result, not a credential receipt.

`quarantinePreviewDatabase` uses that connection boundary and requires an explicit, non-protected, non-default preview target plus metadata ownership. One transaction revokes active grants and cancels pending/running jobs, clears leases and advances their fencing tokens. Terminal jobs remain unchanged. A repeated call reports zero additional changes. Transaction failures roll back both updates and return a redacted error rather than PostgreSQL's potentially sensitive failed-row details. This is the database quarantine stage only; it does not disable provider triggers or undo external effects already performed by running actions. Deployment orchestration must use it before first activation of a cloned branch, not as an unconditional step in ordinary redeploys.

Evidence: the integration fixture first failed on the missing export. It now verifies revocation, cancellation/fencing, terminal-history preservation, idempotence, runtime-role refusal, rollback after an injected job constraint failure, provider identity changes after the deployment lock, and a mismatched connection destination. The provider adapter in this fixture is controlled test data; the SQL operations and role checks run against PostgreSQL 18. Unit coverage verifies exact provider credential-selection arguments and rejects malformed URLs, wrong roles/databases/hosts, pooled endpoints and identity overrides before database access. Disposable Neon clone and provider-trigger acceptance remain pending.

All 13 workspace checks pass with 103 unit tests and zero Vite+ findings. All 47 integration tests pass with 576 Bun expectations plus Node assertions. Inline reuse/quality/efficiency review retained the shared migration-session lifecycle and separate deployment target policy, and added transaction-error redaction. No skipped findings remain in this slice. Grant issuance, deployed-entry wiring, provider trigger control, deployment receipts and the remaining plan phases are still incomplete.

## Activation grant preparation and final database stage (U14)

`prepareDeploymentActivation` stages a quarantined grant using the provider-verified project, branch ID/name, direct endpoint host, database, deployment identity and build. It stores only the SHA-256 hash of a caller-supplied 32-byte random token; the receipt contains the binding and grant state, never the token or its hash. Preparation is transactional and repeatable with the same token and binding. A conflicting token or target cannot overwrite an existing grant. Callers must securely retain the token for deployment and resumption; automatic secret issuance and provider environment installation remain orchestration work.

`activateDeploymentDatabase` compares the prepared binding against newly resolved provider/database identity under the deployment lock and activates only the matching grant. Both preparation and activation refuse active grants from another branch. They also refuse queued work on a branch without an existing matching active grant, including a production-labelled target. This prevents legacy or copied work without activation metadata from skipping quarantine. A legitimate redeploy on an already activated branch can retain its queue and prepare a new build. Preparation is not itself authorization to execute work, and the activation primitive does not prove code/schema compatibility or trigger state: the deployment orchestrator must satisfy those stages before calling it.

The connection boundary now supplies a sanitized database identity alongside its verified target, avoiding another credential request or parsing path. Inline reuse/quality/efficiency review consolidated the grant-match predicate and parameter order across preparation and activation, and removed an initial preview-only exception from the unactivated-work guard. No skipped findings remain.

Evidence: the extended PostgreSQL fixture first failed on the missing exports. Separate regressions demonstrated that pending work without an active grant, and the same case under a production target, previously escaped the preparation guard. Tests now cover quarantined preparation, secret-free receipts, repeatable preparation/activation, wrong tokens, changed branch names, work appearing between preparation and activation, and queue preservation during a subsequent build activation. Unit tests reject malformed tokens before provider access. All 13 workspace checks pass with 104 unit tests and zero Vite+ findings. All 47 integration tests pass with 585 Bun expectations plus Node assertions. No provider resources were changed; provider policy planning/apply, entry bundling, trigger control, cloud receipts and later plan phases remain incomplete.

## Deployable Neon entry files (U14 prerequisite)

`prepareNeonEntrypoints` now writes immutable service and worker bootstraps for a verified deployment binding and generated project version. They resolve runtime credentials only when a request starts the application, retain provider identity environment variables, and contain no secret values. Preparation refuses a changed artifact under the same content hash. Trigger names use locale-independent ordering for repeatable hashes.

`createNeonEntrypoint` shares concurrent startup, returns a fixed 503 while startup is quarantined or fails, and retries after a one-second cooldown. Shutdown waits for an in-progress startup and closes its application without serving it. Successful responses retain their identity for native WebSocket upgrades.

Evidence: three unit tests cover startup sharing, redacted failure/retry and shutdown races. The consumer fixture imports both generated entries in fresh Node processes without runtime credentials, then bundles each through the pinned Neon SDK, unpacks its ZIP outside the workspace, and imports it in Node 24 without workspace dependencies. Both return 503 until activated. The fixture also verifies immutable-file tamper refusal. All 13 workspace checks pass with 107 unit tests and zero Vite+ findings; all 47 integration tests pass with 595 Bun expectations plus Node assertions. The ce-simplify-code reuse, quality and efficiency reviews ran sequentially inline: zero applied or skipped findings. Owned TypeScript imports remain extensionless with Preserve/Bundler resolution. Provider reconciliation, secrets installation, trigger activation, cloud acceptance and the later plan phases remain incomplete.

## Read-only Neon function plan (U14 prerequisite)

`planNeonFunctions` runs the pinned Neon SDK's actual policy planner for explicit service and worker slugs. It captures inputs before provider awaits, resolves the configured preview or production branch, checks the prepared activation binding, and rechecks target identity after planning. Duplicate slugs, mismatched bindings, target drift, conflicts, unexpected resource changes and incomplete function plans fail closed. The result contains only the target, build/artifact hashes and proposed create/update actions; it omits SDK detail objects, invocation URLs, source paths and downstream errors. Planning requires no application credentials and does not bundle source, change branch settings or activate work.

Evidence: the fixture first failed on the missing export. Real SDK execution then exposed that the policy schema rejects an explicit `runtime` key, despite generic input inference accepting it; the pinned SDK supplies Node 24 as its resolved default. Five integration tests now cover update/create planning, binding refusals, provider error redaction, target drift, protected production settings and input mutation during awaits. They use the real SDK planner with a typed provider fixture whose unexpected operations fail locally. All five pass with 21 Bun expectations plus Node assertions. All 13 workspace checks pass, including 107 unit tests and zero Vite+ findings. Inline reuse, quality and efficiency review found no simplification changes or skipped findings. Function apply, durable partial-progress receipts, provider trigger reconciliation and cloud acceptance remain pending; this plan is not an apply authorization or proof of artifact availability.

## Neon function apply and receipts (U14, U16 foundation)

`applyNeonFunctions` now uses the real pinned SDK apply engine for service and worker code, with shared policy construction for dry runs. It archives both bundles before submission, pins their hashes and stores private, atomic, synced progress receipts. Receipts include the verified target, build/artifact hashes, function IDs and every acknowledged deployment ID. Application environment values remain outside receipts; an activation-token-keyed HMAC detects changed resume inputs. The stage validates runtime connection identity, rejects management/migration variable names and identity overrides in application variables, and explicitly deletes old provider identity overrides under Neon's documented environment-merge semantics.

Resume skips verified completed functions, observes acknowledged submissions without reupload, retries a failed or unacknowledged submission only when its remote baseline remains valid, and refuses superseding/replaced or uncertain remote resources. Local locking serializes applies. Cancellation waits for a mutation already in flight so its late acknowledgement can still be recorded. Bounded reads cover both the SDK's post-deployment inspection and completion polling. Status verification requires the exact submitted deployment to be completed and active. This function stage neither enables triggers nor activates database grants.

Evidence: initial tests failed on the missing export, then exposed macOS canonical-path aliases. Two successive stalled-read regressions proved that polling and SDK post-deployment inspection both needed bounded reads; both now pass. Eight apply tests cover partial worker failure, source changes after archival, secret redaction/input changes, lost responses, pending completion, archive tampering, target drift, newer deployments, terminal failure retry history, local concurrency and late acknowledgement after cancellation. The five existing planner tests also pass: 13 scoped integration tests, 47 Bun expectations plus Node assertions. All 13 workspace checks pass with 107 unit tests and zero Vite+ findings. Inline ce-simplify-code reuse, quality and efficiency review retained the shared function policy and found no remaining simplification changes or skipped findings; archive publication was made atomic during correctness review. `deployment-recovery.md` documents the current guarantees and limits.

Full release orchestration, distributed locking around provider stages, explicit reconciliation of uncertain submissions, abandoned-lock recovery, schema compatibility/rehearsal, trigger activation and live Neon acceptance remain incomplete. These tests used the actual SDK engine and a controlled provider interface; no cloud resources were mutated.

## Disabled provider schedules and worker quarantine (U14 prerequisite)

`disableNeonTriggers` disables schedules and storage notifications attached to explicitly selected worker slugs, including inherited triggers, and verifies the final provider state. It leaves triggers for other functions alone. `prepareNeonScheduleTriggers` requires a completed worker deployment, creates or reconciles schedules in the disabled state, and returns actual provider IDs with their runtime bindings. Repeated preparation preserves existing IDs. Both operations recheck target identity around mutations and refuse ownership conflicts, ambiguous provider records and enabled final state. Schedule and binding validation reuse the runtime validators.

Evidence: five new unit tests initially failed on the missing exports. They cover reconciliation, inheritance, unrelated resources, partial failure and retry, malformed schedules, ownership conflicts, provider target drift and a provider ignoring disable requests. All 13 workspace checks pass with 112 unit tests and zero Vite+ findings. The real PostgreSQL cron integration now prepares a schedule through the tooling adapter, installs its returned binding in the worker, submits duplicate deliveries with the provider-issued ID, and verifies one durable occurrence and application effect. That focused integration passes. The provider interface is controlled in these tests; no live Neon resources were changed.

Inline reuse, quality and efficiency review retained the shared target context and core validators and reduced the disabling result to the metadata its callers need. No skipped findings remain. This stage leaves every prepared schedule disabled. Binding IDs into deployed worker artifacts, health checks, trigger activation, the release coordinator and later phases remain incomplete. Owned TypeScript imports remain extensionless.

## Verified storage object adapter (U15 foundation)

The Neon storage adapter now uses pinned AWS S3 client and presigner 3.1138.0 with explicit branch endpoint validation and path-style requests. Upload descriptors bind UUID, bucket, length, media type and SHA-256. Signed uploads address staging keys only; sealing validates metadata and bounded bytes before writing a separate server-controlled ready key. Signed downloads verify ready metadata. Explicit deletion derives a pending or ready key from the captured descriptor. Operations redact provider errors and bound request duration; shutdown destroys the owned client.

The first integration run failed on the missing adapter export. A strengthened retry scenario then reproduced a failed seal after staging changed despite the ready write already succeeding. Sealing now recovers a matching ready object first, falling back to staging only on provider 404. Three adapter integration tests pass against a local HTTP object server using the real AWS SDK and presigner. Together with all eight consumer/CLI integration tests, 11 tests pass with 101 Bun expectations plus Node assertions. All 13 workspace checks pass with 112 unit tests and zero Vite+ findings.

The ce-simplify-code reuse, quality and efficiency passes ran sequentially inline. Shared intent validation and metadata verification were retained; streaming verification uses one size-bounded buffer instead of retaining arbitrary numbers of chunks. No further changes or skipped findings remain. This is a low-level adapter, not completed storage authorization: private bucket provisioning, durable intent state, tenant policy, event ingestion, orphan cleanup and live Neon signature/metadata acceptance remain outstanding. See `docs/architecture/storage.md` for the provider constraints and verification limits.

## Durable authorized upload intents (U15)

Framework metadata version 10 stores branch-bound upload intents with database-generated UUIDv7 IDs, verified owner identity, an owner/request hash, a fixed upload specification, an upload deadline and pending/ready/failed state. All earlier migration hashes remain unchanged. Runtime grants permit insert/read and lifecycle updates, while refusing changes to existing ownership or upload specifications.

`createStorageIntents` requires a bucket allowlist, an explicit application authorization callback, a branch activation verifier and a matching storage backend target. It scopes access and retry keys to project, branch, deployment and the verified issuer/subject/tenant. Duplicate creation returns the same intent; conflicting retry data fails. Upload URLs use the saved pending intent and remaining deadline. Finalization seals bytes outside a database transaction, rechecks activation and records ready state. Verification failures become diagnosable failed intents; provider/database failures leave pending state for recovery. Downloads require current authorization and ready state before provider metadata verification and sixty-second signing.

Evidence: the new PostgreSQL integration first failed on the missing service export, while the migration fixture failed on the missing version 10. A separate privilege regression reproduced unrestricted runtime updates to saved upload metadata; the final grant permits updates only to state, error code and modification time. The fixture uses the real S3 adapter and local HTTP object server, runs as a non-owner runtime role, injects failure after the object write and recovers after staging disappears. It also exercises duplicate creation/finalization, tenant separation, copied-branch refusal, invalid buckets, changed retry inputs, policy/activation denial, upload expiration, verification failure and activation loss before the database update.

All 13 workspace checks pass with 112 unit tests and zero Vite+ findings. The full integration suite passed 64 tests with 643 Bun expectations plus Node assertions. After narrowing the runtime grant, all seven affected storage/bootstrap integrations passed again. Inline ce-simplify-code reuse, quality and efficiency reviews retained shared validation, target verification and the post-authorization deadline read. The HTTP storage fixture was extracted for reuse across provider and database tests; no remaining changes or skipped findings were identified. Public/runtime wiring, storage declaration/code generation, durable event ingestion, cleanup, live cloud acceptance and the remaining deployment/documentation/release phases are still incomplete.

## Durable storage event ingestion (U15)

Storage trigger bindings now include the expected bucket and require a storage dispatcher. The HTTP boundary verifies provider attestation, trigger ID/name/type, bucket and bounded payload before ingestion. It returns 202 after persistence without running application handlers. Schedule preparation rejects storage bindings.

`createStorageEventDispatcher` resolves branch-derived staging keys to saved intents, persists invocation fingerprints before reconciliation and rejects conflicting redelivery. After verification it locks the intent then the receipt, atomically enqueues one internal job, links it to the intent and completes the receipt. Different invocation IDs and later handler versions reuse the saved job. Verification failures become terminal failed receipts; missing objects and database failures retain pending receipts. Handler arguments contain verified metadata and the saved uploader as data, while the job identity remains null. `onObjectCreated` preserves typed internal function references and rejects handlers requiring arguments absent from the event.

Framework metadata version 11 adds receipts and an event-job link; prior migration hashes remain unchanged. The trigger unit test first failed because the storage binding was unsupported, and the PostgreSQL integration first failed on the missing dispatcher export. A type regression exposed covariance accepting extra required handler arguments; the corrected conditional reference signature passes TS7 negative checks. The integration uses real PostgreSQL, the real S3 adapter with a local HTTP object server, the HTTP trigger boundary and the real durable worker. It covers repeated/concurrent delivery, conflicting invocation IDs, invalid keys, completed-job redelivery, bad content, delayed objects and a database failure after queue insertion but before receipt completion.

All 13 workspace checks pass with 113 unit tests and zero Vite+ findings. All 65 integrations pass with 644 Bun expectations plus Node assertions; the final handler-signature change passed TS7 checks. A later regression reproduced case-sensitive object keys for case-insensitive PostgreSQL UUID input; intent IDs now normalize to lowercase and ingress requires the exact canonical staging key. All eight affected storage/bootstrap integrations passed again after that fix. Inline reuse, quality and efficiency review consolidated branch key derivation and stored-owner validation, retained the fixed database lock order and atomic receipt/job write, and found no further changes or skipped findings. Storage declaration/code generation, public/runtime assembly, disabled storage trigger provisioning, cleanup, cloud acceptance and later phases remain incomplete.

## Storage declarations and immutable generation (U15)

`defineStorage` captures bucket handlers and an explicit authorization policy, freezes the captured configuration and defaults to denial. Project loading accepts an optional branded `backend/storage.ts`, validates handler references against the current internal registry and job attempt ceiling, and includes the declaration in the immutable project bundle. Generated registries export storage. The build contract epoch advances to 4 so changed artifact semantics cannot collide with earlier generations.

Evidence: the declaration unit test first failed on the missing export. The consumer integration exercises omitted-policy denial, policy-only version changes, stale and wrong-kind handler rejection, attempt-limit rejection, forged declarations, unchanged active generation after errors, and fresh Node imports of the prepared policy after source becomes invalid. Owned TypeScript imports remain extensionless with Bundler resolution; emitted JavaScript uses runtime-resolvable paths. All 13 workspace checks pass with 114 unit tests and zero Vite+ findings. All 66 integrations pass with 654 Bun expectations plus Node assertions. Sequential inline ce-simplify-code reuse, quality and efficiency passes found no further changes or skipped findings; shared bucket and handler validators remain authoritative. Generated runtime assembly, public storage routes, provider provisioning, cleanup, cloud acceptance and later plan phases remain incomplete.

## Runtime storage ownership and worker assembly (U15)

Generated service/worker options now include the immutable storage declaration and accept an explicit backend factory with expected project/branch identity. The build contract epoch advances to 5. Runtime startup refuses missing or unused backend configuration, forged declarations, stale/wrong-kind handlers and excessive retry policies. The backend is created only after activation and database connection verification. Target mismatch or later startup failure closes owned storage and database resources.

Storage intents and event ingestion use the runtime's input capture, cancellation and pending-operation tracking. Shutdown rejects new calls, aborts in-flight work, waits for it to settle and then closes storage and database resources. Generated Neon workers wire configured storage triggers into durable receipt ingestion; a separate wake executes the queued internal job under explicit application authorization.

Evidence: the integration first failed because a runtime with declared buckets and no backend started successfully. The completed fixture uses non-owner PostgreSQL connections and separate real S3 adapters against the shared local HTTP object server. It verifies missing/forged/stale configuration rejection, target-mismatch cleanup, activation before connecting, captured identity, tenant denial, duplicate HTTP notifications, verified download, queued internal execution and cancellation/drain-before-close. Consumer tests confirm generated runtime storage identity, typed backend options and fresh Node imports. All 13 workspace checks pass with 114 unit tests and zero Vite+ findings. The full integration suite passes 67 tests with 656 Bun expectations plus Node assertions. Inline reuse, quality and efficiency review retained typed ownership wrappers and startup validation; test cleanup now releases blocked policies even on assertion failure. Public storage routes/client operations, provider credential/bucket/trigger provisioning, cleanup, cloud acceptance and later plan phases remain incomplete.

## Authenticated storage HTTP and browser client (U15)

The public service now mounts configured storage intent operations at `/api/loom/storage`. It reuses the existing bearer, exact-origin, method, content-type, bounded-read and cancellation guards. Storage always requires a verified session, even when functions allow anonymous callers. The strict operation envelopes accept upload metadata or a server-issued intent ID, never caller identity. Metadata requests are capped at 16 KiB. Expected storage failures use fixed 403/409/422 responses; policy callback details remain private.

The browser client exposes typed create/status/signUpload/finalize/signDownload methods through its existing bounded transport. Creation captures one retry key and descriptor before authentication/IO; retries retain the identity partition. Shared browser-safe validators keep server/client metadata aligned and validate signed URL schemes and intent-state consistency. Direct signed uploads send bytes to object storage; verified finalization and download authorization remain server operations.

Evidence: two new unit tests first failed on the absent route/client surface. A further regression reproduced acceptance of an impossible ready intent with a failure code; client response validation now rejects it. The integration exercises real tenant JWTs, non-owner PostgreSQL access, the actual runtime/application boundary, separate S3 clients and the HTTP object fixture. A lost successful create response retries to the same database intent. It also covers tenant denial, retry conflict, upload/finalize/download, repeated finalization, bad bytes, policy-error redaction, forged identity, disallowed origin, body limits and CORS preflight. TS7 negative checks reject identity arguments and misplaced creation retry keys. All 13 workspace checks pass with 117 unit tests and zero Vite+ findings. All 68 integrations pass with 656 Bun expectations plus Node assertions; the packed browser test also passes. Inline reuse, quality and efficiency reviews retained the shared transport and boundary guards and identified no further changes or skipped findings. Cloud credential/bucket/trigger provisioning, orphan cleanup, live acceptance and later plan phases remain incomplete.

## Private buckets and disabled storage triggers (U15)

`prepareNeonStorageBuckets` creates only missing declared private buckets on the inspected target and verifies their final access. Existing public buckets are refused. Lost create responses recover by the existing name. `prepareNeonStorageTriggers` requires private buckets and a completed active worker, refuses conflicting names, creates/reconciles disabled triggers and returns actual provider IDs with runtime bindings. Its exact branch staging prefix excludes ready-object notifications. Target and worker deployment identity are rechecked around mutations; no trigger activation is performed.

The first three preparation tests failed on absent exports. Five added unit tests now cover private bucket creation, public/missing buckets, response loss, partial retries, input duplication, unrelated ownership, inherited triggers, target/worker drift and ignored disable requests. The runtime storage integration now uses the actual pinned SDK's bucket/trigger HTTP serialization and parsing against a local control-plane fixture. It carries returned provider IDs and prefixes through real S3 adapters, non-owner PostgreSQL intent/receipt persistence and durable handler execution. Fixture setup was corrected to retain SDK prototype methods rather than spreading a class instance.

Correctness review found the pinned SDK silently normalized unknown/missing bucket access levels to private. A new integration regression reproduced this acceptance. A narrow Apache-2.0 `@neon/config@1.7.2` patch changes only that normalization and its comment, preserving both known values and rejecting everything else. The regression now rejects unknown, missing and public access metadata. A separate clean installation, frozen reinstall and fresh Node process verified known values and both rejection cases. Patch provenance and the requirement to preserve this behavior in consumer distribution are recorded in `patches/README.md`.

Sequential inline ce-simplify-code reuse, quality and efficiency passes retained the shared bucket validator, staging prefix, worker inspection and target guards. No further simplification changes or skipped findings remain. All 13 workspace checks pass with 122 unit tests and zero Vite+ findings. The full integration suite passes 69 tests with 656 Bun expectations plus Node assertions. Cloud credentials, worker binding deployment, health/activation, storage cleanup, the release coordinator, packed patch delivery and live Neon acceptance remain outstanding. No cloud resources were changed.

## Bounded storage cleanup and finalization serialization (U15)

Framework metadata version 12 adds an indexed cleanup deadline and backfills existing intents without changing earlier migration hashes. The internal cleanup capability scopes every row to deployment/project/branch and declared buckets, processes bounded batches with skip-locked rows and preserves durable history. It expires pending intents after a 24-hour recovery window, deletes staging and abandoned ready objects, preserves verified ready objects, retries provider failures after five minutes and revisits successful deletions daily for late uploads.

Finalization now holds the same intent row lock while sealing and committing metadata. SQL and provider writes remain separate transactions; object-write/database-failure recovery still passes. Activation checks inside these transactions use the transaction connection, avoiding a second-connection deadlock. Runtime maintenance participates in cancellation/draining and runs after jobs on scheduled Neon deliveries. Public storage routes do not expose cleanup.

Evidence: the new integration initially failed on the absent cleanup export. The strengthened worker test then showed a scheduled wake leaving expired staging behind before runtime wiring was added. Real PostgreSQL/non-owner and S3 fixtures now verify scope, expiration, ready-object preservation, bounded batches, deletion failure/backoff, late writes, cancellation, rollback after object deletion, both finalizer/cleanup races and a one-connection activation query. The contender race observes an actual PostgreSQL lock wait before releasing cleanup. Bootstrap verifies the version-11 upgrade and earlier hashes; runtime storage rejects cleanup after shutdown. All 13 workspace checks pass with 122 unit tests and zero Vite+ findings. The full suite passes 70 integrations with 657 Bun expectations plus Node assertions, followed by the strengthened lock-observation test.

Sequential inline reuse, quality and efficiency review retained shared runtime storage configuration, ownership wrappers, validation and row lock ordering. No additional simplification changes or skipped findings remain. Cloud credentials, release orchestration, consumer patch delivery, live acceptance, recovery/backfills, capacity evidence, documentation/examples and release workflow remain incomplete. No cloud resources were changed.

## Injected storage credentials and deployment bootstraps (U14/U15)

Official Neon environment documentation confirms that deployed functions receive branch storage credentials and endpoint/region variables automatically. Loom now consumes those injected values instead of issuing duplicate credentials. `createNeonStorageBackend` captures project/branch identity, delays environment reads until the activated runtime connects, creates a fresh client per connection and delegates endpoint/region validation to the existing adapter. Missing credentials and mismatched endpoints use a fixed error.

Deployment bootstraps supply the backend only for projects declaring buckets. The entry artifact epoch advances to 2. A shared injected-variable list prevents database/storage identity overrides and clears retained provider overrides during function apply, without recording injected secrets. Runtime database credentials cannot reuse those variable names.

Evidence: the new unit test first failed on the missing backend factory. It now covers delayed reads, target capture, credential rotation between connections, all four missing variables and wrong-branch/credential-bearing endpoints. Function-apply tests verify reserved variable refusal and override removal. A new integration prepares both storage entries, bundles them with the pinned provider SDK and starts the extracted bundles in fresh Node processes against a real PostgreSQL activation grant and non-owner runtime role. Correct injected fixture values permit startup; wrong-branch or missing endpoints return a redacted 503. The packed artifact is checked for absence of fixture credentials. Existing generation tests verify that projects without buckets omit storage setup.

Sequential inline ce-simplify-code review applied one reuse improvement (a shared injected-variable list) and one quality improvement (explicit generated option assembly). The efficiency pass found no further change; no findings were skipped. All 13 workspace checks pass with 123 unit tests and zero Vite+ findings. All 71 integrations pass with 658 Bun expectations plus Node assertions. Live credential issuance/SigV4 access, full release coordination, consumer patch delivery and later plan phases remain incomplete. No cloud resources were changed.

## Runtime database authority preflight (U14, KTD16)

`inspectRuntimeDatabase` checks the exact deployment runtime credentials in a read-only PostgreSQL 18 transaction. It validates the expected endpoint, database and role before connecting, verifies actual session identity, rejects administrative flags and role memberships, and inspects effective schema/database creation privileges and shared ownership dependencies. This includes enum ownership, which an initial table/routine-only implementation missed. It checks schema usage and activation-table readability and refuses writes to activation and migration history tables, including column-only grants. Results contain identity only; every failure uses a fixed redacted error. Transaction-local catalog search path and timeouts support pooled runtime URLs.

Evidence: the new real PostgreSQL integration first failed on the missing export; a subsequent enum-owner case failed against the incomplete ownership check. It now covers a restricted login, every administrative flag, non-inheriting/non-settable membership, explicit and PUBLIC schema creation grants, table/type ownership, activation column grants, migration history writes, missing usage/read grants, wrong credentials/identity/target, URL overrides, cancellation and an unchanged empty activation table. Test grants are revoked in finally blocks before the next case. No mock database or provider mutation is used.

Sequential inline ce-simplify-code reuse, quality and efficiency review retained the existing dedicated connection helper and identifier validators and improved fixture grant cleanup (one quality finding applied; zero reuse/efficiency findings or skipped findings). All 13 workspace checks pass with 123 unit tests and zero Vite+ findings. The full integration suite passes 72 tests with 658 Bun expectations plus Node assertions; the affected preflight test is rerun after the transaction-local settings adjustment. This preflight is a point-in-time authority check; application/schema health and its wiring into the release coordinator remain pending. No cloud resources were changed, and the full plan remains incomplete.

## Quarantined deployment startup probes (U14)

Generated service and worker entries now use `createNeonDeploymentEntrypoint`. Its reserved POST health route authenticates the activation token before creating a separate runtime. The preparation verifier shares the normal verifier's complete environment/database/grant binding checks but accepts a quarantined grant for this isolated startup. The probe immediately stops the application without invoking its dispatcher and returns only version, entry artifact hash and role. Normal traffic retains an independent active-only runtime. Concurrent probes join one startup, failures remain redacted and retryable, and shutdown drains both runtimes even when ordinary cleanup fails. Entry artifact identity advances to epoch 3.

Evidence: unit tests first failed on the missing factory. Additional failing cases reproduced non-ASCII authorization causing unequal byte lengths in constant-time comparison and shutdown returning before an outstanding probe after ordinary cleanup failure. The fixes validate token character sets before comparison and await both cleanup outcomes. Packed Node service/worker tests caught Drizzle's interpolation of arrays as SQL lists; the shared grant query now uses parameterized IN values. These real PostgreSQL tests verify active and quarantined startup, wrong branch/token denial, missing or mismatched injected storage configuration, unchanged grant state and a pending job with zero attempts after every probe. Normal requests still return 503 while quarantined.

Sequential inline ce-simplify-code reuse, quality and efficiency passes retained the shared grant verifier and normal entrypoint lifecycle, with one quality improvement capturing the authorization header once and no skipped findings. All 13 workspace checks pass with 126 unit tests and zero Vite+ findings. The full integration suite passes 72 tests with 658 Bun expectations plus Node assertions; the packed-entry test is rerun after the shutdown correction. Startup health does not establish schema compatibility, runtime authority or live storage access. Coordinator-side health requests, release ordering, trigger activation, cloud acceptance and later plan phases remain unfinished. No cloud resources were changed.

## Provider-bound deployment health observations (U14)

`inspectNeonFunctionHealth` reads the saved completed service/worker receipt and observes current provider target/function metadata before and after authenticated HTTPS probes. It rejects partial receipts, replaced/superseded deployments, changed targets or invocation URLs and responses whose version, artifact or role differs. Redirects are refused, response parsing is capped at 4096 bytes and the entire network observation shares a bounded deadline. It returns only non-secret identity evidence and does not rewrite receipts or activate grants/triggers. Provider SDK reads share the existing abortable observation helper extracted from function apply.

Evidence: the Node HTTPS fixture first failed on the missing export. It now exercises a temporary trusted CA and an untrusted client, both successful function probes, stale build identity, redirects without a follow-up request, oversized and stalled bodies, deployment replacement during probing, changed branch identity, partial receipts, invalid tokens and pre-aborted requests. A stalled SDK read times out; resolving it later sends no token-bearing request. Failure cases assert request counts so unrelated early failures cannot satisfy the checks. The saved receipt remains byte-for-byte unchanged. This fixture uses a local HTTPS responder and an injected provider metadata adapter; the separate packed-runtime PostgreSQL fixture verifies the actual health handler and quarantined runtime behavior.

Sequential inline ce-simplify-code reuse, quality and efficiency review applied one reuse improvement (sharing the provider observation helper), with no further quality/efficiency changes or skipped findings. All 13 workspace checks pass with 126 unit tests and zero Vite+ findings. All 73 integration tests pass with 658 Bun expectations plus Node assertions; the health fixture is rerun after strengthening request-count assertions. Full release ordering/receipts, trigger activation, cloud acceptance and later plan phases remain incomplete. No cloud resources were changed.

## Verified trigger activation and retry (U14/U15)

`activateNeonTriggers` accepts original disabled trigger snapshots and the final worker's expected function/deployment IDs. It verifies target identity, exact schedule or branch-scoped storage configuration, private buckets and a mandatory coordinator-supplied active-grant callback before each mutation and at completion. Unexpected enabled triggers attached to that worker are refused. Only enabled state is updated; unrelated workers are left alone. Already-enabled matching triggers satisfy a retry, while partial and lost-acknowledgement progress remains in provider state for reconciliation. A final read must prove that every selected trigger is enabled.

Evidence: unit tests first failed on the missing activation export. They now cover quarantine denial, partial update failure, lost acknowledgement, idempotent resume, a revoked grant after successful activation, schedule drift, worker identity/replacement, unknown enabled work, cancellation, ignored provider updates and target drift after a write. The runtime-storage PostgreSQL/S3 integration now sends the activation through the pinned Neon SDK and local control-plane HTTP fixture, verifies its minimal PATCH body, refuses public buckets and inactive guards, preserves the bound storage prefix and then delivers events to the worker. The integration's active-grant callback remains its existing fixture guard; full release coordination must supply the real database verifier.

Sequential inline ce-simplify-code reuse, quality and efficiency review reused existing target/trigger/bucket validators and worker inspection and applied one efficiency improvement by retaining the prepared ID set for membership checks. No further findings or skipped findings remain. All 13 workspace checks pass with 129 unit tests and zero Vite+ findings. All 73 integrations pass with 658 Bun expectations plus Node assertions. The full release lock, ordered orchestration and durable whole-release receipt remain unfinished, along with cloud acceptance and later plan phases. No cloud resources were changed.

## Shared activation session and database grant observation (U14)

`withDeploymentActivationSession` keeps preparation, active-grant reads and activation on one verified owner connection under the deployment advisory lock. Existing one-shot APIs now use the same grant operations. Its callback also receives that connection for coordinator database stages; callers must await methods sequentially and finish direct SQL transactions before invoking them. Concurrent method use and use after callback completion are refused, cancellation is checked before and after stages, and cleanup drains an in-flight method before closing the connection. The exact branch/build/token grant observation can serve as trigger activation's active-grant callback.

Evidence: the existing PostgreSQL quarantine integration first failed on the missing session export. It now verifies that a second connection cannot acquire the deployment lock during the callback, missing/quarantined/revoked and wrong-branch/token grants fail, an active matching grant succeeds, concurrent calls and aborted checks fail, all session methods reject after callback completion, and a failed callback releases the lock. Existing preparation, quarantine and activation tests remain in place. All 13 workspace checks pass with 129 unit tests and zero Vite+ findings. All 73 integration tests pass with 663 Bun expectations plus Node assertions; the focused quarantine test passes again with 21 expectations after adding the lock-release assertion.

Sequential inline ce-simplify-code reuse, quality and efficiency passes found no additional changes or skipped findings. The grant predicate and parameter construction remain shared across preparation, observation and activation. This provides activation-session plumbing, not the complete release coordinator: migration/quarantine session operations, durable release receipts, ordering and live cloud acceptance remain outstanding. No cloud resources were changed.

## Preview quarantine on the shared release connection (U14)

The activation session now exposes `quarantinePreview`, using the existing atomic grant revocation and job cancellation/fencing operation on its already-locked connection. The standalone quarantine API delegates to the same internal stage. Both paths retain explicit preview and metadata-owner checks; the session additionally retains its sequential-use, cancellation and closed-session guards. The returned receipt has an exported type. This stage does not disable provider triggers and must not be replayed as a harmless check after a preview has become active.

Evidence: the PostgreSQL quarantine integration first failed because the session method was absent. It now performs quarantine followed by grant preparation in one callback, checks the existing revocation/job counts and fencing results, refuses quarantine in a production session and refuses calls after session closure. Existing rollback-on-job-update-failure and runtime-role denial cases exercise the shared implementation through the standalone API. All 13 workspace checks pass with 129 unit tests and zero Vite+ findings; all 73 integrations pass with 665 Bun expectations plus Node assertions.

Sequential inline ce-simplify-code reuse, quality and efficiency passes found no further changes or skipped findings. The SQL mutation remains in one shared helper and safety checks remain intact. Migration session plumbing, whole-release receipt/order orchestration, cloud acceptance and later phases remain unfinished. No cloud resources were changed.

## Migrations on an owned release connection (U14)

`applyMigrationsOnConnection` exposes the existing migration pipeline for an active dedicated connection owned by Loom. Connection ownership is tracked only while the owning callback is active; arbitrary clients and closed sessions are refused before stage SQL. The ordinary `applyMigrations` API delegates to the same implementation. Both retain the session migration lock until connection closure, ORM transactions/savepoints, history and catalog validation, artifact reviews and nontransactional migration refusal.

Evidence: the existing PostgreSQL migration runner integration first failed on the missing export. It now runs the reviewed upgrade through `withDeploymentConnection`, verifies rejection of an arbitrary client, requires review, rolls back a failing NOT NULL upgrade, backfills and retries successfully on that same connection, verifies the second apply is empty, and proves another connection cannot acquire either the deployment or migration lock during the callback. Reusing the client after callback completion is refused. Existing concurrent apply, runtime privilege, fresh replay and drift assertions remain in place. All 13 workspace checks pass with 129 unit tests and zero Vite+ findings; all 73 integrations pass with 669 Bun expectations plus Node assertions.

Sequential inline ce-simplify-code reuse, quality and efficiency passes found no additional changes or skipped findings. The runner options schema and migration execution remain shared, and ownership tracking has bounded connection lifetime. Connecting migration and activation under one outer coordinator, complete release receipts/order, recovery, cloud acceptance and later phases remain unfinished. No cloud resources were changed.

## Migration-to-activation handoff under one deployment lock (U14)

`withDeploymentActivationSessionOnConnection` creates an activation session on an existing verified deployment connection after metadata bootstrap. The connection owner captures immutable target/database/namespace context under the lock and removes it when its callback ends. Borrowers cannot supply replacement target fields and cannot use an arbitrary or expired client. Sessions combine outer and local cancellation signals, check metadata ownership and retain the existing closed-session/sequential-operation guards. The normal activation-session API now delegates to this path.

Evidence: the migration runner integration first failed on the missing export. It now drops metadata and application schemas, rejects activation before bootstrap, replays migrations, prepares and activates a grant, and verifies it using the exact same client. Arbitrary and closed clients are refused; explicit local cancellation prevents callback entry and cancellation inherited from the connection blocks subsequent grant observation. The existing unit test caught token validation moving after provider access; the shared identity validator restores rejection before any provider call. Existing lock, rollback and replay assertions remain intact. All 13 workspace checks pass with 129 unit tests and zero Vite+ findings; all 73 integrations pass with 673 Bun expectations plus Node assertions.

Sequential inline ce-simplify-code reuse, quality and efficiency passes retained the shared activation implementation and bounded connection context. One quality improvement consolidates the repeated deployment options in the integration fixture; no findings were skipped. Whole-release receipts and ordering, compatibility/recovery, cloud acceptance and later plan phases remain incomplete. No cloud resources were changed.

## Durable ordered release journal (U14/U16)

`withNeonReleaseReceipt` persists release identity and ordered stage acknowledgements under a local per-release lock. Identity binds source/input hashes, target/database/namespaces, migration hashes and a declared schema compatibility range. Stage records retain bootstrap/final artifact and provider function IDs, prepared trigger snapshots/bindings and final enabled trigger IDs. Validation refuses skipped or conflicting stages, wrong migration heads, changed function/worker identities and mismatched final trigger IDs. Receipts use the existing function receipt's private atomic file/directory-sync writer, extracted into a shared helper. Failed writes make the session unusable until reopened; callback errors preserve saved progress and closed sessions cannot append.

Evidence: new unit tests first failed on the missing API. Subsequent failing tests exposed acceptance of changed final function identity and validator errors echoing supplied values; the implementation now checks identity consistency and uses fixed input errors. Tests cover initial/reopened progress, idempotent acknowledgements, detached snapshots, changed release inputs, local lock contention, private file permissions, corrupt-file refusal, the full ten-stage sequence, disabled trigger validation, wrong worker/final trigger IDs, write failure, refusal to continue an uncertain session and recovery after reopening. All 13 workspace checks pass with 132 unit tests and zero Vite+ findings; all 73 integrations pass with 673 Bun expectations plus Node assertions, including the existing function receipt/apply recovery tests after writer extraction.

Sequential inline ce-simplify-code review applied two reuse improvements: sharing the atomic receipt writer and the provider trigger snapshot validator. Quality/efficiency review found no further changes or skipped findings. The journal records caller acknowledgements; it does not prove remote state or compatibility. Whole-release orchestration/resume, migration-lineage compatibility checks, abandoned-lock recovery, live cloud acceptance and later plan phases remain unfinished. No cloud resources were changed.

## Schema bounds tied to committed migration history (U14/U16)

`inspectReleaseSchema` validates the committed artifact chain and exact ordered hashes, enforces namespace containment and resolves the declared schema bounds in migration order. The target must equal the committed head and fall within the resolved interval. Consecutive unchanged schemas from data-only migrations are supported. Optional migration-hash anchors distinguish a schema that disappears and later returns; a null anchor denotes the empty baseline. The release journal and inspector share the range validator, so anchors are retained in immutable receipt identity. The returned ordinals, schema list and migration hashes describe structural lineage only.

Evidence: the new unit test first failed on the missing inspector export. Tests use real programmatic Drizzle plans and committed artifacts for initial, additive, data-only and contract migrations. They cover changed history, foreign namespaces, absent/reversed bounds, excluded or stale target heads, edited SQL artifacts, empty history, ambiguous repeated schemas and explicit anchors, including nonexistent and wrong-schema anchors. All 13 workspace checks pass with 134 unit tests and zero Vite+ findings; all 73 integrations pass with 673 Bun expectations plus Node assertions.

Sequential inline ce-simplify-code review applied one reuse improvement by sharing the schema-range validator with the journal. Quality/efficiency passes found no further changes or skipped findings. This inspection does not establish application compatibility, live database state or contraction safety. Wiring these gates into the release coordinator, reader/job compatibility, resumable recovery, cloud acceptance and later plan phases remain incomplete. No cloud resources were changed.

## Live schema evidence on the release connection (U14/U16)

`inspectReleaseDatabase` combines committed schema-range inspection with live migration history/catalog checks on the verified deployment connection. It derives namespaces from captured connection context, refuses namespace substitution and requires initialized, consistent metadata with no pending migrations and exact target/hash agreement. Status observation runs in a read-only repeatable-read transaction under the shared migration lock, retained until the owner closes the connection. `migrationStatusOnConnection` provides the shared implementation; standalone status delegates to it. Returned evidence includes target/database identity, structural range and catalog hash, with no schema/grant mutations.

Evidence: the PostgreSQL migration integration first failed on the missing inspection export. It now rejects arbitrary/expired clients, pending migrations and namespace substitution; accepts the migrated head; compares observed catalog evidence; detects external DDL drift and succeeds after that drift is removed. Cancellation inherited from the release connection is also refused. Existing same-connection rollback/retry, deployment/migration lock and fresh replay assertions remain intact. All 13 workspace checks pass with 134 unit tests and zero Vite+ findings. All 73 integrations pass with 678 Bun expectations plus Node assertions; the focused migration integration passes again with 28 expectations after adding expiry/cancellation checks.

Sequential inline ce-simplify-code review applied one reuse improvement by extracting the existing status transaction for both APIs. Quality/efficiency passes found no further changes or skipped findings. Live structural evidence does not establish application compatibility or contraction safety. Release ordering/resume, old reader/job compatibility, cloud acceptance and later plan phases remain unfinished. No cloud resources were changed.

## Journaled database release preparation (U14/U16)

`withNeonReleaseDatabase` connects source/schema preflight, metadata bootstrap, explicit clone quarantine, committed migrations, live schema observation and grant preparation to the ordered release journal. It keeps the verified deployment connection, exclusive migration lock and local receipt lock through the caller's provider-stage callback. The receipt identity binds configuration, roles, schema bounds, artifact hashes, review acknowledgements and caller input fingerprint using the activation token as an HMAC key; neither the token nor database credentials are persisted. Source and artifact identity are rechecked under the lock. The migration runner can require exact expected hashes before applying its captured artifact list.

Resume checks live metadata/history/catalog before trusting acknowledgements. An acknowledged grant is inspected without recreating it; an acknowledged activation must still be active. Explicit clone quarantine refuses an already active target branch and never runs again after its acknowledgement. The preserve mode supports subsequent releases on an existing branch. A lost migration acknowledgement is reconciled through the ORM/history checks, while a missing or changed database grant stops the release. Production refuses clone quarantine before provider or database access.

Evidence: the new integration first failed on the missing coordinator export. PostgreSQL coverage exercises copied grants/jobs, callback failure, retry after activation, lost migration acknowledgement, same-branch preservation, lock ownership, changed token/role, stale source, cancellation, production quarantine refusal, live DDL drift and missing grants. The migration integration also verifies expected-hash mismatch before metadata creation. All 13 workspace checks pass with 134 unit tests and zero Vite+ findings. All 74 integrations pass with 692 Bun expectations plus Node assertions; the two focused integrations pass again with 47 expectations after strengthening lost-acknowledgement and preserve-mode coverage.

Sequential inline ce-simplify-code review retained the plan's settled decisions, shared grant observation with preparation and removed an unnecessary signal-options helper. No further efficiency changes or skipped findings. This is the database portion of the coordinator: provisioning, provider function/trigger ordering and reconciliation, runtime credential inspection, compatibility/contraction, CLI wiring, cloud acceptance and remaining plan phases are still required. No cloud resources were changed.

## Function and trigger preparation on the release journal (U14-U16)

`withNeonReleasePreparation` extends the locked database sequence through runtime credential inspection, bootstrap function deployment, private buckets, disabled schedule/storage triggers, and final functions containing the acknowledged trigger bindings. It derives cron schedules and buckets from the loaded project and includes a one-minute durable-job wake schedule. Reserved provider variables cannot be supplied as application secrets. Slugs, secrets and generated trigger declarations are bound into the release identity by a keyed fingerprint. The callback retains the database and receipt locks for the remaining health and activation stages.

Resume follows the release journal. After trigger preparation is acknowledged it never reapplies bootstrap functions or reconciles triggers by mutation. It instead checks target identity, bucket privacy, exact trigger bindings and function IDs, then resumes the final function receipt. Acknowledged function stages require their existing completed receipts; missing receipts do not cause a replacement upload. An active grant requires an acknowledged final function stage, and retries preserve already enabled matching triggers. Trigger and bucket observations run both before and after final function application. These remain observations of cooperating provider state, not an atomic transaction with the provider.

Evidence: the integration first failed on the missing preparation export. The fixture uses PostgreSQL 18, real project generation, SDK policy planning and archive creation, and a typed in-memory provider control plane. It interrupts the final worker upload after the final service completed, then proves that retry uploads only the unfinished final worker. It covers private storage, disabled triggers, changed secrets, changed trigger schedules, changed bucket privacy, replaced function IDs, active-release retry and missing saved function receipts. All 13 workspace checks pass with 134 unit tests and zero Vite+ findings. All 75 integrations pass with 718 Bun expectations plus Node assertions; the focused integration passes again with 24 expectations after adding replacement, active-release and missing-receipt cases.

Sequential inline ce-simplify-code review shared the existing slug validator and trigger identity comparison (two reuse improvements); quality and efficiency passes found no further changes or skipped findings. Health, final live-schema observation, database/trigger activation, provisioning, compatibility/contraction and CLI wiring still need to be connected, followed by cloud acceptance and the remaining plan phases. The fixture is not evidence of a live Neon deployment. No cloud resources were changed.

## Release health and activation coordinator (U14-U16)

`deployNeonRelease` runs the preparation sequence on an explicitly selected, already provisioned branch, verifies both packed functions over authenticated HTTPS, then rechecks live migration/catalog state, runtime credential authority and source identity. Health acknowledgements are historical: every retry probes again, even when the saved journal says complete. Observed function IDs/deployment IDs must match the final release acknowledgement. Database activation runs only after the trigger helper has verified the provider target, current worker, private buckets and prepared trigger identities. The same locked activation session verifies the active grant before each trigger enable operation. Completion records the exact enabled trigger IDs.

Failure preserves the acknowledged prefix. Failed health or post-health schema/role checks leave the grant quarantined. Trigger preflight failure also prevents database activation. If database activation succeeds but its acknowledgement is lost, retry reconciles the matching active grant. If a trigger update fails, retry preserves enabled matching triggers and finishes the remaining updates without uploading functions or repeating quarantine. This coordinates observable stages; it does not claim atomicity across PostgreSQL and provider operations.

Evidence: the new integration first failed on the missing coordinator export. A subprocess trusts a temporary test CA and serves the SDK's unpacked function archives over real HTTPS against PostgreSQL 18. It tests a failed health response, DDL drift and role privilege escalation introduced during health checking, trigger changes before activation, partial trigger enablement, a deliberately removed activation acknowledgement, preserved pending jobs, and failure of a fresh health check after prior completion. After recovery, the typed client calls the deployed task query over HTTPS under an explicit fixture authorization policy. All 13 workspace checks pass with 134 unit tests and zero Vite+ findings; all 76 integrations pass with 721 Bun expectations plus Node assertions.

Sequential inline ce-simplify-code reuse, quality and efficiency passes found no additional changes or skipped findings; the coordinator delegates to the existing checked stages. Provider provisioning, CLI wiring, compatibility/contraction, backfill and nontransactional recovery, cloud acceptance and remaining plan phases are still required. The in-memory provider control plane and local HTTPS runtime are not live Neon acceptance. No cloud resources were changed.

## Release declaration and CLI (U14, R12/R23)

`loom deploy --release <project-relative JSON file>` now calls `deployProjectRelease`, which resolves a strict release declaration and environment-variable references before invoking the existing coordinator. The declaration keeps the exact release key, source version, ordered migration hashes, schema bounds, role names, quarantine mode and function slugs. Secrets remain in the environment. Reserved administrative/token sources and provider-variable destinations are refused, as are unknown fields and escaping paths. Retry uses the same receipt identity. CLI success returns a receipt; errors use a fixed diagnostic and exit code 5. Signal handlers request cancellation and are removed on completion. Source imports remain extensionless.

Evidence: the full-release fixture first failed on the missing helper export, and the subprocess CLI test failed on the missing command. The fixture now retries and completes through the release-file helper against PostgreSQL 18 and packed runtimes over HTTPS. It also refuses raw secret fields, missing environment values, reserved sources/destinations, stale source versions, escaping paths and pre-cancelled calls before provider deployment. CLI subprocess cases cover unrelated flags, unsupported dry-run, positional arguments and redacted invalid declarations. A subsequent empty-positional regression failed before the guard was changed to require exactly one positional argument, then passed. All 13 workspace checks and 134 unit tests pass; the full integration run passed 77 tests with 721 Bun expectations plus Node assertions, followed by focused passes for the additional validation and CLI edge cases.

Sequential inline ce-simplify-code reuse, quality and efficiency reviews retained the shared release/slug validators and found no further behavior-preserving simplifications or skipped findings. The argument guard correction was a separate correctness fix. The release runbook documents the declaration and updates stale coordinator status. CLI success is covered through its shared tooling helper; a live provider-backed CLI deployment, provisioning, whole-release dry-run, compatibility/contraction, recovery, cloud acceptance and the remaining plan phases are unfinished. No cloud resources were changed.

## Release dry-run (U14, R12/R16/R23)

`loom deploy --release <file> --dry-run` now uses `planProjectRelease`. It shares declaration validation and desired schedule/storage resources with apply, obtains the explicit target's migration connection, makes that session read-only, and observes migration/catalog state under the deployment/shared migration locks. It reads saved acknowledgements without creating a receipt or write lock. Function intent comes from the real Neon SDK planner; bucket/trigger intent comes from the shared declarations and current provider observations. The output identifies metadata, quarantine, pending SQL/reviews, bootstrap/final function passes, buckets, triggers, initial disablement, activation and observed conflicts. Application secrets remain unresolved and unreturned. Local immutable generation is permitted; database/provider mutations, health probes and receipt writes are excluded.

The result distinguishes observations from checks still required during apply. No blocker is not a deployment-success or compatibility guarantee. Runtime credentials, secret-bound receipt identity, archive construction, fresh state, health and activation are checked by apply. Invalid input/observation errors remain redacted; observed blockers produce the plan with exit code 5. Other commands refuse `--dry-run` rather than silently ignoring it.

Evidence: the integration first failed on the missing planner export, and the CLI test failed before the flag was supported. The PostgreSQL/packed-runtime fixture removes application environment values for initial planning, traps provider mutation methods, verifies no metadata/receipt creation, and checks planned function/bucket/trigger creation. Planning a completed release leaves its receipt bytes, active provider resources and pending jobs unchanged, even with health probes configured to fail. Added cases cover public buckets, trigger ownership/binding conflicts, DDL drift, changed release identity, replaced active function IDs, active-branch clone refusal, copied-work preservation refusal, custom migration review and nontransactional artifacts. The fixture confirms planned backfill/index SQL never runs. All 13 workspace checks and 134 unit tests pass; all 77 integrations pass with 721 Bun expectations plus Node assertions, followed by a focused pass for additional migration cases.

Sequential inline ce-simplify-code review consolidated receipt-file parsing (one reuse improvement); the quality and efficiency passes found no further changes or skipped findings. Shared resource derivation preserves existing deployment fingerprint ordering. Live provider-backed CLI acceptance, provisioning, application compatibility/contraction, recovery, cloud acceptance and remaining phases are still required. No cloud resources were changed.

## Acknowledged branch infrastructure creation (U14)

`planNeonBranchProvision` observes an explicit existing PostgreSQL 18 project and parent ID without mutation. `provisionNeonBranch` creates the branch through the pinned configuration runtime's typed API, requests production protection when applicable, and saves the acknowledged branch ID before looking for its read-write endpoint. Private atomic receipts bind all inputs and serialize local writers. Retry observes the saved branch instead of submitting again; completed receipts still verify branch and endpoint identity. Cancellation waits for an in-flight create acknowledgement. A lost response remains uncertain and refuses automatic name-based adoption or duplicate creation.

Evidence: the new tests first failed on missing exports. Subsequent failing regressions exposed raw provider observation errors and duplicate endpoint IDs across branches; fixed diagnostics and uniqueness checks now reject both. Seven unit tests cover explicit target/version/name preflight, acknowledged retry, endpoint replacement, uncertain creation, cancellation, concurrent writers, private file permissions, corrupt/escaping receipts and production protection drift. The integration drives the real pinned SDK HTTP adapter against a local server, verifies the explicit create payload and proves retry sends one create request. All 13 workspace checks pass with 141 unit tests and zero Vite+ findings; all 78 integrations pass with 721 Bun expectations plus Node assertions.

Sequential inline ce-simplify-code reuse, quality and efficiency passes retained the shared atomic receipt writer and found no further behavior-preserving changes or skipped findings. Error redaction and endpoint ambiguity were separate correctness fixes. Source imports remain extensionless. This stage creates infrastructure only: project/database/role provisioning, CLI composition, uncertain-create reconciliation, abandoned-lock recovery, application compatibility/contraction and later phases remain unfinished. The pinned typed SDK lacks role/database creation methods; that integration needs a deliberate implementation path. Database quarantine and activation remain in the release coordinator. The HTTP fixture is not live Neon acceptance. No cloud resources were changed.

## Branch provisioning declaration and CLI (U14, R12/R23)

`loom provision --branch <file> [--dry-run]` exposes the acknowledged branch creation flow. A project-contained strict JSON declaration holds `format: 1` and the explicit branch options; the shared validator refuses unknown fields, including credentials. The tooling helpers `planProjectBranchProvision` and `provisionProjectBranch` read this declaration without executing project modules. The CLI resolves provider authentication through the existing API, forwards cancellation, returns structured plans/receipts, and uses fixed failure diagnostics with exit code 5. Extra positional arguments and unrelated options are refused. The command reports infrastructure completion separately from database preparation and release activation.

Evidence: the helper integration first failed on missing exports and the CLI subprocess failed before the command existed. The strengthened HTTP fixture now launches the actual CLI with the pinned SDK, confines its transport to a local server, and checks successful dry-run, failure after acknowledged creation, and successful retry without a second create request. It checks JSON output and credential redaction, then re-observes the same receipt through the public tooling helper. Invalid fields, malformed JSON, escaping paths and pre-cancellation are refused before creation; dry-run leaves no `.loom` directory. Separate CLI subprocess cases cover invalid declarations, mixed flags and positional arguments. All 13 workspace checks pass with 141 unit tests and zero Vite+ findings; all 79 integrations pass with 721 Bun expectations plus Node assertions.

Sequential inline ce-simplify-code review shared the existing provisioning options validator (one reuse improvement). Quality and efficiency passes found no further changes or skipped findings. The runbook documents command usage, retained retry identity and the creation-only dry-run. Project/database/role provisioning, composition with the full release, uncertain-create reconciliation, abandoned-lock recovery, application compatibility/contraction, cloud acceptance and later phases remain unfinished. The redirected local HTTP transport is not a live Neon deployment. No cloud resources were changed.

## Local Bun runtime transport (U8)

`startDevelopmentServer` provides the missing local transport for an assembled runtime. It listens on loopback, delegates ordinary HTTP to the existing application adapter, and uses Bun's native WebSocket upgrade with the shared Loom session/poller. Socket admission requires an allowed Origin, the exact protocol/ticket pair and successful runtime ticket redemption. Reservations bound both pending and open connections, with handshake deadlines and cleanup on refusal, expiration, disconnect and shutdown. Stop closes the transport, drains application work and stops the owned runtime; startup failure also releases runtime ownership. This transport does not bypass activation or start a job/cron wake loop.

Evidence: the new integration first failed on the missing export. The real Bun socket fixture now covers HTTP calls, origin/protocol refusals before redemption, negotiated subprotocol, authenticated subscription results, capacity refusal, ticket replay, expired authentication, occupied-port startup cleanup, active request/socket shutdown, idempotent stop and port reuse. Delayed ticket redemption cannot open a socket after shutdown. The assembled-runtime PostgreSQL integration also serves a query through the new adapter and checks that its runtime-role database connections close. Type checking caught socket state optionality and the DOM declaration hiding Bun's documented client headers overload; the fixture uses a documented platform-specific constructor assertion. All 13 workspace checks pass with 141 unit tests and zero Vite+ findings; all 80 integrations pass with 723 Bun expectations plus Node assertions.

Sequential inline ce-simplify-code reuse and quality reviews retained the existing HTTP/session implementations and found no further changes. Efficiency review removed two unnecessary Set snapshots during shutdown; stopped admission prevents additions during iteration. No findings were skipped. The development runbook records ownership and configured bounds without claiming measured capacity. Development target/credential activation, coherent runtime replacement, jobs/cron wake composition, the `loom dev` command and abandoned-lock recovery remain required for U8. Other unfinished framework phases and cloud acceptance remain active. No cloud resources were changed.

## Runtime replacement on the development listener (U8)

The local server now owns separate generation lifetimes. `replace(candidate, signal?)` validates an already started candidate and cancellation before synchronously switching the listener. Existing HTTP work and socket callbacks retain their original generation; old sockets close for resynchronization and old requests drain before runtime cleanup. The listener and URL remain stable. Rejected candidates are stopped without replacing the active generation. Already owned runtime objects cannot be submitted again, and replacement is serialized against retirement and rejected-candidate cleanup.

The result distinguishes installation from retirement: `retired: false` leaves the new generation installed, records the old cleanup failure and refuses another replacement until restart. Shutdown drains the active/retiring generations and candidates already being discarded, then reports a fixed cleanup error if any cleanup failed. Source-version, schema, database activation and generated-reference checks remain the responsibility of the development pipeline before replacement.

Evidence: the new tests first failed on the missing `replace` method. Four integration scenarios cover invalid/stale/concurrent/reused/late candidates, uninterrupted new HTTP routing while old dispatch drains, old socket closure, new-generation WebSocket results, failed retirement, stop during replacement and rejected-candidate cleanup. A strengthened race test failed because shutdown resolved before a rejected candidate finished cleanup; the server now tracks and drains those disposals and retains cleanup failures. The PostgreSQL runtime fixture replaces a real runtime and verifies that only its successor's connection remains before final shutdown closes it. All 13 workspace checks pass with 141 unit tests and zero Vite+ findings; all 84 integrations pass with 725 Bun expectations plus Node assertions.

Sequential inline ce-simplify-code review retained the extracted generation owner and shared application/session implementations. Quality review made the detached socket-open callback's lack of `this` explicit; no further reuse/efficiency changes or skipped findings. The shutdown ownership gap was a separate correctness fix. Automatic source-driven composition, development credentials/activation, job/cron wake loops, `loom dev`, abandoned-lock recovery and remaining framework phases are still unfinished. No cloud resources were changed.

## Development connection identity before activation (U8, R13)

Development connections now require the provider-returned direct URL to match the selected endpoint, role and database before opening a database session. Target inspection rejects duplicate endpoint IDs across branches, validates branch names, returns an immutable identity and rechecks the name after acquiring the migration lock. Provider lookup failures, malformed metadata and malformed URLs produce fixed diagnostics without retaining credential-bearing provider causes.

Evidence: the strengthened real PostgreSQL test first failed because a connection for a different endpoint reached the callback. Unit regressions also failed on accepted duplicate endpoint IDs, absent branch names and raw credential errors. The completed tests cover endpoint mismatch before SQL, branch-name drift after locking, malformed URL/metadata rejection and fixed credential-lookup errors. Existing protected/default-branch, lock-release and additive-schema synchronization tests remain green. Local database fixtures now derive their synthetic endpoint ID from the fixture host to exercise the same URL binding as deployment fixtures; they do not bypass validation. All 13 workspace checks pass with 143 unit tests and zero Vite+ findings; all 84 integrations pass with 726 Bun expectations plus Node assertions.

Focused review kept the development helper scoped to its existing migration-lock lifecycle; it does not introduce an unused activation context or weaken the deployment helper. Owned TypeScript imports remain extensionless, including the CLI export, under the existing TS7 Bundler configuration. Development runtime activation/composition, wake loops, `loom dev`, recovery and remaining framework phases are still unfinished. No cloud resources were changed.

## Verified development runtime startup (U8, R13)

`startDevelopmentRuntime` now assembles a synchronized project generation using separately resolved runtime credentials. Development connections acquire deployment then migration locks, matching release ordering. Startup checks metadata ownership, development history, synchronized source identity, catalog drift, runtime database host/port and least privilege before preparing and activating the existing hash-only grant. A fresh provider observation immediately before the grant rejects changed identity or protection. Copied foreign grants/work remain refused rather than implicitly quarantined or adopted.

The local activation verifier captures credentials per generation and shares the existing durable grant checks. It neither reads nor changes provider environment variables; deployed Neon verification still reads them at every boundary. Concurrent local generations retain independent version grants. Startup returns the owned runtime and secret-free binding/target after rechecking source identity and cancellation, and closes constructed resources on failure, including failure while releasing the administrative session. It does not publish generated references or open a listener. Failed startup preserves committed schema and can leave its grant active for an exact-identity retry; it never revokes a grant potentially used by another same-version runtime.

Evidence: the new integration first failed on the missing public startup export. Type checking exposed discovery's loss of executable function typing; discovery now preserves the registered runtime function type without a cast. The PostgreSQL scenario verifies unsafe-role and mismatched-credential refusal before grant creation, two simultaneous versions without environment mutation, token mismatch refusal, source/synchronization mismatch, independent grant revocation, foreign-branch refusal, pool closure, storage construction failure, cancellation and edits detected after runtime construction. A separate red regression within the fixture demonstrated that protection could change during credential preflight; the final provider observation now refuses this before writing the grant. Failed startup leaves the synchronized nullable column intact and does not apply the later edit. All 13 workspace checks pass with 143 unit tests and zero Vite+ findings; all 85 integrations pass with 730 Bun expectations plus Node assertions.

Sequential inline ce-simplify-code passes retained shared credential resolution, grant operations and activation verification. The quality pass moved the runtime function type beside its definition and preserved its existing public re-export, eliminating a circular type dependency. No additional reuse/efficiency changes or skipped findings. Development watcher/publication/replacement composition, credential provisioning, explicit development quarantine, wake loops, `loom dev`, recovery and remaining phases are still unfinished. No cloud resources were changed.

## Automatic development generations (U8)

`startDevelopment` now owns the source watcher and local listener. Each current revision prepares immutable artifacts, synchronizes an allowed schema edit and starts a verified runtime before publishing references. Initial failures leave the watcher alive for recovery on save; later failures preserve the serving generation. Unchanged source versions do not restart the runtime. The generated-reference rename invokes a synchronous publication hook that installs the matching runtime before cleanup. A failure after that commit preserves the new generation instead of pretending publication rolled back. Shutdown cancels and drains the watcher before closing the listener and its runtime.

The server's optional publication callback keeps the previous generation serving until installation. Concurrent replacement is refused; callbacks that never install are rejected and their candidates stopped. Installation callbacks become inert when their publication closes, including while rejected-candidate cleanup is still pending. Shutdown waits for an in-flight publication and stops any generation it installs. Failed retirement still requires restart.

Evidence: the new end-to-end integration first failed on the missing startup export. Publication tests first failed because the callback was ignored; a stronger race then demonstrated that a late callback could install a candidate during its cleanup. Both regressions are fixed. Real PostgreSQL, filesystem watching and Bun HTTP cover failed initial compilation, recovery, nullable edits, credential failure after committed DDL, destructive-edit refusal, rapid revisions, provider protection, cancellation, pool/port cleanup and restart. Runtime and generated references retain matching versions throughout the checked outcomes. All 13 workspace checks pass with 143 unit tests and zero Vite+ findings; all 88 integrations pass with 732 Bun expectations plus Node assertions.

Sequential inline ce-simplify-code passes retained the existing watcher, synchronization, runtime startup and publication helpers. The quality pass removed redundant outer retirement state: the publication promise now owns publication and retirement together. No additional reuse/efficiency changes or skipped findings. Owned TypeScript imports remain extensionless. The `loom dev` command, job/cron wake loops, credential provisioning, explicit development quarantine, recovery, live Neon acceptance and remaining framework phases are still unfinished. No cloud resources were changed.

## Development declaration and CLI (U8, R12-R15/R23)

`loom dev` now starts the watcher from a strict, project-contained `loom.dev.json` declaration, with `--development` for another file. The public `startProjectDevelopment` helper shares startup and transport validators, captures the activation secret through a declared environment name and returns the existing development owner. The CLI reports watching, serving versions/URLs, failed updates and successful shutdown in human or newline-delimited JSON output. Failed edits remain watchable; recovery reports readiness again. SIGINT/SIGTERM cancel the command's status wait and drain the watcher/runtime. Terminal startup, watcher and cleanup failures produce a fixed diagnostic with exit code 5; usage errors return 2. Project/provider exceptions and secret values are not printed.

Evidence: the CLI test first failed because `dev` was unrecognized. It now covers invalid declarations, missing/reserved secret sources, invalid ports, extra options/positionals, escaping paths, custom declaration paths, initial compile failure and SIGTERM cleanup without exposing a sentinel error. The existing real PostgreSQL watcher scenario now begins through the declaration helper and also launches the actual CLI with the pinned Neon SDK against a local HTTP fixture. It verifies a served query, failed edit, same-version recovery, structured status, secret redaction and Ctrl-C port cleanup. All 13 workspace checks pass with 143 unit tests and zero Vite+ findings; all 89 integrations pass with 732 Bun expectations plus Node assertions. The expanded invalid-declaration cases also pass in a focused CLI rerun.

Sequential inline ce-simplify-code passes retained shared validation, path containment and development ownership. The quality pass removed a duplicate stop call so the finalizer alone drains the owner before reporting successful shutdown. No additional reuse/efficiency changes or skipped findings. Imports remain extensionless. CLI storage backend composition, credential provisioning, explicit development quarantine, background wake loops, recovery, live Neon acceptance and remaining framework phases are unfinished. No cloud resources were changed.

## Job version ownership before automatic development wake-up (U8/U13/U16)

Tracing the planned development wake loop exposed a shared queue defect: claim selection used deployment identity without the job's source version. A newer worker could claim an older pending job, fail dispatch with `VERSION_MISMATCH` and consume its retry attempts. Claim selection and bounded expired-attempt recovery now require the exact queue source version. They preserve the existing database locks, fencing and due-time ordering within that version. Older work stays durable for a matching worker; this change does not implement old-handler retention or cross-version compatibility.

Evidence: the strengthened PostgreSQL queue integration first failed when the newer queue returned the older job instead of null. It now proves that older pending jobs retain zero attempts, newer work can execute despite older due work, another version cannot reclaim an expired lease, and terminal expired-attempt recovery remains owned by the matching version. Existing transaction rollback, deduplication, lease fencing, retry and replay scenarios remain green. All 13 workspace checks pass with 143 unit tests and zero Vite+ findings; all 89 integrations pass with 732 Bun expectations plus Node assertions.

Focused review retained the existing SQL selection and lease protocol; no abstractions or public API changes were needed. Automatic development wake loops, old-handler retention/draining, storage composition, provisioning/quarantine, recovery and remaining framework phases are still unfinished. No cloud resources were changed.

## Automatic development job polling (U8/U13)

Published development generations now poll the existing durable worker in bounded, non-overlapping passes of ten jobs. The configurable `jobPollMs` interval defaults to one second and applies after each pass completes or fails. Candidates do not poll before publication. Replacement halts the previous generation's worker before starting the new loop; shutdown clears timers, blocks queued admission, requests cooperative worker cancellation and drains its work before closing runtime resources. The standalone startup helper still does not poll by itself.

`workerFailure` exposes a fixed diagnostic for infrastructure or activation failures, clears after a successful pass and leaves polling eligible for retry. The CLI reports a failure once per observed episode. Individual failed jobs keep their existing durable retry policy. The public `createDevelopmentJobLoop` provides explicit start/halt/stop ownership; cleanup failures are retained without exposing underlying errors. Declaration and programmatic interval validation share one validator.

Evidence: the new unit tests first failed on the missing loop export. A strengthened shutdown regression then showed that a delivered timer could admit its queued worker pass after halt; admission now rechecks the stopped state. Four unit scenarios cover delayed start, bounded serialization, retry timing, failure redaction, queued admission, cancellation/draining and cleanup failure. The real PostgreSQL development fixture schedules jobs through HTTP and observes durable success before and after replacement, exercises activation denial and recovery, and verifies automatic execution through the actual CLI and pinned SDK transport fixture. Existing listener/pool shutdown and failed-edit recovery checks still pass. All 13 workspace checks pass with 147 unit tests and zero Vite+ findings; all 89 integrations pass with 732 Bun expectations plus Node assertions.

Sequential inline ce-simplify-code reuse, quality and efficiency passes retained the existing worker's activation, lease, execution and cancellation contracts. No additional simplification changes or skipped findings. The queued-admission race was a separate correctness fix. Cron occurrence dispatch, retention/draining of old handlers, CLI storage composition, provisioning/quarantine, recovery, live Neon acceptance and remaining framework phases are unfinished. No cloud resources were changed.

## Local development cron occurrences (U8/U13)

Published development generations now dispatch observed UTC minute occurrences from captured project cron schedules. MIT-licensed `cron-parser` 5.10.1 supplies calendar matching after Loom's existing numeric five-field validator; Luxon 3.7.2 is locked transitively. The current [Neon schedule reference](https://neon.com/docs/compute/functions/triggers/schedule#cron-reference) confirms five numeric fields and UTC. Local calendar semantics are tested explicitly; this is not live provider equivalence acceptance.

The loop starts at the next minute boundary, skips startup/downtime gaps, serializes dispatch and retries failed entries at most once per second while their observed minute remains current. Successful entries are remembered for that minute; persisted queue deduplication remains authoritative. Clock rollback cannot redeliver the startup minute or an already observed minute. Replacement and shutdown halt admission, cooperatively abort active dispatch and drain both background loops before runtime closure. `cronFailure` and the CLI's fixed `DEVELOPMENT_CRON_FAILED` diagnostic expose failures without leaking arbitrary exceptions. Standalone runtime startup returns captured schedules without starting timers.

Evidence: unit scenarios first failed on the missing loop export. A separate red regression exposed clock rollback dispatching the startup minute; initialization now marks that minute consumed. Ten unit scenarios cover UTC boundaries, omitted-minute behavior, rollback, retry, cancellation/draining, dialect validation, Sunday aliases, weekday ranges, steps, day-of-month/day-of-week matching and leap day. The real PostgreSQL development scenario waits for a real minute boundary and observes its durable cron job succeed through the automatic worker. Existing replacement, activation recovery, CLI and shutdown tests remain green. All 13 workspace checks pass with 157 unit tests and zero Vite+ findings; all 89 integrations pass with 732 Bun expectations plus Node assertions.

Sequential inline ce-simplify-code reuse, quality and efficiency passes retained the shared schedule validator and durable runtime dispatcher. No further simplification changes or skipped findings. Runtime cleanup also covers calendar construction failure before ownership transfers. Retention/draining of older job handlers, CLI storage composition, provisioning/quarantine, recovery, live Neon acceptance and remaining framework phases are unfinished. No cloud resources were changed.

## Development storage configuration and private bucket preflight (U8/U15)

`loom.dev.json` now accepts explicit storage target, endpoint, region and credential environment names. Credentials are captured before project execution, with distinct sources from the activation token and management key. The existing Neon backend factory accepts optional captured connection settings while preserving lazy injected credentials for deployed functions. Both paths share endpoint validation; each runtime owns a separate adapter. The AWS SDK receives a mutable copy of credentials rather than the frozen session snapshot.

Development runtime startup now checks declared buckets through the provider before creating its activation grant. Missing, public, duplicate or unavailable bucket metadata is refused with fixed diagnostics; backend target mismatch is also refused. The helper reuses release bucket parsing and retains the final development target observation. It does not create buckets or credentials, and administrators must preserve bucket privacy after startup.

Evidence: new tests first showed explicit backend settings being ignored, missing/private-bucket validation allowing startup, and the CLI refusing a valid storage declaration. Unit coverage verifies credential capture despite caller mutation, independent adapter ownership, signing with the captured access key and invalid configuration refusal. PostgreSQL coverage verifies rejection before activation/backend construction, provider failure redaction, successful declaration-driven startup and connection cleanup. The actual CLI integration loads storage through the pinned SDK's HTTP bucket metadata path, serves the authenticated storage route, recovers after a failed edit and shuts down without exposing fixture secrets. Existing storage signing, HTTP, event, cleanup and packed deployment tests pass. All 13 workspace checks pass with 158 unit tests and zero Vite+ findings; all 89 integrations pass with 732 Bun expectations plus Node assertions.

Sequential inline ce-simplify-code passes found the production reuse and resource ownership appropriate. One redundant test setup was removed; no skipped findings. Development provisioning/quarantine, retention/draining of old handlers, migration recovery and compatibility, cloud acceptance and remaining framework phases are unfinished. No cloud resources were changed.

## Explicit development database quarantine (U8/U14)

`loom dev quarantine` now uses the development declaration and verified target to revoke active grants and cancel pending/running jobs in the metadata namespace. The command is explicit and is never part of ordinary startup or synchronization. It reuses the preview transaction, including metadata-owner enforcement, rollback, lease removal and fencing-token increments. Preview-only entry points retain their environment guard; the shared transaction now reports a branch-neutral failure message. The development connection supplies target validation, deployment/migration locks and a fresh target observation before mutation.

The declaration reader is shared with normal startup. Configuration can now be loaded independently while preserving the existing configuration hash in full project generation. Quarantine does not load backend schema/functions or read activation/storage secrets. CLI cancellation drains the database operation; fixed diagnostics and structured receipts omit credentials. Provider triggers and already-performed external effects remain outside this database-only command.

Evidence: the new database scenario first failed on the missing quarantine export; the CLI regression first returned usage exit 2 instead of entering quarantine. PostgreSQL tests cover protected/default/production-shared targets, protection changes during credential resolution, cancellation, metadata-owner refusal, transaction rollback, fencing and repeat runs. The actual CLI/SDK integration quarantines seeded work with invalid backend syntax and no activation-token value, then verifies cancelled jobs and revoked grants. The sequential inline ce-simplify-code reuse, quality and efficiency passes retained the shared transaction, config hash and command ownership patterns; no further changes or skipped findings.

All 13 workspace checks pass with 158 unit tests and zero Vite+ findings; all 90 integrations pass with 732 Bun expectations plus Node assertions. Development credential provisioning, retention/draining of old job handlers, migration compatibility/recovery, cloud acceptance and remaining framework phases are unfinished. No cloud resources were changed.

## Explicit concurrent index recovery (U16)

Reviewed custom nontransactional artifacts can now add simple column B-tree indexes through `migrations apply --recover-nontransactional`. The SQL parser must match the declared index-only snapshot expansion. Unsupported operations remain refused. Framework metadata version 13 journals the artifact identity and baseline catalog before DDL; status reports incomplete recovery and deployment remains blocked until it completes. Runtime credentials cannot mutate this journal.

Recovery compares the baseline catalog while excluding only artifact-owned new indexes, checks each existing index's ownership and definition, preserves valid indexes and rebuilds invalid ones. Completion records ORM and Loom histories and removes the journal atomically. A failed history commit preserves valid DDL for the next attempt. Release checks now reject pending nontransactional artifacts rather than rejecting already applied history.

The first competing-runner regression exposed PostgreSQL deadlock 40P01: a blocking advisory-lock SELECT retained a virtual transaction needed by concurrent index DDL. All owned migration/deployment lock waits now use bounded nonblocking acquisition with delays outside SQL statements; connection lifetime still owns the locks. This also covers read-only status and development connections.

Evidence: core recovery first failed on the missing option and CLI recovery first returned usage exit 2. Real PostgreSQL coverage exercises duplicate-data failure, invalid-index rebuilding, unrelated policy drift, mismatched existing indexes, lost history checkpoints, preserved index OIDs, competing runners and exactly-once history completion. CLI coverage generates, fails and resumes the reviewed artifact. Bootstrap upgrade rehearsals retain earlier framework hashes; runtime preflight rejects column-level journal write grants. Additional parser cases refuse IF NOT EXISTS, expressions, predicates, descending columns, wrong columns, foreign namespaces and data updates. A release regression refuses pending recovery and succeeds after explicit application.

All 13 workspace checks pass with 158 unit tests and zero Vite+ findings. The full integration run passed 91 tests with 752 Bun expectations plus Node assertions; the final three-test recovery/release run passed after adding parser and release regressions. Sequential inline ce-simplify-code reuse, quality and efficiency passes retained existing connection ownership, catalog evidence, parser and ORM history boundaries; no additional simplification changes or skipped findings. Checkpointed backfills, general nontransactional recovery, handler retention, rollback/contraction, cloud acceptance and remaining framework phases are unfinished. No cloud resources were changed.

## Durable row backfill checkpoints (U16)

Backfill plans now bind a reviewed SQL update, application namespace/table, committed migration hash and batch size. Project generation publishes an immutable JSON artifact without replacing a prior plan. The CLI provides `backfill generate`, `backfill apply` and read-only `backfill status`, with explicit review, optional bounded batches and cooperative signal handling. Source generation and live applied history are checked before execution; fixed CLI diagnostics omit underlying database errors.

Framework metadata version 14 captures the initial visible row IDs in a durable work list. Each UUID-ordered batch locks surviving rows and commits data updates, work-list removal and progress in one transaction. Returned IDs must exactly match the assigned live rows. Deleted captured rows have their own counter; later inserts are outside the captured population and require compatible application writers. Completed updates are not replayed after interruption. Runtime credentials cannot change progress or work-list rows. Pending schema application reports `BACKFILL_IN_PROGRESS` while any backfill remains running; resumption still works after a later migration has been generated.

Evidence: the initial integration failed on the missing plan API. PostgreSQL tests exercise bounded interruption, SQL failure, checkpoint-write failure after updates, concurrent resumptions, altered artifact and checkpoint identity, unrelated drift, overbroad UPDATE rollback, deleted rows, observed in-flight cancellation and resumption with a pending later schema migration. The CLI fixture generates a persisted plan, refuses overwrite, observes unstarted/running/completed states across separate processes, resumes and proves rows were updated once. Its expanded subprocess coverage required increasing its timeout from five to thirty seconds. Framework upgrade rehearsals preserve versions 1-13; runtime preflight rejects write grants to both new metadata tables. Two unit tests cover plan identity and invalid SQL/target/batch contracts.

All 13 workspace checks pass with 160 unit tests and zero Vite+ findings. All 93 integrations pass with 783 Bun expectations plus Node assertions; the final backfill/CLI regression pass is also green after adding checkpoint failure injection. Sequential inline ce-simplify-code reuse, quality and efficiency passes retained transaction, parser, catalog and connection ownership boundaries. One quality cleanup replaces positional CLI apply parameters with named options; no reuse/efficiency changes or skipped findings. Backfill abandonment/reconciliation, old-handler retention, rollback/contraction, cloud acceptance and remaining framework phases are unfinished. No cloud resources were changed.

## Runtime schema compatibility declarations (U16)

Release source schemas may now occur anywhere within the declared supported range while the target remains the committed migration head. Framework metadata version 15 records source identity, ordinal bounds and ordered migration hashes per namespace/deployment/version. Replacement declarations preserve source identity and the existing artifact prefix; an active version must continue to cover the currently applied database. Runtime preflight rejects writes to the new table.

The migration runner checks active grants and pending/running job versions before pending DDL or data migrations. Missing versions or declarations, insufficient bounds and mismatched artifact history fail closed. Repeated schema hashes cannot bypass the ordinal check. Read-only release planning reports INCOMPATIBLE_RUNTIME and evaluates the incoming declaration without storing it; apply repeats validation after metadata upgrade and any clone quarantine.

Evidence: the old-source release regression first failed on source/target equality. A later regression exposed planning accepting a proposed range that excluded the current database of an active version; planning now uses the same active-current bound. PostgreSQL coverage demonstrates original Drizzle readers working after a nullable-column expansion, contraction refusal despite a repeated schema hash, queued-job dependencies surviving grant quarantine, contraction after dependency removal, source-identity refusal and runtime write denial. Release fixtures verify read-only proposed declarations, missing-version refusal during explicit index recovery and source exclusion. Framework upgrade rehearsals preserve versions 1-14.

All 13 workspace checks pass with 160 unit tests and zero Vite+ findings. The full integration run passed 94 tests with 785 Bun expectations plus Node assertions; the final three-test compatibility/release pass is green after the active-current regression fix. Sequential inline ce-simplify-code passes narrowed the proposed declaration input to its required fields and computed expected hash serialization once per check. No skipped findings. Old-handler retention, in-flight runtime/socket retirement, complete rollback/contraction, backfill abandonment, cloud acceptance and remaining framework phases are unfinished. No cloud resources were changed.

## Persistent function name ownership (U16 prerequisite)

Framework metadata version 16 reserves release service/worker names by provider project, branch, logical deployment, source version and role. Release preparation refuses a conflicting owner before function or trigger mutation, while same-owner bootstrap/final passes and retries remain supported. Reservations persist across failed releases and local receipt loss. The read-only planner reports FUNCTION_NAMES_RESERVED; runtime preflight rejects table or column writes. This uses the existing owned deployment connection and lock.

Evidence: a second-source-version regression initially reached provider deployment using the previous release's names. It now fails at ownership inspection with unchanged provider functions and call counts. Tests also cover swapped roles, the original owner's repeated preparation and read-only conflict detection for a different logical deployment. Framework upgrade rehearsals preserve versions 1-15; runtime authority coverage includes column-level reservation writes. All 13 workspace checks pass with 160 unit tests and zero Vite+ findings. The full integration suite passes 94 tests with 789 Bun expectations plus Node assertions; the final focused preparation regression also passes.

Sequential inline ce-simplify-code reuse, quality and efficiency reviews retained the existing connection, metadata and preflight patterns. Queries select at most two reserved names, and both inserts share one atomic statement. No simplification changes or skipped findings. This is overwrite prevention within one metadata authority, not completed handler retention: shared trigger names still prevent a complete multi-version release. Trigger handoff, old-worker draining, retirement, rollback/contraction, cloud acceptance and remaining framework phases are unfinished. Pre-existing unregistered functions, code-only deployment and external provider mutations remain outside the reservation gate. No cloud resources were changed.

## Worker-scoped triggers and recoverable ingress handoff (U16)

Release trigger names now include the worker slug, allowing distinct runtime versions to prepare separate disabled wake, cron and storage triggers. Runtime cron bindings retain the application declaration name. A two-version preparation rehearsal proves the older functions and enabled triggers remain unchanged while the candidate stays quarantined.

Framework metadata version 17 records candidate/current/retired ingress claims. Database release preparation refuses a retired release key before quarantine or migration application. After fresh health checks, the coordinator atomically claims ingress, retires other claims and disables older active workers' cron/storage triggers before activating new ingress. Old active grants and verified once-per-minute wake schedules remain available for version-specific queued jobs. Retrying an interrupted provider disablement repeats live inspection; a superseded release cannot re-enable its triggers. Planning exposes retained workers, old trigger IDs and RELEASE_SUPERSEDED without writes. Runtime preflight rejects claim mutations.

Evidence: side-by-side preparation first failed because the old global schedule name belonged to another worker. The handoff test first failed on its missing module/API. A separate missing-wake regression exposed unsafe disablement; handoff now refuses missing, disabled, misrouted or altered wake schedules before continuing. PostgreSQL/provider fixtures exercise a failed handoff, retained old grants and wakeups, disabled old storage ingress, repeat recovery and superseded-key refusal. Packed HTTPS release planning/application tests verify superseded refusal through the public project API. Framework upgrade rehearsals preserve versions 1-16.

All 13 workspace checks pass with 161 unit tests and zero Vite+ findings. The full integration suite passes 94 tests with 808 Bun expectations plus Node assertions; the final preparation/activation pair passes after simplification. Sequential inline ce-simplify-code passes retained shared trigger validation/disablement and extracted one shared retained-worker query for planning and application. One quality cleanup limits handoff arguments to the fields it consumes. No efficiency changes or skipped findings.

This establishes ordered provider handoff, not atomic cutover or complete runtime retirement. Already admitted ingress can still finish, delivery gaps are not reconstructed, and a full old-handler execution rehearsal across two active cloud releases remains outstanding. Runtime ingress fencing, safe worker retirement, complete code rollback/contraction, cloud acceptance and remaining framework phases are unfinished. Unscoped existing wake names require deliberate migration rather than inferred ownership. No cloud resources were changed.

## Transactional runtime ingress fencing (U16)

Generated Neon entrypoints now supply a dedicated ingress verifier through the runtime to cron and storage dispatchers. A shared lock spans each enqueue transaction; the release coordinator acquires the matching exclusive lock before changing the current ingress claim. The verifier checks branch-bound activation and the current source version. Wake deliveries and old queued-job execution retain their existing activation checks. The entry artifact format hash advances to prevent silently reusing archives without this wiring.

Storage performs an early admission check and repeats it in the enqueue transaction after object verification. A cutover between them creates no callback job and leaves the receipt pending. Activation checks in the final storage transaction use that transaction's database. Runtime preflight now requires read access to ingress claims and still refuses mutations; bootstrap grants that SELECT without changing historical framework migration hashes.

Evidence: the initial regression failed on the missing ingress verifier export. The real PostgreSQL test observes handoff blocked on the old cron transaction, then rejects a later old-version occurrence while executing the previously queued old handler. It runs the runtime pool with one connection. A second red regression showed repeatable-read admission was accepted; the verifier now requires READ COMMITTED, avoiding stale claim snapshots after lock waits. Nonblocking shared-lock polling honors cancellation and has a five-second bound. Storage coverage cuts over between verification and enqueue, checks the pending receipt and missing job, and retries successfully. Permission coverage removes claim SELECT access and verifies preflight refusal.

All 13 workspace checks pass with 161 unit tests and zero Vite+ findings. The full integration suite passes 95 tests with 808 Bun expectations plus Node assertions; the final four-test ingress/storage/release/preflight pass is green after the isolation and cancellation checks. Sequential inline ce-simplify-code reviews retained the shared activation verifier, transaction ownership and lock-key helper. Capturing the storage ingress callback removes a non-null assertion; no further reuse/efficiency changes or skipped findings. Old runtime/socket retirement, complete rollback/contraction, pending storage reconciliation, live two-version deployment acceptance and remaining framework phases are unfinished. No cloud resources were changed.

## Durable pending storage reconciliation (U16)

Storage dispatchers now expose bounded reconciliation through the runtime's shutdown/cancellation owner. Framework metadata version 18 adds a pending-receipt retry index and durable one-minute retry time. A short transaction validates current ingress and claims due receipts with row locks and SKIP LOCKED. Object verification then runs outside that transaction through the existing receive path, retaining its saved-owner checks, intent-level job identity and atomic terminal receipt update. Selection is scoped to project, branch, deployment and configured buckets.

A typed retired-ingress result distinguishes a positively observed different current version from missing authority or database failure. Retained workers skip receipt admission but still execute their queued jobs. Neon schedule delivery runs jobs before reconciliation to prevent slow object verification from starving existing work. Mid-batch errors retain pending receipts for a later attempt; cancellation propagates. Removed bucket handlers leave receipts pending for explicit handling.

Evidence: the regression first failed because reconcile did not exist. Real PostgreSQL tests now cover concurrent one-effect recovery after cutover, retry deferral and progress past an unavailable object, later successful recovery, target isolation, invalid limits, cancellation, retired ingress and authority errors. The real ingress verifier test confirms a retired version returns an inactive batch while its queued handler still executes. Trigger unit coverage checks wake/job/reconciliation ordering. Bootstrap upgrade rehearsals preserve migration hashes through version 17; an initial downgrade-fixture ordering error was corrected before acceptance.

Sequential inline ce-simplify-code reuse, quality and efficiency passes kept the existing receive path, activation checks, transaction lifecycle and bounded query. No further simplification edits or skipped findings. This recovers persisted receipts only; undelivered cutover events, safe old runtime/socket retirement, complete rollback/contraction, live cloud acceptance and remaining framework phases are unfinished. Owned TypeScript imports remain extensionless. No cloud resources were changed.

Validation: all 13 workspace checks pass with 162 unit tests and zero Vite+ findings. The full PostgreSQL integration suite passes 95 tests with 809 Bun expectations plus Node assertions. Formatting and diff checks pass. No browser run was needed for this server-only increment.

## Explicit retained-code release recovery (U16)

Release declarations and the public preparation API now accept an optional retainedReleaseKey. It requires a new release key, preserve mode, a completed retained receipt for the exact source/deployment/target/database, matching function names and a migration-history prefix. Pending migrations are refused. The retained activation must remain active; read-only planning reports RETAINED_RUNTIME_INACTIVE when it does not.

Preparation adopts only bootstrap, trigger and final-function acknowledgements after checking both local function receipts. Existing archive/input/provider verification then runs against the retained final deployment. It neither overwrites active functions nor replays bootstrap trigger preparation. Health and activation stages are not copied: the release coordinator repeats health, database and credential checks before its normal ingress handoff. Superseded old release keys remain refused; the new declaration owns recovery and retries.

Evidence: the first regression failed on the missing retained-release input. Preparation now reuses the old active version after a newer candidate's ingress handoff with unchanged provider functions/call counts. It rejects missing/incomplete receipts and changed names. A populated PostgreSQL rehearsal generates and applies a nullable-column expansion, restores the exact older source, and reuses retained function receipts while preserving both original rows and expanded data. It first verifies that rollback refuses pending DDL. Packed HTTPS runtimes exercise declaration parsing, read-only planning, fresh health failure without claim changes, success, idempotent retry, public reads and superseded-original refusal. An existing fixture restore was narrowed to one release key once the fixture held multiple claims.

Sequential inline ce-simplify-code reuse, quality and efficiency reviews retained the shared retained-receipt inspector for planning/application, existing function acknowledgement and archive verification, and existing locked release lifecycle. No simplification edits or skipped findings. No cloud resources were changed. This is retained-code recovery, not restoration of deleted/revoked deployments or completed live two-version cloud acceptance. Safe old service/socket retirement, contraction proof and remaining framework phases are unfinished.

Validation: all 13 workspace checks pass with 162 unit tests and zero Vite+ findings. The full PostgreSQL integration suite passes 95 tests with 816 Bun expectations plus Node assertions. A final packed HTTPS pass also verifies the inactive-retained-runtime planner blocker. Formatting and diff checks pass; no browser run was needed for this deployment-only increment.

## Version-bound durable client sessions (U16)

Framework metadata version 19 adds client-session records and namespace/version bindings on connection tickets. Redemption consumes a matching ticket and records its authenticated session expiration in one transaction. A nonblocking shared migration lock precedes the runtime activation recheck, using explicit READ COMMITTED isolation. The migration runner's exclusive lock serializes its compatibility observation with admitted redemptions. Unexpired session records require the corresponding runtime declaration to cover both the current and pending migration range, even after its activation grant has been removed.

Runtime credentials can insert and read session records but cannot update or delete them. Credential preflight rejects those extra permissions, including column-level expiration updates. The ledger stores hashed ticket identifiers and runtime identity, without authentication identity or bearer tokens. Closed sockets and failed upgrades conservatively remain protected through session expiration. Automatic expired-record pruning and early retirement are not implemented; metadata owners can prune expired rows. Older unscoped tickets are refused by the new runtime, while old binaries still rely on retained active grants.

Evidence: the initial red regression accepted a ticket at the wrong source version. Real PostgreSQL coverage now verifies version/namespace isolation, concurrent single consumption, session-lifetime tracking, migration lock contention in both directions, rollback when tracking insertion fails, runtime permission refusal, and contraction refusal until the old client record expires. Existing ticket expiration/row-lock races remain green. Bootstrap upgrades preserve historical migration hashes through version 18.

Sequential inline ce-simplify-code reuse, quality and efficiency passes retained the existing transaction, activation and compatibility paths; no simplification edits or skipped findings. All 13 workspace checks pass with 162 unit tests and zero Vite+ findings. The full integration suite passes 95 tests with 821 Bun expectations plus Node assertions. The React browser test passes with 5 expectations. Formatting and diff checks pass. No cloud resources were changed. This is a conservative dependency guard; in-flight request fencing, safe old-runtime/socket retirement, full deployment-boundary rehearsal and the remaining framework phases are unfinished.

## Database-handler retirement boundary (U16)

The assembled runtime now wraps its dispatcher connection's transaction callback. Before identity binding or any SELECT freezes the transaction snapshot, it takes ACCESS SHARE on the activation table with NOWAIT and rechecks activation on the same transaction. PostgreSQL retains that lock through commit or rollback. An owner using ACCESS EXCLUSIVE can therefore wait for admitted database handlers and commit a revocation before later handlers inspect authority. Queries retain read-only repeatable-read isolation; mutations retain serializable isolation and the existing whole-callback retry loop. Public/internal dispatch and subscription evaluation share the boundary without adding another pool connection. Neon entrypoint format advances to 5.

Evidence: a real PostgreSQL regression first passed the outer activation check, revoked the grant and still executed the handler. The fixed test refuses that delayed request before its handler runs. It also holds a query handler open and verifies that the owner's nonblocking exclusive lock fails until the handler transaction completes. The test uses runtime credentials and a one-connection pool. The existing assembled-runtime test covers mutation scheduling, worker handlers, live queries and shutdown.

Sequential inline ce-simplify-code reuse, quality and efficiency passes retain the existing transaction owner, retry loop and activation verifier. No simplification edits or skipped findings. The pre-transaction activation check remains because it also gates actions and request preparation. This is not yet a coordinated retirement operation: storage work, action side effects, service/socket shutdown and older binaries need separate lifecycle handling before contraction can rely on complete drainage. No cloud resources were changed.

Validation: all 13 workspace checks pass with 162 unit tests and zero Vite+ findings. The full PostgreSQL integration suite passes 95 tests with 825 Bun expectations plus Node assertions. The browser test passes with 5 expectations. Formatting and diff checks pass. Full coordinated retirement and the remaining framework phases remain unfinished.

## Storage retirement boundary (U16)

Storage intents now hold the activation-table admission lock across creation, reads, signed URL generation and finalization. Cleanup holds it through provider deletion and the metadata commit. Storage events record admission and receipts together under the barrier, then reacquire it for final dispatch; pending reconciliation also takes it before authority checks. The transaction uses its own database for activation reads. Runtime database handlers and storage share one internal lock helper, with no new public API.

The initial PostgreSQL regression observed no activation-table lock during upload signing. Provider-call assertions now verify the lock remains held for upload signing, sealing and download signing. Existing paused finalization and cleanup rehearsals also verify that an exclusive retirement lock cannot pass either provider operation. Concurrent intent creation, object-write/SQL-failure recovery, activation revocation, pending receipt recovery and one-connection pool cases remain covered.

This still requires a coordinated retirement operation and service/socket lifecycle handling. Already issued presigned URLs retain their provider expiration. No cloud resources were changed. The user selected Neon project late-moon-6948364 (loom) for cloud validation. Plugin project reads currently fail with INVALID_ARGUMENT for a supplied project_id, plugin search reports Unknown tool, and the local Neon CLI reports a missing unauthenticated default profile. The user authorized CLI authentication and its browser flow completed, but project and branch reads then returned an internal authorization error. No credentials were printed or stored in the repository.

Sequential inline ce-simplify-code reuse, quality and efficiency passes retain the shared lock helper, local intent transaction wrapper and existing provider recovery paths. No additional simplification edits or skipped findings. All 13 workspace checks pass with 162 unit tests and zero Vite+ findings. The full PostgreSQL integration suite passes 95 tests with 825 Bun expectations plus Node assertions; the browser test passes with 5 expectations. Formatting and diff checks pass. Coordinated retirement and the remaining framework phases are unfinished.

## Runtime database-drain capability evidence (U16)

Neon health now reports the database-drain protocol from the actual started application. The assembled service and worker advertise version 1, which identifies their database-handler and storage admission barrier. Older or custom applications without the capability report 0. The health inspector validates each role independently, defaults omitted fields to 0 and rejects unknown protocol values while retaining its existing HTTPS, build identity and provider re-observation checks. This prepares retirement to refuse binaries without the barrier; it does not implement retirement or prove that requests, actions or sockets have drained.

Evidence: the initial health regression returned undefined protocol values instead of conservative zeroes. HTTPS fixtures now cover legacy, supported, mixed-role and unknown protocol responses. The packed release activation rehearsal verifies both real runtime factories report version 1. The full integration run passed 94 tests and exposed one stale exact-response assertion in the packed storage test. That assertion now requires version 1 for both roles, and the focused rerun passes. All 95 integration cases therefore have passing evidence across those runs. All 13 workspace checks pass, including 162 unit tests and zero Vite+ findings. Formatting and diff checks pass. Browser verification was not repeated for this health-contract change.

Sequential inline ce-simplify-code reuse, quality and efficiency reviews found no changes needed: the capability uses the existing startup probe and validated health observation, with no extra network calls or persistent cache. No skipped findings. No cloud resources changed; a fresh read of Neon project late-moon-6948364 still returns an internal authorization error after successful CLI authentication. Coordinated retirement, live cloud acceptance and remaining framework phases remain unfinished.

## Retired activation grant state (U16)

Framework metadata version 20 adds `retired` to activation grants without changing historical migration statements. Normal preparation and session inspection refuse retired grants. Activation predicates accept only quarantined or active states, so replaying an old preparation or activation cannot revive a retired grant. The read-only release planner reports RUNTIME_RETIRED for the deployment/source version even with a new release key. Runtime startup continues to require active authority. A retired ingress claim remains distinct: it allows the old runtime to finish queued jobs. No public operation performs grant retirement yet; safe dependency observation, the exclusive database barrier and service/provider lifecycle coordination remain required.

Evidence: the first PostgreSQL regression failed because the old constraint rejected retired state; the planner regression separately failed to report retirement. Coverage now exercises session and one-shot preparation/activation refusal, runtime startup refusal, unchanged retired state after retries, read-only planning under a new release key, and upgrade from version 19 while preserving all historical hashes. Existing active/quarantined retry paths remain covered. Sequential inline ce-simplify-code reuse, quality and efficiency review kept the atomic predicate and existing grant/session helpers; it removed an unnecessary test-only binding extraction. No other changes or skipped findings.

All 13 workspace checks pass, including 162 unit tests and zero Vite+ findings. The full PostgreSQL run passed 94 of 95 tests. Its only failure was the unchanged development filesystem-watcher test observing a third activation after settled(); both watcher tests passed on immediate focused rerun without code changes. This is unresolved intermittent watcher evidence, not a fixed failure or a fully green single integration run. The browser test passes with 5 expectations. Formatting and diff checks pass. No cloud resources changed. U16 coordination and the remaining plan phases are incomplete.

## Filesystem-watcher test isolation (U14)

The intermittent watcher assertion above mixed a generated-file write with source/setup notifications that could still arrive after settled(). Its documented contract only waits for currently observed work. Thirty repetitions of the original fixture passed, but moving the output assertion before source edits reproduced the extra update in 25 of 30 repetitions. Temporary event tracing showed delayed rename notifications for backend and schema.ts alongside correctly ignored generated-file notifications. The tracing was removed and the original build output restored.

The source-edit fixture now records its stopped baseline after stop() drains callbacks. A separate real-filesystem fixture starts with an empty watched directory and exercises nested files in all six ignored artifact directories, then verifies that a source file still schedules an update. This preserves a strict no-update assertion without unrelated source/setup events. All three watcher tests pass across 30 repetitions (90 executions). Temporarily removing the ignore filter from build output makes the artifact test fail with three updates instead of one; the build output was restored before normal verification. No runtime behavior or settled() contract changed.

Validation after restoration: all 13 workspace checks pass, including 162 unit tests and zero Vite+ findings. The complete PostgreSQL integration run passes 96 tests with 829 Bun expectations plus Node assertions. Formatting and diff checks pass. No browser rerun was needed for the test-only correction. This addresses the recorded watcher-test timing issue; it does not claim that filesystem notifications are delivered exactly once. Retirement coordination, cloud acceptance and the remaining plan phases remain unfinished.

## Coordinated database-authority retirement (U16)

The public retireNeonReleaseDatabase stage now combines completed release receipts, verified deployment connection ownership, fresh per-role health capability evidence, the namespace migration lock and the activation-table barrier. It refuses a changed target, namespace or token, incomplete receipts, unsupported runtime drainage, current/candidate or missing retired ingress claims, pending/running jobs for the retiring version (including jobs without a version), and unexpired client sessions. Under the exclusive barrier it rechecks authority and dependencies before committing retired state. A busy barrier returns a retryable refusal rather than waiting indefinitely. Cancellation before commit and database update failures roll back.

A retry after commit verifies the saved target/token and dependencies without requiring the now-retired runtime to pass startup health. It returns database retirement evidence while retaining provider functions, triggers, archives, receipts and job/session rows. It does not implement provider deletion, action-side-effect drainage, forced socket shutdown or CLI wiring. Those remain explicit work; this stage is not a claim that U16 or the full framework is complete.

The initial packed-runtime regression failed on the missing operation. The HTTPS/PostgreSQL rehearsal now verifies current ingress, unknown-version queued jobs, matching-version running jobs, live sessions, missing receipts, wrong namespace/token, legacy worker health, failed health, cancellation, migration-lock contention, a held shared database barrier, injected update failure, successful retirement, idempotent retry with health unavailable, and refusal of an existing client's query after retirement. The receipt and provider deployment/enable counts remain unchanged. An early running-job fixture omitted its required lease fields; those were supplied without changing the production job constraints.

Sequential inline ce-simplify-code reuse, quality and efficiency passes retained the existing deployment connection, migration lock and health inspector. The quality pass renamed the local state reader for clarity; no other changes or skipped findings. No cloud resources changed. Live Neon authorization remains unresolved.

Validation: all 13 workspace checks pass, including 162 unit tests and zero Vite+ findings. The complete PostgreSQL integration suite passes 96 tests with 829 Bun expectations plus Node assertions; the final strengthened retirement fixture also passes in a focused rerun. The browser test passes with 5 expectations. Formatting and diff checks pass. Provider/service retirement, cloud acceptance and the remaining framework phases are unfinished.

## Database retirement CLI (U16)

`loom retire database --retirement <file>` now exposes the saved-release database retirement stage. Its strict declaration records the release key, environment, database, migration role and activation-token environment variable name. The tooling wrapper loads operational configuration without compiling application schema or functions. Reserved privileged token sources are refused. The CLI rejects unrelated flags, reports redacted failures with exit 5, returns structured retirement evidence and forwards termination signals through the existing cancellation boundary. Provider/service retirement remains separate and unfinished.

Evidence: the initial CLI regression returned usage exit 2 instead of the new retirement failure exit 5. The packed HTTPS/PostgreSQL fixture verifies missing tokens, reserved token sources, successful declaration-based retirement with deliberately invalid application source, and a real CLI subprocess retry against the PostgreSQL grant through the provider HTTP adapter. It checks exact structured output, empty stderr, no token disclosure, retained receipt/provider counts and rejection of queries after retirement. The CLI argument test verifies missing declarations, wrong subcommands, unsupported dry-run and accidental declaration flags on init. The first fixture run used the wrong schema path; it was corrected to the existing backend/schema.ts. Typechecking caught an explicit undefined optional signal, and anti-slop rejected the subsequent conditional empty-object spread; the final implementation follows the existing deployment wrapper's input construction.

Sequential inline ce-simplify-code reuse, quality and efficiency reviews preserve the existing config loader, validators, retirement coordinator and command cancellation pattern. No additional simplification edits or skipped findings. The focused integration cases and all 13 workspace checks pass, including 162 unit tests and zero Vite+ findings. Browser verification is unchanged because this adds no browser surface.

Neon CLI authentication and project reads are now verified. The selected project is `late-moon-69483649`, named `loom`, PostgreSQL 18 in `aws-us-east-1`. The previously supplied ID omitted its final 9; its authorization error did not prove the authenticated profile was broken. A live branch list succeeds and reports the ready default `main` branch `br-solitary-sun-aw6tznuf`. No cloud resources were changed. Full cloud acceptance and the remaining framework phases are still required.

Final validation for this increment: the complete PostgreSQL integration suite passes 96 tests with 829 Bun expectations plus Node assertions. Formatting and diff checks pass.
