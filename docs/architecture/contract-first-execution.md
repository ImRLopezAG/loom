# Contract-first application execution

This records implementation of the revised application API requested on September 25, 2026. Earlier oRPC acceptance does not establish acceptance of this revision.

## Settled decisions

- Every application procedure has a native oRPC contract. Support object and schema-aware callback declarations.
- Streaming contracts are explicit. Use native `liveOptions` on their native client methods; do not synthesize `.live` procedures.
- Accept Standard Schema, including Zod and Valibot, without replacing the user's library. Test runtime validation, transformed types, and OpenAPI separately.
- Generate the native client factory; keep native oRPC TanStack query/mutation/live options. Remove Loom callable-option wrappers and `clientMode`.
- Configure typed environment and named contract-bound builders in `app.config.ts`; use `auth.config.ts` for authentication integration.
- Inject typed database, tables, validators, identity, environment and Effect services. Select transaction behavior internally using forwarded native operation context and safe non-TanStack semantics.
- Components remain future work.

## Execution order and evidence

1. Standard Schema environment and contract foundations: mixed-vendor runtime and negative type checks; fail-closed validation and secret redaction review.
2. Contract-bound application builders and context: middleware refinement, Effect services, mandatory contract matching, initialization without production credentials.
3. Native client and transaction integration: actual HTTP/WebSocket operation forwarding, direct calls and OpenAPI semantics, streaming cancellation, isolation and retries.
4. Project discovery and generation: stable generated imports, clean-project bootstrap, missing/extra implementations, browser dependency and credential isolation.
5. Migrate examples, CLI and documentation: exercise generated native clients and all three native option forms; remove superseded public APIs.
6. Acceptance: full build/type/lint suites, PostgreSQL integration, browser checks and real disposable Neon Functions deployment; review and scoped commits per completed unit.

Existing README and untracked plan/explainer changes are excluded from this work. Work continues on `feat/orpc-effect-core` and existing PR #1.

## Evidence

- Environment tests first failed because `parseApplicationEnvironment` did not exist. Tests cover mixed Zod/Valibot transformations, async schemas, redacted failures, undeclared variables, inherited properties and special property names.
- Validation foundation verified: core build; core browser/server and test TypeScript checks; full Oxlint; scoped formatting; all 195 unit tests pass. Zod OpenAPI first failed with `Unsupported input schema vendor: zod`; the new Standard JSON Schema converter passes representable contracts and rejects unsupported conversions. No Zod runtime dependency was added.
- Code/security review of the validation foundation ran sequentially in the implementation context, not as independent review: checked secret leakage through errors/causes, inherited environment properties, prototype-sensitive names, immutable outputs, transformed type inference, browser export isolation and fail-closed OpenAPI conversion. No unresolved findings. Runtime startup wiring and generated consumers remain later work; these tests do not establish Neon acceptance.
- Native oRPC TanStack options expose an operation context symbol. Prior executable probes demonstrated query/mutation labels at the link and HTTP header forwarding. Production WebSocket and server integration remain to be verified.
- Native contract declarations: object/callback forms preserve schemas, errors and explicit iterator outputs through native `implement`/`call`. Five focused tests pass, along with core browser/server and consumer type checks, full lint and formatting. Negative type checks cover incorrect output, missing routes, unknown routes, finite outputs on streaming contracts and invalid client input. Discovery rejects output-less contracts, implementations mixed into declarations, and cycles; shared contract subtrees remain valid.
- Contract declaration code/security review ran sequentially in this context: checked runtime-only server dependencies are absent from the contract export, callbacks receive only explicit bindings, recursive traversal rejects cycles, and no unsafe cast widens native client or handler types. Generated registration and application-wide completeness are not yet wired.
- Native operation forwarding: an actual Bun WebSocket server and native TanStack QueryClient/MutationObserver first reproduced missing operation labels. The Loom native link now forwards oRPC's operation context per call; HTTP/WebSocket ingress parses the label independently of authenticated identity. All 12 transport integration tests, 12 targeted client unit tests, core/test/e2e type checks, build and lint pass. Invalid/combined/case-mismatched labels fail before dispatch, forged credentials still fail authentication, and direct calls are identified as `call`.
- Operation forwarding code/security review ran sequentially in this context: verified per-request headers rather than shared socket state, enum validation on both ingress paths, HTTP error mapping, CORS allowance, cancellation and identity ownership. This commit does not yet change transaction policies or establish native streaming runtime acceptance.
- Automatic transaction policy: PostgreSQL 18 integration first reproduced a query-labeled automatic handler receiving a read-write transaction. The corrected boundary gives read-only repeatable-read snapshots to query/infinite/streamed/live intent, serializable writes to mutation/direct intent, and one attempt to automatic handlers. Direct server/OpenAPI calls need no idempotency key; explicit keys retain receipt replay and authorization. Legacy explicit-policy behavior remains during migration. A retryable legacy parent cannot invoke an automatic handler and accidentally replay its external work.
- Transaction verification: PostgreSQL integration passes 45 assertions, including database rejection of writes through query intent, direct calls without keys, SQLSTATE 40001 without automatic handler replay, and mixed nesting rejection. Core/e2e type checks and lint pass. Code/security review checked authorization stays inside the transaction, receipt validation remains unchanged, nested read-to-write escalation remains denied, and transaction guards still own database lifetime.
- Foundation simplification pass used ce-simplify-code's reuse, quality and efficiency rubrics sequentially. Reused native `OperationType` instead of maintaining a duplicate type union (reuse: 1); no quality or efficiency changes. Kept strict OpenAPI conversion because upstream's fallback is not behavior-equivalent (skipped: 1). Workspace typecheck/build dependencies passed all 13 tasks; focused behavior suites above cover the changes.
- Application builders now preserve native contract routes and middleware context refinement while injecting typed schema bindings, database, Effect services and validated environment. Generation constructs them without credentials; runtime scopes validate once and isolate concurrent instances, including two environments for the same application definition.
- Application verification: all 202 unit tests, core/browser/test/e2e type checks, full lint and the PostgreSQL transaction suite (46 assertions) pass. The PostgreSQL test caught native contract implementers intentionally not merging middleware metadata; internal binding now recognizes the actual middleware capability without altering the public contract. Conflicting database capabilities fail closed.
- Application code/security review ran sequentially in this context: checked native input/output schema identity, unknown environment/table/ID and wrong-output type rejection, environment scope ownership across concurrent calls, missing/foreign scope rejection, secret-free builder construction, and database middleware detection. The generic native builder cast preserves middleware-derived context types; it does not weaken handler contracts. Runtime startup and generated project registration remain subsequent units.
- Runtime startup now validates the application's declared environment before opening connections and scopes public/internal graph calls to that runtime. The assembled HTTP/PostgreSQL test verifies transformed environment delivery and authorization; missing environment fails before an unreachable database connection. Both transaction and runtime integration suites pass (92 assertions). Review checked scope ownership, startup ordering, direct-call compatibility and preservation of existing job/activation/shutdown behavior.
- Contract-first discovery/generation now reads `app.config.ts`, `auth.config.ts`, and default contract declarations under `contracts/`. Contract filenames own route prefixes; `contracts/internal/` mirrors server-only implementations. Generation bootstraps native builders without reading secrets, emits typed project registration and stable `_generated/rpc`/contract imports, and checks that every discovered implementation uses its exact declared schemas/errors/metadata and application database capability.
- Generated contract-first clients own the authenticated transport and expose its native `client` plus disposal. They use native oRPC TanStack options unchanged, with no callable-option wrappers or `clientMode`. Contract-first server bindings no longer expose author-facing database policy middleware. Legacy project loading remains temporarily available while examples/CLI migrate.
- Generation verification: clean-project mixed Zod/Valibot contracts, refined auth context, typed environment/schema, explicit iterator payloads, native query/mutation/live options, missing/extra/wrong-contract rejection, internal route exclusion and browser bundling pass. No server imports or environment names enter the bundled browser client. Existing generation tests also pass (3 tests, 50 assertions total); all 202 unit tests and all 13 workspace typecheck/build tasks pass, with full lint clean.
- Generation code/security review ran sequentially: checked path-segment/prototype restrictions, exact contract identity, browser runtime dependency isolation, internal route filtering, generated-file non-file refusal, and startup environment ownership. Native oRPC exposes `liveOptions` even for finite contracts: its data type is `never` and runtime requires an iterator; Loom does not replace that native API. Middleware errors must be declared on the shared contract base to be available across router-wide middleware. Actual streaming runtime acceptance and example migration remain incomplete.
- Automatic write handlers can now use invocation storage without restoring the legacy nontransactional authoring mode. Storage remains independently committed; automatic handlers have one attempt, and storage work joins database work ownership so caught/unawaited failures prevent that handler's database commit. Read-intent calls still cannot acquire storage side effects; retryable legacy handlers remain prohibited.
- The real PostgreSQL storage scope test first reproduced the automatic-call rejection, then passed with authorized access, query-intent refusal, cross-owner denial, caught storage-error database rollback, cancellation and shutdown draining. Transaction/runtime suites also pass (3 integration tests, 92 expect assertions plus Node assertions). Core/e2e type checks, build, formatting and lint pass. Sequential code/security review found and fixed identity/backend checks occurring outside database work ownership; independent storage commits are not described as atomic with application SQL.
- Neon release declarations now automatically include custom environment names from `app.config.ts`, while explicit source-name mappings remain supported. Deployment validates effective raw custom values with Standard Schema before provider work; transformed values stay runtime-owned. Absent optional values are sent as deletions because Neon merges deployment environments. Provider-injected Postgres/Auth/Data API/AI Gateway/Object Storage variables are excluded from local forwarding and validated by runtime startup.
- Verified against Neon's current environment-variable documentation: https://neon.com/docs/compute/functions/environment-variables . Added the documented Auth/Data API/AI Gateway defaults to protected injected variables. This is a Loom guard against overriding branch identity/credentials, not a claim that Neon reserves those names.
- Environment release tests verify automatic declaration wiring, mapped source values, preservation of raw values for runtime transforms, optional deletion, required-variable rejection and secret-free validation errors. Generation/browser tests, tooling/e2e typechecks and lint pass. Sequential code/security review checked that source mappings remain subject to privileged-variable restrictions, defaults do not copy local provider credentials, and no secret values enter declarations or generated artifacts. Live Neon deployment acceptance remains pending.
- Explicit native iterator contracts now pass the runtime wire boundary without buffering. Native schemas validate each yielded/returned value; Loom additionally enforces supported wire values and per-event byte limits. Stream creation always receives read-only database authority, including direct calls, and cannot use write receipts. Finite permissive schemas cannot implicitly become streaming contracts.
- Streaming boundary verification: five focused tests cover lazy validation, redacted event and cleanup failures, declared errors, cancellation cleanup, undeclared iterators and oversized-event cleanup. PostgreSQL/HTTP acceptance delivers two events and verifies read-only authority (50 assertions); core/browser/test/e2e type checks and scoped lint pass. Sequential code/security review checked cleanup error mapping, event-boundary limits and policy consistency with storage. Runtime-wide stream ownership, reactive snapshot evaluation and live-options acceptance are separate remaining work; this unit does not establish complete subscription support.
- Runtime graph ownership now retains explicit iterators through completion/cancellation and drains them before closing database resources. Deferred iteration and cleanup restore the originating application's environment scope. Streams completing after shutdown are closed before rejection, and aborted pending reads cannot publish late values. Iterator producers must cooperate with their invocation abort signal when waiting on external work.
- Lifetime verification: three focused tests verify environment isolation, blocked cleanup draining, late-emission refusal and late-created stream cleanup. The real PostgreSQL/HTTP runtime test leaves a stream open and verifies cleanup before shutdown resolves (53 assertions); core/browser/test/e2e typechecks, build and scoped lint pass. Sequential code/security review checked shutdown ordering, abort-listener removal, cleanup rejection handling, application scoping and existing transport session-expiry cancellation. Reactive snapshot evaluation and actual native live-options acceptance remain incomplete.
- Explicit stream handlers can now return `context.live(({ db, tables, env }) => readSnapshot)`. A lazy, bounded subscription reevaluates the full handler/middleware pipeline, executes the callback in a fresh read-only transaction, and validates each published value through the native iterator contract. The callback context contains fresh base application bindings; refined middleware values can be captured from the handler context, which is also rebuilt on every evaluation. Streaming handlers and their middleware must remain safe to reevaluate; external side effects are not made idempotent by a read-only SQL transaction.
- Native `createTanstackQueryUtils(client).watch.liveOptions(...)` now receives real PostgreSQL snapshots over WebSocket in the integration suite. A concurrent write during a held evaluation first yields the old snapshot and then the new one with matching revision behavior. Input transformation runs once; captured transformed URL values retain their native representation. Authorization reruns before each evaluation, and revoking permission ends the subscription with its declared error. No transaction is idle between updates.
- Reactive verification: new native live-options integration passes 14 assertions; combined legacy live/runtime/reactive suites pass 74 assertions. Generated consumers typecheck `context.live` with project tables and environment, including negative unknown-table checks. All 209 unit tests and core/browser/test/e2e typechecks pass. Sequential code/security review checked route-specific snapshot scopes, fresh invocation/database ownership, input cloning, authorization revocation, bounded pending values, cancellation, and output validation before publication. Review fixed transformed URL loss by reusing native RPC serialization for captured input. This remains local PostgreSQL/WebSocket acceptance; new Neon acceptance is pending.
- Both examples now declare native contracts, use `app.config.ts`/`auth.config.ts`, implement generated named routers, and connect through the generated native client. Task/catalog subscriptions use explicit iterator contracts and native `liveOptions`; finite job status uses `queryOptions`, and writes use `mutationOptions`. Development and acceptance runtime assembly now carries the application environment scope.
- Example verification: task lifecycle/ownership/schema evolution (four tests) and upload owner/retry workflow pass against PostgreSQL 18. Both example typechecks and scoped lint pass. React Doctor reports no issues (tasks 89/100, catalog 85/100). Collaborative browser acceptance created a project/task, persisted a completion toggle, verified Bob cannot see Alice's workspace, and restored Alice's state after sign-in. Local fixtures remain explicitly separate from Neon acceptance.
- Sequential code/security review preserved owner predicates, bounded reads, native contract validation, internal job boundaries and per-session query caches. Review fixed scheduler references to use the actual native router leaf after oRPC clones procedures. Browser testing exposed asynchronous cancellation frames sent after socket close; the transport now rejects closed-socket sends, six transport tests pass, and browser sign-out adds no socket console errors. The disposable browser database and runtime were stopped after verification.
- `loom init` now creates mandatory contract declarations, `app.config.ts`, `auth.config.ts`, and native generated-router implementations. The starter denies authorization until configured. CLI/codegen fixtures now exercise native client types, explicit live contracts, internal contract registration, immutable cron targets, storage events, watcher recovery and release activation through this layout.
- Verification: CLI and packed-consumer suites pass (10 tests, 117 assertions), native generation/capability suites pass (two tests, 41 assertions), mixed-vendor application generation and tooling/e2e typechecks pass. Full PostgreSQL integration ran 150 tests: 149 passed and one old development-watch fixture failed; after migrating that fixture, its full save/schema/job/cron/recovery/quarantine workflow passes in 58 seconds. This is a full run plus focused correction, not a claim that the original run was green.
- Sequential code/security review checked deny-by-default initialization, exclusive file creation/rollback, symlink refusal, contract/implementation identity, internal route exclusion, browser bundle isolation and bounded retained generations. The native proxy's runtime property access does not prove route exclusion; negative consumer typechecks and server dispatch checks provide that evidence. App-less legacy project loading and old callable-option exports remain a separate removal unit.
- Generation now requires `app.config.ts` and unconditionally verifies all public/internal implementations against their native contracts. Removed app-less authoring bindings and callable-option artifact generation; all generated clients use the native transport/client factory. The generation format identity advances to invalidate cached artifacts.
- Route removal now prunes obsolete generated contract imports while preserving unrelated files. Pruning refuses symlinks, including the contracts directory itself. Review caught and fixed that root-directory case before commit; a regression proves an external generated-looking file is preserved.
- Verification: CLI/codegen/application suites pass (12 tests before the pruning addition); focused codegen/application suites pass after cleanup and symlink checks (three tests, 52 assertions). Tooling/e2e typechecks and scoped lint pass. Sequential code/security review covered mandatory contract enforcement, removal of fallback paths, generated-file ownership, symlink refusal and cache invalidation. Old runtime/query helper exports remain a separate removal unit; this does not claim new Neon acceptance.
- Removed `@loom/core/query` and its callable option, optimistic-pause and session wrappers. Consumers now use native oRPC TanStack utilities, QueryClient ownership and mutation callbacks; React exports remain upstream reexports. Native overrides, skipToken, explicit retry intent keys, stream sharing/cancellation and finite-endpoint SSR hydration remain covered.
- Verification: 205 unit tests pass, including React tests now included by the root test glob; native SSR/retry and process-recovery integration checks pass. Browser optimistic state now uses native mutation variables to survive refetch. Task cross-client/reconnect and upload/retry/owner-isolation browser acceptance pass. Core build, test/e2e typechecks and scoped lint pass.
- Browser regression testing found two migration assumptions: awaiting invalidation of an endless live query prevents mutation settlement, and building a frontend before explicit API generation embeds stale version identity. Fixtures and the subscription guide now handle both. The initial browser run failed three cases; focused reruns pass after correction.
- Sequential code/security review checked identity-scoped QueryClient disposal, explicit retry keys, abort propagation, native override semantics and removal of obsolete exports. Native cache keys do not provide authorization or automatic identity partitioning; docs make app ownership explicit. Cloud fixtures have been migrated separately but have not yet been exercised on Neon for this goal.
- Removed public `clientMode`, its default metadata and the synthesized `createLiveProcedure` runtime branch. Runtime streaming is determined by explicit iterator contracts; ordinary handlers remain finite. Database authority continues to follow validated operation intent, with iterator contracts always read-only.
- Verification: core build, core/e2e typechecks, scoped lint and 11 native-contract unit tests pass. Runtime, snapshot evaluation, explicit application subscriptions and API SIGKILL recovery pass against PostgreSQL. The separate low-level coordinator/wire test passes after migrating its explicit iterator boundary and keeping source-module transaction scopes consistent. Snapshot/wire tests retain empty-result invalidation, rollback, Date/bigint, authorization and cancellation checks.
- Sequential code/security review confirmed no metadata can silently turn a finite procedure into a stream, no removed wrapper remains in the runtime graph, and output validation/read-only checks remain in place. Internal finite/read/write primitives remain available for runtime assembly and focused tests; generated application authoring uses automatic policy only. Documentation example migration follows separately.
- Documentation examples now use actual CLI generation rather than handwritten server bindings. Added mandatory contracts and application/auth configuration; the docs build generates its example client/router declarations and Turbo tracks those outputs. The packed-consumer test generates from the same examples against packed core/tooling artifacts, then compiles their handlers and validates schemas/contracts.
- Updated authoring, quickstart, authorization, relations, jobs and validator guidance for native options and automatic single-attempt transaction selection. Both Zod and Valibot are supported at Standard Schema boundaries; nested composition still uses each library's own API. Components remain explicitly unimplemented.
- Verification: packed docs consumer passes, documentation build/typecheck and e2e typecheck pass, and scoped lint passes. Sequential code/security review checked required output contracts, ownership predicates, internal route placement, generated artifacts excluded from source, environment secrecy and accurate retry/storage limitations. Fixed an empty preexisting handwritten `_generated` directory by removing that owned empty directory so generation can establish ownership normally.
- Cloud fixtures now use mandatory public/internal contracts and generated builders. Hosted services verify transformed custom application env plus provider-injected `NEON_BRANCH`, Promise/Effect storage, cross-owner refusal, OpenAPI, private-route isolation and stale-version refusal. Tasks and jobs/storage examples pass actual Neon Auth and Functions acceptance; populated-release upgrade passes retained legacy-job migration. All run on owned disposable branches of project `late-moon-69483649` in `aws-us-east-1`.
- The native live-options acceptance passed 100 subscriptions on two Functions, terminated-listener recovery, 600 observed writes over 60 seconds, cancellation and bounded pool cleanup (8 total connections, 0 active, 0 idle transactions). Observed p95 latency was 300.2ms after commit acknowledgment / 393.4ms from write start. This is a one-minute acceptance measurement, not a ten-minute performance claim. Diagnostics now use native queryOptions rather than raw calls; earlier raw-call metrics returned INTERNAL_SERVER_ERROR under load, and their cause is not established. A full-runtime PostgreSQL regression verifies automatic conflict errors remain CONFLICT and handlers execute once.
- Full verification before final review: build, all 13 workspace typechecks, 204 unit tests, 150 PostgreSQL integration tests, six browser suites and the packed documentation consumer passed. Simplification reviewed reuse, quality and efficiency sequentially under workspace instructions; no behavior-preserving changes were warranted. Native serializers, transaction ownership, bounded snapshot state, exact contract identity and generated-file safety checks remain intact.
