diff --git a/api-postgres.d.mts b/api-postgres.d.mts
index 6bb57f48913618880a417c8872748469904921e6..7266900f41fc562146f2aa835bacd79a937e91b8 100644
--- a/api-postgres.d.mts
+++ b/api-postgres.d.mts
@@ -6,7 +6,11 @@ import { PgAsyncDatabase } from "drizzle-orm/pg-core/async";
 
 //#region src/ext/api-postgres.d.ts
 declare const generateDrizzleJson: (imports: Record<string, unknown>, prevId?: string, schemaFilters?: string[]) => Promise<PostgresSnapshot>;
-declare const generateMigration: (prev: PostgresSnapshot, cur: PostgresSnapshot) => Promise<string[]>;
+type PostgresRenameHint =
+  | { type: "rename"; kind: "table"; from: [string, string]; to: [string, string] }
+  | { type: "rename"; kind: "column"; from: [string, string, string]; to: [string, string, string] };
+declare const generateMigration: (prev: PostgresSnapshot, cur: PostgresSnapshot, renameHints?: PostgresRenameHint[]) => Promise<string[]>;
+declare const inspectSchema: (drizzleInstance: PgAsyncDatabase<any>, schemaFilters: string[]) => Promise<PostgresSnapshot>;
 declare const pushSchema: (imports: Record<string, unknown>, drizzleInstance: PgAsyncDatabase<any>, entitiesConfig?: EntitiesFilterConfig, migrationsConfig?: {
   table?: string;
   schema?: string;
@@ -32,4 +36,4 @@ declare const up: (it: Record<string, any>) => {
   hints: string[];
 };
 //#endregion
-export { generateDrizzleJson, generateMigration, pushSchema, startStudioServer, up };
\ No newline at end of file
+export { generateDrizzleJson, generateMigration, inspectSchema, pushSchema, startStudioServer, up };
\ No newline at end of file
diff --git a/api-postgres.d.ts b/api-postgres.d.ts
index 8d206277192490fa70d2000aa26ac7171ef56b53..e108122510f8ce4c1270d7fc0f626e92446a9b26 100644
--- a/api-postgres.d.ts
+++ b/api-postgres.d.ts
@@ -6,7 +6,11 @@ import { PgAsyncDatabase } from "drizzle-orm/pg-core/async";
 
 //#region src/ext/api-postgres.d.ts
 declare const generateDrizzleJson: (imports: Record<string, unknown>, prevId?: string, schemaFilters?: string[]) => Promise<PostgresSnapshot>;
-declare const generateMigration: (prev: PostgresSnapshot, cur: PostgresSnapshot) => Promise<string[]>;
+type PostgresRenameHint =
+  | { type: "rename"; kind: "table"; from: [string, string]; to: [string, string] }
+  | { type: "rename"; kind: "column"; from: [string, string, string]; to: [string, string, string] };
+declare const generateMigration: (prev: PostgresSnapshot, cur: PostgresSnapshot, renameHints?: PostgresRenameHint[]) => Promise<string[]>;
+declare const inspectSchema: (drizzleInstance: PgAsyncDatabase<any>, schemaFilters: string[]) => Promise<PostgresSnapshot>;
 declare const pushSchema: (imports: Record<string, unknown>, drizzleInstance: PgAsyncDatabase<any>, entitiesConfig?: EntitiesFilterConfig, migrationsConfig?: {
   table?: string;
   schema?: string;
@@ -32,4 +36,4 @@ declare const up: (it: Record<string, any>) => {
   hints: string[];
 };
 //#endregion
-export { generateDrizzleJson, generateMigration, pushSchema, startStudioServer, up };
\ No newline at end of file
+export { generateDrizzleJson, generateMigration, inspectSchema, pushSchema, startStudioServer, up };
\ No newline at end of file
diff --git a/api-postgres.js b/api-postgres.js
index bce99a92d2f9d7e383488f119329124939fdb8b8..6218705a8db217f1fb9b52c2194e71b58fdbb767 100644
--- a/api-postgres.js
+++ b/api-postgres.js
@@ -140254,16 +140254,37 @@ const generateDrizzleJson = async (imports, prevId, schemaFilters) => {
 	}
 	return toJsonSnapshot(ddl, prevId ? [prevId] : [originUUID], []);
 };
-const generateMigration = async (prev, cur) => {
+const generateMigration = async (prev, cur, renameHints = []) => {
 	const { resolver } = await Promise.resolve().then(() => (init_prompts(), prompts_exports));
 	const { ddlDiff } = await Promise.resolve().then(() => (init_diff(), diff_exports));
+	const { HintsHandler, parseHints } = await Promise.resolve().then(() => (init_hints(), hints_exports));
+	const hints = new HintsHandler(parseHints(renameHints));
 	const from = createDDL$4();
 	const to = createDDL$4();
 	for (const it of prev.ddl) from.entities.push(it);
 	for (const it of cur.ddl) to.entities.push(it);
-	const { sqlStatements } = await ddlDiff(from, to, resolver("schema"), resolver("enum"), resolver("sequence"), resolver("policy"), resolver("role"), resolver("privilege"), resolver("table"), resolver("column"), resolver("view"), resolver("unique"), resolver("index"), resolver("check"), resolver("primary_key"), resolver("foreign key"), "default");
+	const { sqlStatements } = await cliContext.run({ output: "json", interactive: false }, () => ddlDiff(from, to, resolver("schema", hints), resolver("enum", hints), resolver("sequence", hints), resolver("policy", hints), resolver("role", hints), resolver("privilege", hints), resolver("table", hints), resolver("column", hints), resolver("view", hints), resolver("unique", hints), resolver("index", hints), resolver("check", hints), resolver("primary_key", hints), resolver("foreign key", hints), "default"));
+	if (hints.hasMissingHints()) throw new Error(`Unresolved migration hints: ${JSON.stringify(hints.toResponse().unresolved)}`);
 	return sqlStatements;
 };
+const inspectSchema = async (drizzleInstance, schemaFilters) => {
+ init_snapshot$5();
+ if (!schemaFilters.length) throw new Error("Introspection requires explicit schema filters");
+ const { prepareEntityFilter } = await Promise.resolve().then(() => (init_pull_utils(), pull_utils_exports));
+ const { fromDatabaseForDrizzle } = await Promise.resolve().then(() => (init_introspect(), introspect_exports));
+ const { sql } = require("drizzle-orm");
+ let pending = Promise.resolve();
+ const db = { query: (query, _params) => {
+  const next = pending.then(async () => (await drizzleInstance.execute(sql.raw(query))).rows);
+  pending = next.then(() => {}, () => {});
+  return next;
+ } };
+ const filter = prepareEntityFilter("postgresql", { schemas: schemaFilters, tables: [], extensions: [], entities: void 0 }, []);
+ const previous = await fromDatabaseForDrizzle(db, filter, () => {}, { schema: "drizzle", table: "__drizzle_migrations" });
+ const { ddl, errors } = interimToDDL(previous);
+ if (errors.length) throw new Error("PostgreSQL introspection produced unsupported metadata");
+ return toJsonSnapshot(ddl, [], []);
+};
 const pushSchema = async (imports, drizzleInstance, entitiesConfig, migrationsConfig) => {
 	const { prepareEntityFilter } = await Promise.resolve().then(() => (init_pull_utils(), pull_utils_exports));
 	const { resolver } = await Promise.resolve().then(() => (init_prompts(), prompts_exports));
@@ -140338,6 +140359,7 @@ const up = upToV8;
 //#endregion
 exports.generateDrizzleJson = generateDrizzleJson;
 exports.generateMigration = generateMigration;
+exports.inspectSchema = inspectSchema;
 exports.pushSchema = pushSchema;
 exports.startStudioServer = startStudioServer;
 exports.up = up;
diff --git a/api-postgres.mjs b/api-postgres.mjs
index f2d2742df79ad0269a6becb21a48e352a321315e..f28d9517ec0ea890153ad3e86213c8d41f8db690 100644
--- a/api-postgres.mjs
+++ b/api-postgres.mjs
@@ -140239,16 +140239,37 @@ const generateDrizzleJson = async (imports, prevId, schemaFilters) => {
 	}
 	return toJsonSnapshot(ddl, prevId ? [prevId] : [originUUID], []);
 };
-const generateMigration = async (prev, cur) => {
+const generateMigration = async (prev, cur, renameHints = []) => {
 	const { resolver } = await Promise.resolve().then(() => (init_prompts(), prompts_exports));
 	const { ddlDiff } = await Promise.resolve().then(() => (init_diff(), diff_exports));
+	const { HintsHandler, parseHints } = await Promise.resolve().then(() => (init_hints(), hints_exports));
+	const hints = new HintsHandler(parseHints(renameHints));
 	const from = createDDL$4();
 	const to = createDDL$4();
 	for (const it of prev.ddl) from.entities.push(it);
 	for (const it of cur.ddl) to.entities.push(it);
-	const { sqlStatements } = await ddlDiff(from, to, resolver("schema"), resolver("enum"), resolver("sequence"), resolver("policy"), resolver("role"), resolver("privilege"), resolver("table"), resolver("column"), resolver("view"), resolver("unique"), resolver("index"), resolver("check"), resolver("primary_key"), resolver("foreign key"), "default");
+	const { sqlStatements } = await cliContext.run({ output: "json", interactive: false }, () => ddlDiff(from, to, resolver("schema", hints), resolver("enum", hints), resolver("sequence", hints), resolver("policy", hints), resolver("role", hints), resolver("privilege", hints), resolver("table", hints), resolver("column", hints), resolver("view", hints), resolver("unique", hints), resolver("index", hints), resolver("check", hints), resolver("primary_key", hints), resolver("foreign key", hints), "default"));
+	if (hints.hasMissingHints()) throw new Error(`Unresolved migration hints: ${JSON.stringify(hints.toResponse().unresolved)}`);
 	return sqlStatements;
 };
+const inspectSchema = async (drizzleInstance, schemaFilters) => {
+ init_snapshot$5();
+ if (!schemaFilters.length) throw new Error("Introspection requires explicit schema filters");
+ const { prepareEntityFilter } = await Promise.resolve().then(() => (init_pull_utils(), pull_utils_exports));
+ const { fromDatabaseForDrizzle } = await Promise.resolve().then(() => (init_introspect(), introspect_exports));
+ const { sql } = await import("drizzle-orm");
+ let pending = Promise.resolve();
+ const db = { query: (query, _params) => {
+  const next = pending.then(async () => (await drizzleInstance.execute(sql.raw(query))).rows);
+  pending = next.then(() => {}, () => {});
+  return next;
+ } };
+ const filter = prepareEntityFilter("postgresql", { schemas: schemaFilters, tables: [], extensions: [], entities: void 0 }, []);
+ const previous = await fromDatabaseForDrizzle(db, filter, () => {}, { schema: "drizzle", table: "__drizzle_migrations" });
+ const { ddl, errors } = interimToDDL(previous);
+ if (errors.length) throw new Error("PostgreSQL introspection produced unsupported metadata");
+ return toJsonSnapshot(ddl, [], []);
+};
 const pushSchema = async (imports, drizzleInstance, entitiesConfig, migrationsConfig) => {
 	const { prepareEntityFilter } = await Promise.resolve().then(() => (init_pull_utils(), pull_utils_exports));
 	const { resolver } = await Promise.resolve().then(() => (init_prompts(), prompts_exports));
@@ -140321,4 +140342,4 @@ const startStudioServer = async (imports, credentials, options) => {
 };
 const up = upToV8;
 //#endregion
-export { generateDrizzleJson, generateMigration, pushSchema, startStudioServer, up };
+export { generateDrizzleJson, generateMigration, inspectSchema, pushSchema, startStudioServer, up };
