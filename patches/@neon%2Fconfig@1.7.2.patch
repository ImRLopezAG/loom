diff --git a/dist/lib/neon-api-real.js b/dist/lib/neon-api-real.js
index d03d792eabd8a5ae19320fe827ddd46ea2329b45..3224c35733e13bfa587161fb3b3ff3b21cb76341 100644
--- a/dist/lib/neon-api-real.js
+++ b/dist/lib/neon-api-real.js
@@ -983,11 +983,12 @@ function bucketToSnapshot(bucket) {
 }
 /**
 * The Neon API returns `access_level` as a free-form string (per the API guidelines:
-* responses use plain strings, not enums). Map the known values onto our union and treat
-* anything else as `private` — the safe default for an unrecognised access level.
+* responses use plain strings, not enums). Unknown or missing values cannot prove
+* private access, so reject them instead of silently treating them as private.
 */
 function normalizeBucketAccessLevel(value) {
-	return value === "public_read" ? "public_read" : "private";
+	if (value === "private" || value === "public_read") return value;
+	throw new Error("Unrecognized bucket access level");
 }
 function functionToSnapshot(fn) {
 	const snapshot = {
